import fs from 'node:fs';

function read(path){return fs.readFileSync(path,'utf8');}
function write(path,value){fs.writeFileSync(path,value);}
function patch(path,fn){const before=read(path),after=fn(before);if(after===before)throw new Error(`No change produced for ${path}`);write(path,after);console.log(`patched ${path}`);}
function replaceOnce(source,needle,replacement,label){const index=source.indexOf(needle);if(index<0)throw new Error(`Missing ${label||needle.slice(0,80)}`);if(source.indexOf(needle,index+needle.length)>=0)throw new Error(`Non-unique ${label||needle.slice(0,80)}`);return source.slice(0,index)+replacement+source.slice(index+needle.length);}
function replaceRegex(source,re,replacement,label){if(!re.test(source))throw new Error(`Missing regex ${label}`);re.lastIndex=0;return source.replace(re,replacement);}

patch('shared/ai/ai-workspace-model.v3992_0.js',source=>{
  source=replaceOnce(source,"export const AI_EVENT_CONTRACT_VERSION='ai-event-contract-v3992_0';","export const AI_EVENT_CONTRACT_VERSION='ai-event-contract-v3992_1';",'event contract');
  source=replaceOnce(source,"export const AI_DECISION_PROFILE_VERSION='ai-decision-profile-v3992_0';","export const AI_DECISION_PROFILE_VERSION='ai-decision-profile-v3992_1';",'decision profile version');
  source=replaceRegex(source,/export function explicitProfileSeed\(seed=\{\}\)\{.*?\}\nexport function decisionProfileSeed/s,`function explicitStudentSignalSeed(item={}){const dimension=cleanText(item?.dimension,60),value=cleanText(item?.value,120);if(!dimension||!value)return null;return{dimension,value,polarity:cleanText(item?.polarity,30),source:cleanText(item?.source,40)||'explicit_text'};}\nexport function explicitProfileSeed(seed={}){const primaryGoal=PRIMARY_GOALS.has(cleanText(seed.primaryGoal,60))?cleanText(seed.primaryGoal,60):'undecided',priorities=uniqueStrings(seed.priorities||[],12).filter(value=>PRIORITY_KEYS.has(value)),careerTargets=uniqueStrings(seed.careerTargets||[],8),studentSignals=(Array.isArray(seed.studentSignals)?seed.studentSignals:[]).slice(0,12).map(explicitStudentSignalSeed).filter(Boolean);return{primaryGoal,priorities,familyResourceSensitivity:seed.familyResourceSensitivity==='resource_sensitive'?'resource_sensitive':'unspecified',studyDurationTolerance:['prefer_short','long_ok'].includes(seed.studyDurationTolerance)?seed.studyDurationTolerance:'unspecified',careerTargets,studentSignals,sourceTexts:uniqueStrings(seed.sourceTexts||[],12),updatedAt:seed.updatedAt||nowIso()};}\nexport function decisionProfileSeed`,'explicit profile seed');
  source=replaceOnce(source,"export function createAiWorkspace(seed={}){",`export function decisionSeed(seed={}){const legacyText=cleanText(seed?.text,360),kind=cleanText(seed?.kind,60)||'note',rawStatus=cleanText(seed?.status,20),status=['keep','reject','pending'].includes(rawStatus)?rawStatus:(legacyText?'pending':'');if(!status)return null;let subject;if(typeof seed?.subject==='string')subject={label:cleanText(seed.subject,220)};else if(seed?.subject&&typeof seed.subject==='object')subject={school:cleanText(seed.subject.school,100),major:cleanText(seed.subject.major,140),label:cleanText(seed.subject.label,220),value:cleanText(seed.subject.value,220)};else subject={label:legacyText};const reason=cleanText(seed?.reason||legacyText,360);return{id:cleanText(seed?.id,120)||makeId('decision'),kind,status,subject,reason,text:reason,taskId:cleanText(seed?.taskId,120),sourceTurnId:cleanText(seed?.sourceTurnId,120),createdAt:seed?.createdAt||nowIso(),updatedAt:seed?.updatedAt||seed?.createdAt||nowIso()};}\n\nexport function createAiWorkspace(seed={}){`,'decision seed insertion');
  source=replaceOnce(source,"decisions:safeArray(seed.decisions,WORKSPACE_LIMITS.decisions)","decisions:(Array.isArray(seed.decisions)?seed.decisions:[]).slice(0,WORKSPACE_LIMITS.decisions).map(decisionSeed).filter(Boolean)",'workspace decisions migration');
  return source;
});

patch('shared/ai/ai-workspace-contract.v3992_0.js',source=>{
  source=replaceOnce(source,'nowIso,makeId,cloneValue,cleanText,uniqueStrings,normalizedScore,normalizedRank,viewSeed,turnSeed,agentContextSeed,semanticFrameSeed,createAiWorkspace,activeViewLabel,regionKeyLabel,applyAiViewPatch','nowIso,makeId,cloneValue,cleanText,uniqueStrings,normalizedScore,normalizedRank,viewSeed,turnSeed,agentContextSeed,semanticFrameSeed,decisionSeed,createAiWorkspace,activeViewLabel,regionKeyLabel,applyAiViewPatch','decisionSeed import');
  source=replaceRegex(source,/function mergeDecisionProfile\(workspace,command=\{\}\)\{.*?\}\nfunction createTaskFromCommand/s,`function mergeExplicitStudentSignals(existing=[],incoming=[]){const map=new Map();for(const item of [...(Array.isArray(existing)?existing:[]),...(Array.isArray(incoming)?incoming:[])]){const dimension=cleanText(item?.dimension,60),value=cleanText(item?.value,120);if(!dimension||!value)continue;map.set(dimension,{dimension,value,polarity:cleanText(item?.polarity,30),source:cleanText(item?.source,40)||'explicit_text'});}return [...map.values()].slice(0,12);}\nfunction mergeDecisionProfile(workspace,command={}){const p=command?.mentorProfile?.persistable;if(!p||typeof p!=='object')return;const explicit=workspace.decisionProfile.explicit;let changed=false;if(PRIMARY_GOALS.has(cleanText(p.primaryGoal,60))&&p.primaryGoal!=='undecided'){explicit.primaryGoal=cleanText(p.primaryGoal,60);changed=true;}const priorities=uniqueStrings(p.priorities||[],12).filter(v=>PRIORITY_KEYS.has(v)&&v!=='feasible_set');if(priorities.length){explicit.priorities=uniqueStrings([...explicit.priorities,...priorities],12);changed=true;}if(p.familyResourceSensitivity==='resource_sensitive'){explicit.familyResourceSensitivity='resource_sensitive';changed=true;}if(['prefer_short','long_ok'].includes(p.studyDurationTolerance)){explicit.studyDurationTolerance=p.studyDurationTolerance;changed=true;}const careerTargets=uniqueStrings(p.careerTargets||[],8);if(careerTargets.length){explicit.careerTargets=uniqueStrings([...(explicit.careerTargets||[]),...careerTargets],8);changed=true;}const studentSignals=mergeExplicitStudentSignals(explicit.studentSignals,p.studentSignals);if(studentSignals.length!==((explicit.studentSignals||[]).length)||JSON.stringify(studentSignals)!==JSON.stringify(explicit.studentSignals||[])){explicit.studentSignals=studentSignals;changed=true;}if(changed){explicit.sourceTexts=uniqueStrings([...explicit.sourceTexts,cleanText(command.rawText,280)],12);explicit.updatedAt=nowIso();workspace.decisionProfile.updatedAt=explicit.updatedAt;}}\nfunction createTaskFromCommand`,'merge decision profile');
  source=replaceOnce(source,"if(explicit.studyDurationTolerance!=='unspecified')pieces.push(`培养周期:${explicit.studyDurationTolerance}`);","if(explicit.studyDurationTolerance!=='unspecified')pieces.push(`培养周期:${explicit.studyDurationTolerance}`);if(explicit.careerTargets?.length)pieces.push(`职业方向:${explicit.careerTargets.join(',')}`);if(explicit.studentSignals?.length)pieces.push(`孩子明确条件:${explicit.studentSignals.map(item=>`${item.dimension}:${item.value}`).join(',')}`);",'memory profile summary');
  source=replaceOnce(source,"  if(event.type==='selection_snapshot_imported')",`  if(event.type==='topic_started'){const previousTask=workspace.agentContext?.currentTask||'';workspace.agentContext=agentContextSeed({currentTask:'',previousTask,focus:{},contextUsage:{score:workspace.examContext?.score?'remembered':'cleared',region:'remembered',major:'cleared',school:'cleared',bottomLine:'remembered'}},workspace.examContext);workspace.mainTaskId='';workspace.activeView={...workspace.activeView,id:makeId('view'),target:'candidates',majorKeywords:[],schoolNames:[],combination:'replace',sourceText:'',updatedAt:nowIso()};}\n  if(event.type==='selection_snapshot_imported')`,'topic event');
  source=replaceOnce(source,"  if(event.type==='decision_saved'){workspace.decisions.unshift({id:makeId('decision'),text:cleanText(event.payload.text,360),taskId:cleanText(event.payload.taskId,120)||workspace.mainTaskId,createdAt:nowIso()});workspace.decisions=workspace.decisions.slice(0,WORKSPACE_LIMITS.decisions);}","  if(event.type==='decision_saved'){const saved=decisionSeed({...event.payload,taskId:cleanText(event.payload.taskId,120)||workspace.mainTaskId});if(saved){workspace.decisions=workspace.decisions.filter(item=>!(item.kind===saved.kind&&JSON.stringify(item.subject)===JSON.stringify(saved.subject)));workspace.decisions.unshift(saved);workspace.decisions=workspace.decisions.slice(0,WORKSPACE_LIMITS.decisions);}}",'typed decision saved');
  source=replaceRegex(source,/function compactDecisionProfileForServer\(profile=\{\}\)\{.*?\}\nfunction compactConversationMemoryForServer/s,`function compactDecisionProfileForServer(profile={}){const explicit=profile?.explicit||{};return{version:cleanText(profile?.version,80),explicit:{primaryGoal:cleanText(explicit.primaryGoal,60),priorities:uniqueStrings(explicit.priorities||[],12),familyResourceSensitivity:cleanText(explicit.familyResourceSensitivity,40),studyDurationTolerance:cleanText(explicit.studyDurationTolerance,40),careerTargets:uniqueStrings(explicit.careerTargets||[],8),studentSignals:(Array.isArray(explicit.studentSignals)?explicit.studentSignals:[]).slice(0,12).map(item=>({dimension:cleanText(item?.dimension,60),value:cleanText(item?.value,100),polarity:cleanText(item?.polarity,30),source:cleanText(item?.source,40)}))},inferred:(Array.isArray(profile?.inferred)?profile.inferred:[]).slice(0,8).map(item=>({key:cleanText(item?.key,60),value:cleanText(item?.value,100),confidence:Math.max(0,Math.min(1,Number(item?.confidence||0)))}))};}\nfunction compactDecisionForServer(item={}){return{id:cleanText(item?.id,120),kind:cleanText(item?.kind,60),status:cleanText(item?.status,20),subject:item?.subject&&typeof item.subject==='object'?{school:cleanText(item.subject.school,80),major:cleanText(item.subject.major,100),label:cleanText(item.subject.label,160),value:cleanText(item.subject.value,160)}:{label:cleanText(item?.subject,160)},reason:cleanText(item?.reason||item?.text,220),createdAt:item?.createdAt||''};}\nfunction compactConversationMemoryForServer`,'compact profile and decisions');
  source=replaceOnce(source,'decisionProfile:compactDecisionProfileForServer(workspace.decisionProfile),decisionStage:workspace.decisionStage','decisionProfile:compactDecisionProfileForServer(workspace.decisionProfile),decisions:workspace.decisions.slice(0,20).map(compactDecisionForServer),decisionStage:workspace.decisionStage','server decisions payload');
  return source;
});

patch('functions/_lib/ai/mentor-profile.js',source=>{
  source=`import {careerTargetsFromText,studentSignalsFromText} from './parent-semantic-frame.js';\n`+source;
  source=replaceRegex(source,/function persistableFromText\(source\)\{.*?\}\n\nexport function deterministicMentorProfile/s,`function persistableFromText(source){\n  const primaryGoal=detectPrimaryGoal(source)||'undecided';\n  const priorities=detectPriorities(source).filter(value=>value!=='feasible_set');\n  const familyResourceSensitivity=explicitResourceSensitivity(source);\n  const studyDurationTolerance=explicitStudyDuration(source);\n  const careerTargets=careerTargetsFromText(source);\n  const studentSignals=studentSignalsFromText(source);\n  const hasExplicit=/((更|最|主要|优先|第一|看重|倾向|希望).{0,8}(就业|城市|学校|专业|稳定|收入|学费|成本)|普通家庭|没资源|没背景|不想读太久|本科就就业|本科就业|愿意读研|接受长学制|考公|编制|铁饭碗)/.test(source)||careerTargets.length>0||studentSignals.length>0;\n  return hasExplicit?{primaryGoal,priorities,familyResourceSensitivity,studyDurationTolerance,careerTargets,studentSignals}:{primaryGoal:'undecided',priorities:[],familyResourceSensitivity:'unspecified',studyDurationTolerance:'unspecified',careerTargets:[],studentSignals:[]};\n}\n\nexport function deterministicMentorProfile`,'mentor persistable');
  return source;
});

patch('functions/_lib/ai/turn-orchestrator.js',source=>{
  source=replaceOnce(source,"import {runEducationKnowledge} from './education-knowledge-runtime.js';","import {runEducationKnowledge} from './education-knowledge-runtime.js';\nimport {reflectDecisionTurn} from './decision-reflection.js';",'reflection import');
  source=replaceOnce(source,'  result.pendingChecks=pendingChecksFor(result,regionExecution);','  result.pendingChecks=pendingChecksFor(result,regionExecution);\n  result.decisionReflection=reflectDecisionTurn({workspace:{...workspace,activeView:view},command,result});','reflection execution');
  return source;
});

patch('functions/_lib/ai/next-action-engine.js',source=>{
  source=`import {deriveDecisionProgress} from '../../../shared/ai/decision-progress.v003.js';\n`+source;
  const marker='function candidates({task,school,major,score,topic,result}={}){';
  const helper=`function progressionAction(progress={},context={}){const stage=progress.recommendedStage,majors=progress.majors||[],pairs=progress.pairs||[],score=Number(context.score),s=Number.isFinite(score)&&score>=150&&score<=750?String(Math.round(score)):'',major=context.major||majors[0]||'';if(stage==='score_position')return s?action('progress-score','先确认分数位置',\`按我\${s}分，先确认辽宁物理类位次，再继续做专业和学校选择\`,'先把孩子的位置坐标稳定下来。',130):null;if(stage==='major_direction'){const names=majors.slice(0,3);return action('progress-major','先把专业方向收敛',names.length>=2?\`把\${names.join('、')}按本科就业、培养路径和我家的条件横着比较\`:'普通家庭，本科就业优先，先帮我收敛几个值得继续研究的专业方向','专业方向没收敛时，过早堆学校会增加判断负担。',125);}if(stage==='school_major'){if(major&&s)return action('progress-school-major','落到现实学校',\`按我\${s}分，辽宁\${major}有哪些学校更现实\`,'把专业方向落到真实招生学校与位置。',125);if(major)return action('progress-school-major','落到现实学校',\`辽宁\${major}在各学校2026都多少分\`,'先建立学校×专业的现实坐标。',125);if(pairs.length)return action('progress-school-major','继续研究具体组合',\`继续比较\${pairs.slice(0,2).map(item=>item.label).join('和')}\`,'从方向进入具体学校×专业。',125);return null;}if(stage==='family_tradeoff')return action('progress-tradeoff','把家庭取舍说清楚','把我目前明确的就业、读研、地域、预算和工作环境条件整理一下，告诉我还缺哪个关键取舍','学校和专业最终要服从家庭真实边界。',122);if(stage==='plan')return action('progress-plan','开始形成家庭方案','把目前重点考虑的学校和专业整理成一版家庭方案，我再继续取舍','研究结果需要进入可检查的方案，而不是停在聊天记录里。',120);if(stage==='audit')return action('progress-audit','做最后检查','帮我最后检查当前家庭方案，还缺什么、哪里重复、哪里和家庭条件冲突','最终检查只做结构与证据审计，不制造录取概率。',120);return null;}\n`;
  source=replaceOnce(source,marker,helper+marker,'progression helper');
  source=replaceRegex(source,/export function nextActionsForTurn\(\{task='',school='',major='',score=null,backgroundMajor='',topic='general',result=\{\},workspace=\{\}\}=\{\}\)\{\n  const seen=visitedPrompts\(workspace\),out=\[\],ids=new Set\(\),items=candidates\(\{task,school,major:major\|\|backgroundMajor,score,topic,result\}\);/,`export function nextActionsForTurn({task='',school='',major='',score=null,backgroundMajor='',topic='general',result={},workspace={}}={}){\n  const seen=visitedPrompts(workspace),out=[],ids=new Set(),resolvedMajor=major||backgroundMajor,progress=result?.decisionReflection?.progress||deriveDecisionProgress(workspace),items=candidates({task,school,major:resolvedMajor,score,topic,result}),progressItem=progressionAction(progress,{task,school,major:resolvedMajor,score,result});if(progressItem)items.unshift(progressItem);`,'next action progress');
  source=replaceOnce(source,"ids.add(item.id);out.push({id:item.id,label:item.label,prompt:item.prompt,reason:item.reason});if(out.length>=3)break;","ids.add(item.id);out.push({id:item.id,label:item.label,prompt:item.prompt,reason:item.reason,primary:out.length===0,decisionStage:progress.recommendedStage});if(out.length>=3)break;",'primary action metadata');
  return source;
});

patch('functions/_lib/ai/advisor-presentation.js',source=>{
  source=replaceOnce(source,"add=(id,label,prompt,reason='')=>{if(actions.length<3&&!actions.some(x=>x.id===id))actions.push({id,label,prompt,reason});};","add=(id,label,prompt,reason='',meta={})=>{if(actions.length<3&&!actions.some(x=>x.id===id))actions.push({id,label,prompt,reason,...meta});};",'next question metadata');
  source=replaceOnce(source,"if(relationActions.length){relationActions.forEach(item=>add(item.id,item.label,item.prompt,item.reason));return actions;}","if(relationActions.length){relationActions.forEach(item=>add(item.id,item.label,item.prompt,item.reason,{primary:item.primary===true,decisionStage:item.decisionStage||''}));return actions;}",'relation action metadata');
  source=replaceOnce(source,"  const questions=nextQuestions({command,stage,view,result,workspace,focus});if(questions.length)blocks.push({type:'next_questions',title:'接下来值得继续看',items:questions});","  if(result.decisionReflection&&(result.decisionReflection.conflicts?.length||result.decisionReflection.decisionGaps?.length)){const reflection=result.decisionReflection,text=reflection.conflicts?.length?`先处理这个冲突：${reflection.conflicts[0]}。`:`这轮问题已经回答，但整个家庭决策下一步还缺：${(reflection.decisionGaps||[]).slice(0,2).join('、')}。`;blocks.push({type:'decision_implication',title:'这对你意味着什么',text});}\n  const questions=nextQuestions({command,stage,view,result,workspace,focus});if(questions.length){if(!questions.some(item=>item.primary))questions[0].primary=true;blocks.push({type:'next_questions',title:'建议下一步',items:questions});}",'reflection and next action block');
  return source;
});

patch('aiplus/render.v3992_0.js',source=>{
  source=replaceOnce(source,"import {activeViewLabel,regionKeyLabel} from '/shared/ai/ai-workspace-contract.v3992_0.js?v=002_1';","import {activeViewLabel,regionKeyLabel} from '/shared/ai/ai-workspace-contract.v3992_0.js?v=002_1';\nimport {buildDecisionBook} from '/shared/ai/decision-book.v003.js?v=003_0';",'decision book import');
  source=replaceOnce(source,"if(type==='clarification')classes.push('warning');","if(type==='clarification')classes.push('warning');if(type==='decision_implication')classes.push('guidance','decision-implication');",'decision implication class');
  source=replaceOnce(source,"if(type==='next_questions'){const list=node('div','question-list');for(const item of block.items||[])list.append(promptButton(item.label||item.prompt,item.prompt,onPrompt,'question-button',item.reason||''));box.append(list);}","if(type==='next_questions'){const list=node('div','question-list'),items=block.items||[],primary=items.find(item=>item.primary)||items[0],alternatives=items.filter(item=>item!==primary).slice(0,2);if(primary)list.append(promptButton(primary.label||primary.prompt,primary.prompt,onPrompt,'question-button next-primary',primary.reason||''));if(alternatives.length){const more=node('div','next-alternatives');more.append(node('span','next-alternatives-label','其他选择'));for(const item of alternatives)more.append(promptButton(item.label||item.prompt,item.prompt,onPrompt,'question-button next-alternative',item.reason||''));list.append(more);}box.append(list);}",'next action render');
  source=replaceOnce(source,"const priority={change_explanation:1,official_school_info:1,school_profile_supplement:1,school_experience:1,school_research_snapshot:2,fact_summary:2,candidate_routes:2,history_records:2,fit_assessment:2,region_school_directory:2,background_routes:3,comparison:3,selection_review:3,next_questions:5,decision_guidance:6,decision_details:8};","const priority={change_explanation:1,official_school_info:1,school_profile_supplement:1,school_experience:1,school_research_snapshot:2,fact_summary:2,candidate_routes:2,history_records:2,fit_assessment:2,region_school_directory:2,background_routes:3,comparison:3,selection_review:3,decision_implication:4,next_questions:5,decision_guidance:6,decision_details:8};",'block priority');
  source=replaceRegex(source,/function profileItems\(workspace\)\{.*?return items;\}/s,`function profileItems(workspace){const explicit=workspace.decisionProfile?.explicit||{},items=[];if(explicit.primaryGoal&&explicit.primaryGoal!=='undecided')items.push({label:'主要目标',text:GOAL_LABELS[explicit.primaryGoal]||explicit.primaryGoal});if(explicit.priorities?.length)items.push({label:'你明确看重',text:explicit.priorities.map(key=>PRIORITY_LABELS[key]||key).join('、')});if(explicit.familyResourceSensitivity==='resource_sensitive')items.push({label:'家庭现实',text:'普通家庭 / 预算资源敏感'});if(explicit.studyDurationTolerance==='prefer_short')items.push({label:'培养周期',text:'更偏好本科就业'});if(explicit.studyDurationTolerance==='long_ok')items.push({label:'培养周期',text:'可以接受读研/深造'});const careerLabels={central_soe:'央企方向',state_owned:'国企方向',public_service:'考公/体制',teacher:'教师',manufacturing:'制造业',it:'IT/软件',research:'科研',healthcare:'医疗'};if(explicit.careerTargets?.length)items.push({label:'职业方向',text:explicit.careerTargets.map(key=>careerLabels[key]||key).join('、')});const signalLabels={'programming_affinity:avoid':'不喜欢编程','programming_affinity:accept':'能接受编程','factory_environment:avoid':'不想进工厂/生产一线','shift_work:avoid':'不接受倒班','field_site:avoid':'不想长期现场/工地','travel:avoid':'不接受出差','travel:accept':'可以接受出差','hands_on:strong':'动手/实践能力较好','math_strength:strong':'数学基础较好','physics_strength:strong':'物理基础较好','chemistry_strength:weak':'化学相对弱'};const signals=(explicit.studentSignals||[]).map(item=>signalLabels[\`${'${item.dimension}:${item.value}'}\`]).filter(Boolean);if(signals.length)items.push({label:'孩子明确条件',text:signals.join('、')});return items;}`,'profile items');
  const rail=`\nfunction decisionStatusLabel(value=''){return({not_started:'还没开始',exploring:'正在想',ready:'已有基础',confirmed:'已确认',reviewed:'已检查'})[value]||value;}\nfunction dispatchDecision(payload={}){document.dispatchEvent(new CustomEvent('aiplus:decision-save',{detail:payload}));}\nfunction renderDecisionRail(workspace,els){const book=buildDecisionBook(workspace),position=book.position?.summary||'分数位置还没确认';if(els.decisionChildSummary)els.decisionChildSummary.textContent=position;if(els.decisionProfileSummary){els.decisionProfileSummary.replaceChildren();const items=book.profile?.items||[];if(!items.length)els.decisionProfileSummary.append(node('span','decision-empty','家庭取舍还没说清，可以在对话里直接告诉我。'));for(const text of items.slice(0,6))els.decisionProfileSummary.append(node('span','decision-profile-chip',text));}if(els.decisionProgressList){els.decisionProgressList.replaceChildren();for(const item of book.progress?.stages||[]){const row=node('div',\`decision-progress-item is-\${item.state}\`);row.append(node('span','decision-progress-mark',['ready','confirmed','reviewed'].includes(item.state)?'✓':item.state==='exploring'?'●':'○'),node('span','decision-progress-label',item.label),node('span','decision-progress-state',decisionStatusLabel(item.state)));els.decisionProgressList.append(row);}}if(els.mobileDecisionStrip){const next=(book.progress?.stages||[]).find(item=>item.key===book.progress?.recommendedStage);els.mobileDecisionStrip.replaceChildren(node('strong','',position),node('span','',next?\`下一步 · \${next.label}\`:'当前决策已完成一次检查'));}if(els.decisionBookContent){els.decisionBookContent.replaceChildren();const section=(title)=>{const wrap=node('section','decision-book-section');wrap.append(node('h3','',title));els.decisionBookContent.append(wrap);return wrap;};const profile=section('家庭真正看重什么');if(book.profile.items.length)profile.append(node('p','',book.profile.items.join(' · ')));else profile.append(node('p','decision-empty','还没有足够明确的家庭取舍。'));const directions=section('当前专业方向');const kept=book.directions.keep||[];if(kept.length)directions.append(node('p','decision-kept',\`已保留：\${kept.map(item=>item.label).join('、')}\`));const keptLabels=new Set(kept.map(item=>item.label));for(const major of (book.directions.exploring||[]).filter(value=>!keptLabels.has(value)).slice(0,3)){const row=node('div','decision-book-action');row.append(node('span','',major));const keep=node('button','decision-mini-action','先保留');keep.type='button';keep.addEventListener('click',()=>dispatchDecision({kind:'major_direction',status:'keep',subject:{label:major},reason:'用户在家庭决策书中明确选择先保留'}));row.append(keep);directions.append(row);}const pairs=section('重点学校×专业');if(book.schoolMajors.items.length){for(const item of book.schoolMajors.items.slice(0,6)){const row=node('div','decision-book-action');row.append(node('span','',item.label));const keep=node('button','decision-mini-action','加入重点');keep.type='button';keep.addEventListener('click',()=>dispatchDecision({kind:'school_major',status:'keep',subject:{school:item.school,major:item.major,label:item.label},reason:'用户在家庭决策书中明确加入重点'}));row.append(keep);pairs.append(row);}}else pairs.append(node('p','decision-empty','还没有落到具体学校×专业。'));const unresolved=section('还没解决');if(book.unresolved.items.length)for(const text of book.unresolved.items.slice(0,8))unresolved.append(node('p','decision-gap',text));else unresolved.append(node('p','decision-empty','当前没有新增待核验事项。'));const evidence=section('依据完整度');evidence.append(node('p','',\`已核实 \${book.evidence.verified} · 可参考 \${book.evidence.reference} · 还要核实 \${book.evidence.pending}\`));}}\n`;
  source=replaceOnce(source,'export function renderProcessingTurn(container,text,contextLabel=\'\'){',rail+"\nexport function renderProcessingTurn(container,text,contextLabel=''){",'decision rail helpers');
  source=replaceOnce(source,"export function renderAdvisor(workspace,els,onPrompt){els.conversation?.setAttribute?.('aria-busy','false');","export function renderAdvisor(workspace,els,onPrompt){els.conversation?.setAttribute?.('aria-busy','false');renderDecisionRail(workspace,els);",'render decision rail');
  return source;
});

patch('aiplus/app.v3990_1.js',source=>{
  source=replaceOnce(source,"health:$('#healthBar'),modelConfig:$('#modelConfig'),modelProbeResult:$('#modelProbeResult'),probeModel:$('#probeModel'),exportWorkspace:$('#exportWorkspace')","decisionChildSummary:$('#decisionChildSummary'),decisionProfileSummary:$('#decisionProfileSummary'),decisionProgressList:$('#decisionProgressList'),decisionBookContent:$('#decisionBookContent'),mobileDecisionStrip:$('#mobileDecisionStrip'),newFamilyProfile:$('#newFamilyProfile'),exportWorkspace:$('#exportWorkspace')",'app element map');
  source=replaceOnce(source,"async function startNewWorkspace(){if((workspace.turnHistory||[]).length&&!confirm('新开讨论会保留当前内容到 History，并开始一个新的讨论。继续吗？'))return;stopActiveTurn();workspace=createAiWorkspace();await saveWorkspace();render();closeHistoryDrawer();els.input?.focus();}","async function startNewWorkspace(){if((workspace.turnHistory||[]).length&&!confirm('开始新话题会保留孩子情况、家庭条件和已确认决定，只清掉当前学校/专业焦点。继续吗？'))return;stopActiveTurn();workspace=applyAiWorkspaceEvent(workspace,{type:'topic_started',payload:{}});await saveWorkspace();render();closeHistoryDrawer();els.input?.focus();}\nasync function startNewFamilyProfile(){if((workspace.turnHistory||[]).length&&!confirm('新建另一份家庭档案会保留当前档案到历史，并开始一份独立规划。继续吗？'))return;stopActiveTurn();workspace=createAiWorkspace();await saveWorkspace();render();closeHistoryDrawer();els.input?.focus();}",'topic versus family');
  source=replaceRegex(source,/async function checkHealth\(\).*?function setRunning/s,'function setRunning','remove health/probe functions');
  source=replaceOnce(source,"els.stop.addEventListener('click',()=>stopActiveTurn('已停止这轮；之前的讨论没有被覆盖。'));els.probeModel.addEventListener('click',probeModel);","els.stop.addEventListener('click',()=>stopActiveTurn('已停止这轮；之前的讨论没有被覆盖。'));",'remove probe listener');
  source=replaceOnce(source,"els.newWorkspace.addEventListener('click',startNewWorkspace);els.newWorkspaceSide?.addEventListener('click',startNewWorkspace);","els.newWorkspace.addEventListener('click',startNewWorkspace);els.newWorkspaceSide?.addEventListener('click',startNewWorkspace);els.newFamilyProfile?.addEventListener('click',startNewFamilyProfile);els.mobileDecisionStrip?.addEventListener('click',()=>document.body.classList.add('history-open'));document.addEventListener('aiplus:decision-save',async event=>{const payload=event.detail||{};workspace=applyAiWorkspaceEvent(workspace,{type:'decision_saved',payload});await saveWorkspace();render();});",'new family and decision listener');
  source=replaceOnce(source,"await loadWorkspace();render();checkHealth();","await loadWorkspace();render();",'remove health startup');
  source=source.replaceAll("当前讨论不能直接删除；请先新开一次讨论。","当前家庭档案不能直接删除；请先新建另一份家庭档案。");
  return source;
});

patch('aiplus/index.html',source=>{
  source=source.replace('<button id="historyToggle" class="history-toggle" type="button">讨论记录</button>','<button id="historyToggle" class="history-toggle" type="button">我的决策</button>');
  source=source.replace('<button id="newWorkspace" type="button">新开讨论</button>','<button id="newWorkspace" type="button">新话题</button>');
  source=replaceRegex(source,/<aside id="historyPanel" class="history-panel" aria-label="历史讨论">.*?<\/aside>/s,`<aside id="historyPanel" class="history-panel decision-panel" aria-label="我的决策">\n      <div class="history-head decision-panel-head">\n        <div><strong>我的决策</strong><span>孩子情况、进度和当前结论</span></div>\n        <button id="historyClose" type="button" aria-label="关闭我的决策">×</button>\n      </div>\n      <button id="newWorkspaceSide" class="history-new" type="button">＋ 开始新话题</button>\n      <section class="decision-rail-section" aria-labelledby="childDecisionTitle">\n        <h2 id="childDecisionTitle">孩子现在</h2>\n        <strong id="decisionChildSummary" class="decision-child-summary">分数位置还没确认</strong>\n        <div id="decisionProfileSummary" class="decision-profile-summary"></div>\n      </section>\n      <section class="decision-rail-section" aria-labelledby="decisionProgressTitle">\n        <h2 id="decisionProgressTitle">决策进度</h2>\n        <div id="decisionProgressList" class="decision-progress-list"></div>\n      </section>\n      <details class="decision-book-card" id="decisionBookDetails">\n        <summary>家庭决策书 <span>随讨论自动整理</span></summary>\n        <div id="decisionBookContent" class="decision-book-content"></div>\n      </details>\n      <details class="decision-support-card">\n        <summary>已确认条件与依据</summary>\n        <div class="decision-support-body">\n          <section><h2>已确认条件</h2><div id="constraintList" class="support-list"></div></section>\n          <section><h2>还要核实</h2><div id="pendingList" class="support-list"></div></section>\n          <section><h2>本轮依据</h2><div id="evidenceList" class="support-list"></div></section>\n          <div class="selection-import"><button id="importSelection" type="button">导入当前家庭方案</button><span id="importStatus" class="muted" aria-live="polite"></span></div>\n          <button id="exportWorkspace" class="quiet" type="button">导出这份家庭决策</button>\n        </div>\n      </details>\n      <details class="decision-history-card">\n        <summary>其他家庭档案</summary>\n        <button id="newFamilyProfile" class="history-new secondary-family" type="button">＋ 新建另一份家庭档案</button>\n        <label class="history-search-label" for="historySearch">搜索历史档案</label>\n        <input id="historySearch" class="history-search" type="search" placeholder="学校、专业或问题" autocomplete="off" />\n        <div id="historyList" class="history-list" aria-live="polite"></div>\n        <p class="history-local-note">家庭档案只保存在本设备，不会改动专业初选或家庭方案。</p>\n      </details>\n    </aside>`,'decision aside');
  source=source.replace('<h1 id="pageTitle">把学校、专业和分数问题问清楚</h1>','<h1 id="pageTitle">把孩子的选择一步一步定下来</h1>');
  source=source.replace('<p>先给你能用的结论，再展开依据。只有你明确要求筛选，候选范围才会改变。</p>','<p>分数、专业、学校、就业、读研和家庭条件都可以直接说。只记住你明确表达过的取舍，没有确认的不会替你做主。</p>');
  source=replaceOnce(source,'      <section id="conversationStream" class="conversation" aria-live="polite" aria-busy="false"></section>','      <button id="mobileDecisionStrip" class="mobile-decision-strip" type="button"><strong>我的决策</strong><span>查看当前进度</span></button>\n      <section id="conversationStream" class="conversation" aria-live="polite" aria-busy="false"></section>','mobile decision strip');
  source=source.replace('<h2>先说你真正想解决的问题</h2>','<h2>先说孩子现在最真实的情况</h2>');
  source=source.replace('<p>可以直接说“普通家庭、不太想考研，电气和自动化怎么选”“省内机械电子所有学校分数从高到低”“沈工大电气和大连交通自动化怎么取舍”，不用学习标准问法。</p>','<p>例如“578分，普通家庭，本科就业优先，不太想读研，现在不知道电气、自动化还是机械更适合”。不知道从哪开始也没关系，我会告诉你下一步最值得解决什么。</p>');
  source=source.replace('<p class="composer-help">招生分数走站内确定性数据；复杂取舍先拆成有界证据计划。学校官方资料优先核对阳光高考，需要培养、就业或升学材料时只读取可验证官方页面。模型负责理解和归纳，不用模型补猜事实。</p>','<p class="composer-help">招生分数来自站内确定性数据；学校、专业和政策事实有可靠依据才下结论，拿不到就明确告诉你。</p>');
  source=replaceRegex(source,/\n      <details class="support-panel" id="decisionContextDetails">.*?<\/details>/s,'','remove engineering support panel');
  return source;
});

patch('aiplus/workspace.v3990_1.css',source=>source+`\n\n/* AIPLuS family decision workbench v0.03 */\n.decision-panel{display:grid;gap:12px;align-content:start}.decision-panel-head{margin-bottom:0}.decision-rail-section,.decision-book-card,.decision-support-card,.decision-history-card{border:1px solid #dde3de;background:#fff;border-radius:13px;padding:11px}.decision-rail-section h2{margin:0 0 7px;font-size:12px;color:#6b7770}.decision-child-summary{display:block;font-size:16px;color:#243a30}.decision-profile-summary{display:flex;flex-wrap:wrap;gap:5px;margin-top:8px}.decision-profile-chip{font-size:11px;padding:4px 7px;border-radius:999px;background:#eef4ef;color:#385448}.decision-empty{font-size:11px;color:#7b8580}.decision-progress-list{display:grid;gap:5px}.decision-progress-item{display:grid;grid-template-columns:18px minmax(0,1fr) auto;gap:5px;align-items:center;min-height:27px;font-size:12px}.decision-progress-mark{font-weight:800;color:#8a948f}.decision-progress-item.is-exploring .decision-progress-mark{color:#8a6b24}.decision-progress-item.is-ready .decision-progress-mark,.decision-progress-item.is-confirmed .decision-progress-mark,.decision-progress-item.is-reviewed .decision-progress-mark{color:#2f6a4c}.decision-progress-state{font-size:10px;color:#85908a}.decision-book-card>summary,.decision-support-card>summary,.decision-history-card>summary{cursor:pointer;font-weight:730;color:#334a3f;list-style:none}.decision-book-card>summary::-webkit-details-marker,.decision-support-card>summary::-webkit-details-marker,.decision-history-card>summary::-webkit-details-marker{display:none}.decision-book-card>summary span{display:block;font-size:10px;font-weight:400;color:#7d8782}.decision-book-content,.decision-support-body{display:grid;gap:10px;margin-top:10px}.decision-book-section{border-top:1px solid #edf0ed;padding-top:8px}.decision-book-section:first-child{border-top:0;padding-top:0}.decision-book-section h3{margin:0 0 5px;font-size:12px}.decision-book-section p{margin:3px 0;font-size:11px;color:#5e6b65}.decision-book-action{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:6px;align-items:center;padding:5px 0;font-size:11px}.decision-mini-action{border:1px solid #cfd9d2;background:#f8fbf8;border-radius:8px;padding:4px 7px;color:#345844;font-size:10px}.decision-kept{color:#2f6348!important}.decision-gap{padding:6px 7px;background:#fff8eb;border-radius:8px}.secondary-family{margin-top:10px;background:#fff}.mobile-decision-strip{display:none;width:100%;border:1px solid #ccd8d0;background:#f5faf6;border-radius:12px;padding:9px 11px;margin:0 0 12px;text-align:left}.mobile-decision-strip strong,.mobile-decision-strip span{display:block}.mobile-decision-strip span{font-size:11px;color:#6c7972;margin-top:2px}.decision-implication{border-left:4px solid #91ad9d}.next-primary{border-color:#86a995;background:#eef6f0;padding:12px}.next-primary strong{font-size:14px;color:#214f39}.next-alternatives{display:grid;gap:6px;margin-top:6px;padding-top:7px;border-top:1px solid #edf0ed}.next-alternatives-label{font-size:11px;color:#7b8580}.next-alternative{padding:8px 10px;background:#fff}.support-panel{display:none!important}\n@media(min-width:960px){.history-toggle{display:none!important}.decision-panel .history-head button{display:none}.advisor-layout{grid-template-columns:260px minmax(0,960px)}}\n@media(max-width:959px){.advisor-layout{display:block;width:min(100% - 20px,960px);padding:14px 0 86px}.advisor-layout .advisor-shell{width:auto}.history-toggle{display:inline-flex!important}.history-panel{display:grid;position:fixed;z-index:40;left:0;top:0;bottom:0;width:min(92vw,420px);max-height:none;border-radius:0 18px 18px 0;padding:14px;overflow:auto;transform:translateX(-104%);transition:transform .18s ease;background:#fafbf8;box-shadow:0 20px 60px rgba(25,35,30,.2)}body.history-open .history-panel{transform:translateX(0)}.history-head button{display:block!important}.history-backdrop{display:none;position:fixed;inset:0;z-index:35;border:0;background:rgba(31,39,35,.34)}body.history-open .history-backdrop{display:block}.mobile-decision-strip{display:block}.decision-panel{gap:10px}.top-actions a{display:none}.top-actions .history-toggle,.top-actions #newWorkspace{display:inline-flex}}\n@media(max-width:719px){.advisor-shell{width:100%}.compact-hero{padding-top:4px}.compact-hero h1{font-size:29px}.mobile-decision-strip{position:relative}.decision-profile-chip{font-size:10px}.question-button.next-primary{min-height:56px}.decision-book-action{grid-template-columns:minmax(0,1fr) auto}body{overflow-x:hidden}}\n`);

console.log('FDW builder completed');
