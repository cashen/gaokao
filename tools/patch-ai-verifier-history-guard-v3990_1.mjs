import fs from 'node:fs';
const rel='tools/verify-ai-workspace-v3990_1.mjs';
let text=fs.readFileSync(rel,'utf8');
const before='assert.ok(app.includes("prompt:\'回到上一批\'"));';
const after='assert.ok(app.includes("index===0?\'回到上一批\'"));assert.ok(app.includes("executeTurn(index===0?\'回到上一批\'"));';
if(!text.includes(before))throw new Error('history source guard changed; refusing blind patch');
text=text.replace(before,after);
fs.writeFileSync(rel,text);
fs.rmSync(new URL(import.meta.url));
console.log('patched history guard to verify the actual executable path');
