# PLAN v3.9.46：自选池最终排序一致性收口

## 目标
在 v3.9.45 拖拽排序工作台基础上，继续保持 /ln-rank 为主工具、/fenxi/data 为专业数据来源，不改变 functions/api 的既有路由契约，重点收口自选池排序、诊断与飞书发送三者读取同一份最终顺序。

## 开发范围
1. 首页继续保持右侧小胶囊“自选池 N”作为唯一轻入口，不恢复抽屉，不增加顶部排序链接。
2. 自选池整理页继续保留拖拽排序、上移/下移、移动菜单、按冲稳保整理、删除/清空。
3. 所有排序动作统一通过 store normalize，重新生成 userOrder 并保存到 localStorage。
4. 检查当前排序时记录排序签名；排序或分数变化后，旧诊断自动失效，避免旧诊断被发送到飞书。
5. 发送当前排序到飞书、发送诊断报告到飞书，均读取整理页当前最终顺序。
6. 手机端保留上移/下移与移动菜单，并优化移动菜单触控展示。

## 不做事项
- 不改 GitHub。
- 不把 /ln-rank 迁回 /fenxi。
- 不新增历史类/文科入口。
- 不破坏 /api/major-bands、/api/path-analysis、/api/feishu-create-selection-pool-report。
- 不做增量补丁包，本次输出完整包。
