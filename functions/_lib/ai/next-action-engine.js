import {deriveDecisionProgress} from '../../../shared/ai/decision-progress.v003.js';
import {deriveDecisionFocus,decisionFocusGapAction} from '../../../shared/ai/decision-focus.v006_1.js';
export const AI_NEXT_ACTION_ENGINE_VERSION='ai-next-action-engine-v0.03';

export const NEXT_ACTION_LABELS=Object.freeze({schoolEnvironment:'看校园环境与同学体验',schoolMajorBackground:'看哪些专业更有积累',schoolMajorHistory:'看全校专业分数',majorHistory:'看这个专业分数',majorRegionBackground:'看辽宁其他学校',schoolEvidence:'看证据依据',schoolProfile:'回到学校整体介绍',schoolFit:'按我的分数判断',officialLiving:'核验官方食宿硬信息'});
const ATOMIC_RESEARCH_TASKS=new Set(['region_school_directory','school_research','school_official_qa','school_experience','school_background','school_history','school_major_history','major_background','major_region_history','background_discovery','background_fit_discovery']);
const DECISION_FOCUS_TASKS=new Set(['decision_research','school_comparison','major_comparison','candidate_refinement','plan_review','general_advice']);
function clean(value){return String(value||'').trim();}
function normalizedPrompt(value){return clean(value).replace(/[\s，。！？、,.!?]/g,'');}
function visitedPrompts(workspace={}){return new Set((workspace.turnHistory||[]).slice(-24).map(turn=>normalizedPrompt(turn?.userText)).filter(Boolean));}
function action(id,label,prompt,reason,priority=50){return{id,label,prompt,reason,priority};}
function resultSchool(result={}){const direct=[...(result?.majorHistory?.records||[]),...(result?.history?.records||[]),...(result?.fit?.records||[]),...(result?.candidates?.records||[])];for(const item of direct){const school=clean(item?.school||item?.schoolName);if(school)return school;}for(const item of result?.background?.items||[]){const own=clean(item?.school||item?.schoolName||item?.record?.school);if(own)return own;for(const candidate of item?.schools||[]){const school=clean(typeof candidate==='string'?candidate:candidate?.school||candidate?.schoolName||candidate?.officialName);if(school)return school;}}return'';}
function progressionAction(progress={},context={}){const stage=progress.recommendedStage,majors=progress.majors||[],pairs=progress.pairs||[],score=Number(context.score),s=Number.isFinite(score)&&score>=150&&score<=750?String(Math.round(score)):'',major=context.major||majors[0]||'';if(stage==='score_position')return s?action('progress-score','先确认分数位置',`按我${s}分，先确认辽宁物理类位次，再继续做专业和学校选择`,'先把孩子的位置坐标稳定下来。',130):null;if(stage==='major_direction'){const names=majors.slice(0,3);return action('progress-major','先把专业方向收敛',names.length>=2?`把${names.join('、')}按本科就业、培养路径和我家的条件横着比较`:'普通家庭，本科就业优先，先帮我收敛几个值得继续研究的专业方向','专业方向没收敛时，过早堆学校会增加判断负担。',125);}if(stage==='school_major'){if(major&&s)return action('progress-school-major','落到现实学校',`按我${s}分，辽宁${major}有哪些学校更现实`,'把专业方向落到真实招生学校与位置。',125);if(major)return action('progress-school-major','落到现实学校',`辽宁${major}在各学校2026都多少分`,'先建立学校×专业的现实坐标。',125);if(pairs.length)return action('progress-school-major','继续研究具体组合',`继续比较${pairs.slice(0,2).map(item=>item.label).join('和')}`,'从方向进入具体学校×专业。',125);return null;}if(stage==='family_tradeoff')return action('progress-tradeoff','把家庭取舍说清楚','把我目前明确的就业、读研、地域、预算和工作环境条件整理一下，告诉我还缺哪个关键取舍','学校和专业最终要服从家庭真实边界。',122);if(stage==='plan')return action('progress-plan','开始形成家庭方案','把目前重点考虑的学校和专业整理成一版家庭方案，我再继续取舍','研究结果需要进入可检查的方案，而不是停在聊天记录里。',120);if(stage==='audit')return action('progress-audit','做最后检查','帮我最后检查当前家庭方案，还缺什么、哪里重复、哪里和家庭条件冲突','最终检查只做结构与证据审计，不制造录取概率。',120);return null;}
function candidates({task,school,major,score,topic,result}={}){const s=clean(school),m=clean(major),scoreText=Number.isFinite(Number(score))?String(Number(score)):'',nextSchool=resultSchool(result);if(task==='decision_research'){const frame=result?.decisionResearch?.frame||{},signals=frame.preferenceSignals||[],schools=frame.schools||[],majors=frame.majors||[],career=(frame.careerTargets||[]).length>0,out=[];if(signals.some(item=>item.dimension==='study_duration'&&item.value==='prefer_short'))out.push(action('decision-study-long','如果愿意读研再看','那如果我愿意读研呢','只改变培养周期偏好，不改当前候选筛选。',105));else out.push(action('decision-study-short','按本科就业再看','更希望本科直接就业，不把读研当必选项','把培养周期这个取舍说清楚。',105));if(career)out.push(action('decision-career-proof','只核验职业路径证据','把刚才涉及就业、央企国企或职业路径的部分，只按具体学校官方证据再核验','职业目标是比较维度，不让模型把目标写成事实。',100));if(!schools.length&&scoreText&&majors.length)out.push(action('decision-to-schools','收敛到具体学校',`按我${scoreText}分，把${majors.slice(0,3).join('、')}各自能落到的学校先收敛出来，再继续比较`,'就业、培养和升学最终要落到具体学校。',95));else if(schools.length>=2)out.push(action('decision-only-facts','只看证据差异',`把${schools.slice(0,2).join('和')}刚才有证据和没证据的部分分开列给我`,'把事实差异和价值取舍再分开。',90));return out;}
  if(task==='region_school_directory'){const directory=result?.regionSchools||{},region=clean(directory?.region?.label)||'当前地区',level=directory?.level||'all',key=clean(directory?.region?.key),out=[];if(level==='all')out.push(action('region-undergraduate','只看本科',`${region}有哪些本科院校`,'把学校层次单独收窄，不改变候选筛选。',100));else out.push(action('region-all-schools','看全部高校',`${region}有哪些大学`,'回到本科和专科的完整地域目录。',100));out.push(action('region-major-history','按专业看学校和分数',`${region}电气工程及其自动化有哪些学校，2026都多少分`,'从学校目录进入确定性专业投档事实。',94));if(scoreText&&(key==='ln'||key==='shenyang'||key==='dalian'||key.startsWith('province:')))out.unshift(action('region-score-fit','按我的分数看可达性',`按我${scoreText}分，只看${region}有哪些学校更现实`,'这一步才把地区写入候选筛选。',105));return out;}
  if(task==='school_research')return[action('research-background',NEXT_ACTION_LABELS.schoolMajorBackground,`${s}哪些专业更有底子`,'查看学校专业积累和证据范围。',95),action('research-history',NEXT_ACTION_LABELS.schoolMajorHistory,`${s}所有专业的最低录取分`,'把学校画像与实际投档记录交叉看。',90),action('research-environment',NEXT_ACTION_LABELS.schoolEnvironment,`${s}学校环境和人文关怀怎么样`,'补充同学体验，不冒充官方结论。',85)];
  if(task==='school_background')return[m?action('background-major-history',NEXT_ACTION_LABELS.majorHistory,`${s}${m}多少分`,'从专业积累回到实际投档分数。',98):action('background-school-history',NEXT_ACTION_LABELS.schoolMajorHistory,`${s}所有专业的最低录取分`,'回到全校专业分数。',95),action('background-environment',NEXT_ACTION_LABELS.schoolEnvironment,`${s}学校环境和人文关怀怎么样`,'再看真实生活体验。',80),action('background-evidence',NEXT_ACTION_LABELS.schoolEvidence,`${s}${m||''}的专业背景依据是什么`,'区分学校级、学院级和专业级证据。',75)];
  if(task==='school_major_history'&&result?.history?.directionRedirect?.admissionMajors?.length)return result.history.directionRedirect.admissionMajors.slice(0,3).map((major,index)=>action(`direction-major-history-${index}`,`查${major}分数`,`${s}${major}多少分`,`“${result.history.directionRedirect.direction}”是背景方向；这里改查实际招生专业。`,110-index));
  if(task==='school_history'||task==='school_major_history')return[m?action('history-major-background',NEXT_ACTION_LABELS.schoolMajorBackground,`${s}${m}有专业背景吗`,'从分数反向检查专业积累。',96):action('history-school-background',NEXT_ACTION_LABELS.schoolMajorBackground,`${s}哪些专业更有底子`,'从全校分数进入专业积累。',96),action('history-school-profile',NEXT_ACTION_LABELS.schoolProfile,`介绍下${s}`,'补齐学校整体定位。',85),action('history-school-environment',NEXT_ACTION_LABELS.schoolEnvironment,`${s}学校环境和人文关怀怎么样`,'补充学校生活体验。',75)];
  if(task==='school_experience'){const living=topic==='living',out=[];if(living)out.push(action('experience-official-living',NEXT_ACTION_LABELS.officialLiving,`${s}官方食宿条件`,'把同学体验和官方页面公开的硬信息分开核验。',100));out.push(action('experience-background',NEXT_ACTION_LABELS.schoolMajorBackground,`${s}哪些专业更有底子`,'从生活体验转到专业选择。',90));out.push(action('experience-history',NEXT_ACTION_LABELS.schoolMajorHistory,`${s}所有专业的最低录取分`,'把体验与投档事实交叉查看。',85));out.push(action('experience-profile',NEXT_ACTION_LABELS.schoolProfile,`介绍下${s}`,'回到学校整体定位。',70));return out;}
  if(task==='major_background')return[action('major-background-history',NEXT_ACTION_LABELS.majorHistory,`辽宁${m}在各学校多少分`,'从背景证据回到全省专业分数。',98),action('major-background-schools',NEXT_ACTION_LABELS.majorRegionBackground,`${m}在辽宁哪些学校有背景证据`,'继续沿同一专业横向找学校。',88),nextSchool?action('major-background-school',`继续看${nextSchool}`,`介绍下${nextSchool}`,'从专业主线进入一所具体学校。',70):null].filter(Boolean);
  if(task==='background_discovery'||task==='background_fit_discovery')return[action('discovery-major-history',NEXT_ACTION_LABELS.majorHistory,`辽宁${m||'电气'}在各学校多少分`,'回到确定性专业历史。',96),action('discovery-major-schools',NEXT_ACTION_LABELS.majorRegionBackground,`${m||'电气'}在辽宁哪些学校有背景证据`,'沿专业主线横向找学校。',86),nextSchool?action('discovery-school-research',`继续看${nextSchool}`,`介绍下${nextSchool}`,'进入一所具体学校节点。',60):null].filter(Boolean);
  if(task==='major_region_history')return[action('major-region-background',NEXT_ACTION_LABELS.majorRegionBackground,`${m}在辽宁哪些学校有背景证据`,'从分数横向转到专业积累。',96),scoreText?action('major-region-fit',NEXT_ACTION_LABELS.schoolFit,`按我${scoreText}分，辽宁${m}有哪些学校更现实`,'只有这一步重新激活家庭分数。',92):action('major-region-set-score','带上分数判断',`我580分，辽宁${m}哪些学校更现实`,'做可达判断时再使用个人分数。',92),nextSchool?action('major-region-school',`继续看${nextSchool}`,`介绍下${nextSchool}`,'从专业主线进入一所具体学校。',65):null].filter(Boolean);
  if(task==='school_comparison'||task==='major_comparison')return[action('comparison-priority','明确比较重点','我更看重本科就业和专业匹配，城市其次','比较维度会随家庭关注点变化。',95),!scoreText?action('comparison-score','带上分数看可达性','我580分，再比较一次','加入分数后才能比较可达专业空间。',90):null].filter(Boolean);return[];}
function allowGlobalProgress(task='',result={}){if(result?.partial&&task!=='decision_research')return false;return !ATOMIC_RESEARCH_TASKS.has(task);}
export function nextActionsForTurn({task='',school='',major='',score=null,backgroundMajor='',topic='general',result={},workspace={}}={}){const seen=visitedPrompts(workspace),out=[],ids=new Set(),resolvedMajor=major||backgroundMajor,progress=result?.decisionReflection?.progress||deriveDecisionProgress(workspace),items=candidates({task,school,major:resolvedMajor,score,topic,result}),focus=deriveDecisionFocus(workspace,{currentResult:result});if(allowGlobalProgress(task,result)){const progressItem=progressionAction(progress,{task,school,major:resolvedMajor,score,result});if(progressItem)items.unshift(progressItem);if(DECISION_FOCUS_TASKS.has(task)){const gapItem=decisionFocusGapAction(focus);if(gapItem)items.unshift(gapItem);}}if(result?.partial&&task!=='decision_research')items.unshift(action('retry-failed','重试没有完成的部分','只重试刚才失败的查询','保留已成功结果，只补失败项。',200));for(const item of items.sort((a,b)=>b.priority-a.priority)){if(!item?.prompt||ids.has(item.id)||seen.has(normalizedPrompt(item.prompt)))continue;ids.add(item.id);out.push({id:item.id,label:item.label,prompt:item.prompt,reason:item.reason,primary:out.length===0,decisionStage:item.decisionStage||progress.recommendedStage});if(out.length>=3)break;}return out;}
