import {buildAiResultDelta,AI_WORKSPACE_CONTRACT_VERSION,activeViewLabel,applyAiViewPatch} from '../../../shared/ai/ai-workspace-contract.v3991_0.js';
import {interpretAiCommand,deterministicCommand} from './command-interpreter.js';
import {evidenceForIntent} from './evidence-registry.js';
import {resolveRegionExecution,runMajorBandSearch,runRankLookup,runSchoolComparison,runMajorComparison,AI_TOOL_REGISTRY_VERSION} from './tool-registry.js';
import {runSelectionReview} from './selection-review.js';
import {scopeChanges,changeSummary,decisionStageFor,pendingChecksFor,comparisonText,buildBlocks} from './advisor-presentation.js';

export const AI_TURN_ORCHESTRATOR_VERSION='ai-turn-orchestrator-v3991_0';
function clean(value,max=300){return String(value==null?'':value).trim().slice(0,max);}
function unique(values,max=16){return [...new Set((Array.isArray(values)?values:[]).map(value=>clean(value,120)).filter(Boolean))].slice(0,max);}
function validScore(value){const score=Math.round(Number(value));return Number.isFinite(score)&&score>=150&&score<=750?score:null;}
function clone(value){return value==null?value:JSON.parse(JSON.stringify(value));}
function baseView(workspace={}){const source=workspace?.activeView||{};return{target:source.target||'candidates',score:validScore(source.score??workspace?.examContext?.score),majorKeywords:unique(source.majorKeywords||[],8),regionKeys:unique(source.regionKeys||['all'],8).length?unique(source.regionKeys||['all'],8):['all'],schoolNames:unique(source.schoolNames||[],4),bottomLineMode:['all','public_first','public_regular_only','public_include_sino'].includes(source.bottomLineMode)?source.bottomLineMode:'all',combination:source.combination==='union'?'union':'replace',sourceText:''};}
function fallbackPatch(command={}){return{score:validScore(command.score)?{op:'set',value:validScore(command.score)}:{op:'inherit'},region:command.regionKeys?.length?{op:command.regionKeys.includes('all')?'clear':'set',keys:command.regionKeys}:{op:'inherit',keys:[]},major:command.clearMajor?{op:'clear',values:[]}:command.majorKeywords?.length?{op:command.combination==='union'?'add':'set',values:command.majorKeywords}:{op:'inherit',values:[]},school:command.clearSchool?{op:'clear',values:[]}:command.schoolNames?.length?{op:'set',values:command.schoolNames}:{op:'inherit',values:[]},bottomLine:command.bottomLineMode?{op:'set',value:command.bottomLineMode}:{op:'inherit',value:''}};}
function resolveActiveView(command={},workspace={}){const base=baseView(workspace),patch=command.changeSet&&typeof command.changeSet==='object'?command.changeSet:fallbackPatch(command),mutatesView=['search','refine'].includes(command.operation);if(!mutatesView)return{view:clone(base),commitView:false,patch,previousView:base};const next=applyAiViewPatch(base,patch,workspace?.examContext||{});next.sourceText=clean(command.rawText,320);return{view:next,commitView:true,patch,previousView:base};}
function viewMatchesCommand(view={},command={}){if(command.majorKeywords?.length&&!command.majorKeywords.every(value=>(view.majorKeywords||[]).includes(value)))return false;if(command.regionKeys?.length&&!command.regionKeys.every(value=>(view.regionKeys||[]).includes(value)))return false;if(validScore(command.score)&&validScore(view.score)!==validScore(command.score))return false;return true;}
function restoreView(command={},workspace={}){const history=Array.isArray(workspace?.viewHistory)?workspace.viewHistory:[];if(!history.length)return null;const text=String(command.rawText||'');if(/(上一批|上一个结果|刚才那批|刚才的结果|前面那批)/.test(text)&&!(command.majorKeywords?.length||command.regionKeys?.length))return clone(history[0]);const matched=history.find(view=>viewMatchesCommand(view,command));return clone(matched||history[0]);}
function selectionReviewRequested(input=''){return /(方案|选择池|自选|已选|选了些|选了一些|检查.{0,6}(方案|专业)|看看.{0,6}(方案|已选)|还缺什么)/.test(String(input||''));}
function evidenceIntent(command,result){return{topic:result?.comparison?'candidate_search':command.operation==='verify'?'verification':['search','refine','restore'].includes(command.operation)?'candidate_search':'general_question',question:command.question||command.rawText||'',majorKeywords:command.majorKeywords||[]};}
function resultIdentity({view,command,selectionReview}){return[view.score||'',view.majorKeywords.join('/'),view.regionKeys.join(','),view.schoolNames.join('/'),view.bottomLineMode,command.operation,command.target,selectionReview?.snapshotVersion||''].join('|');}
function validateConfirmedCommand(value,input,workspace){if(!value||typeof value!=='object')return null;const fallback=deterministicCommand(input,workspace);return{...fallback,...value,changeSet:fallback.changeSet,score:fallback.score,regionKeys:fallback.regionKeys,majorKeywords:fallback.majorKeywords,schoolNames:fallback.schoolNames,bottomLineMode:fallback.bottomLineMode,rawText:clean(input,1200),question:clean(input,1200),requiresConfirmation:false,confidence:Math.max(.8,Number(value.confidence||.8)),source:`${clean(value.source,30)||'confirmed'}-confirmed`};}

export async function orchestrateAiTurn(context,payload={}){const input=clean(payload.input,1200),workspace=payload.workspace&&typeof payload.workspace==='object'?payload.workspace:{};if(!input&&!payload.confirmedCommand)return{ok:false,status:400,message:'直接说你现在想解决的问题即可。'};if(workspace?.contractVersion&&![AI_WORKSPACE_CONTRACT_VERSION,'ai-workspace-contract-v3990_1'].includes(workspace.contractVersion))return{ok:false,status:409,message:'AI工作区版本无法安全迁移，请刷新页面后继续；专业初选和家庭方案不会受影响。'};
  let interpreted;const confirmed=validateConfirmedCommand(payload.confirmedCommand,input||payload.confirmedCommand?.rawText||'',workspace);if(confirmed)interpreted={command:confirmed,provider:{ok:false,provider:'',model:'',confirmed:true}};else interpreted=await interpretAiCommand(input,workspace,context.env||{});const command=interpreted.command;if(command.requiresConfirmation&&!confirmed)return{ok:true,pendingConfirmation:true,command,provider:{provider:interpreted.provider?.provider||'',model:interpreted.provider?.model||'',source:command.source,failures:interpreted.provider?.failures||[]},blocks:[{type:'clarification',title:'这句话我不想替你猜',text:command.reason||'请明确是一起看、替换，还是比较。'}],orchestratorVersion:AI_TURN_ORCHESTRATOR_VERSION};
  let resolved=resolveActiveView(command,workspace);if(command.operation==='restore'){const restored=restoreView(command,workspace);if(!restored)return{ok:true,pendingConfirmation:true,command:{...command,requiresConfirmation:true},blocks:[{type:'clarification',title:'还没有可恢复的上一批结果',text:'先执行一次候选探索，再使用“回到上一批/刚才那批”。'}],orchestratorVersion:AI_TURN_ORCHESTRATOR_VERSION};resolved={view:{...baseView(workspace),...restored},commitView:true,patch:command.changeSet,previousView:baseView(workspace)};}
  const view=resolved.view,changes=scopeChanges(resolved.previousView,view),changeText=changeSummary(resolved.previousView,view,command),regionExecution=resolveRegionExecution(view,workspace),result={identity:'',partial:false,rank:view.score?runRankLookup(view.score):null,candidates:null,comparison:null,selectionReview:selectionReviewRequested(input)?runSelectionReview(workspace?.selectionSnapshot||null):null,evidence:[],pendingChecks:[],decisionStage:'start',changeSummary:changeText,execution:{score:view.score,majorKeywords:view.majorKeywords,bottomLineMode:view.bottomLineMode,region:regionExecution,toolRegistryVersion:AI_TOOL_REGISTRY_VERSION}};
  if(['search','refine','restore'].includes(command.operation)&&view.score){if(regionExecution.exact){result.candidates=await runMajorBandSearch(context,{score:view.score,majorKeywords:view.majorKeywords,regionKeys:regionExecution.includeKeys,bottomLineMode:view.bottomLineMode,schoolKeyword:view.schoolNames?.length===1?view.schoolNames[0]:''});result.partial=!result.candidates.ok;}else result.partial=true;}
  if(command.operation==='compare'){const comparisonScore=validScore(command.score)||view.score;if(command.target==='school'&&command.schoolNames?.length>=2)result.comparison=await runSchoolComparison(context,{score:comparisonScore,schoolNames:command.schoolNames,majorKeywords:view.majorKeywords,regionKeys:view.regionKeys,bottomLineMode:view.bottomLineMode});else if(command.target==='major'&&command.majorKeywords?.length>=2)result.comparison=await runMajorComparison(context,{score:comparisonScore,majorKeywords:command.majorKeywords,regionKeys:view.regionKeys,bottomLineMode:view.bottomLineMode});else if(/前两所|这两所|这两个学校/.test(input)){const schools=unique((workspace?.lastResult?.candidates?.records||[]).map(item=>item?.school),2);result.comparison=await runSchoolComparison(context,{score:comparisonScore,schoolNames:schools,majorKeywords:view.majorKeywords,regionKeys:view.regionKeys,bottomLineMode:view.bottomLineMode});}else result.comparison={ok:false,message:'现在还没有两个能安全确定的比较对象。直接说“两所学校名怎么选”或“电气和机械怎么选”即可。'};result.partial=!result.comparison?.ok;}
  result.decisionStage=decisionStageFor({command,view,result,changes});result.evidence=evidenceForIntent(evidenceIntent(command,result));result.identity=resultIdentity({view,command,selectionReview:result.selectionReview});result.pendingChecks=pendingChecksFor(result,regionExecution);const delta=result.candidates?buildAiResultDelta(workspace?.lastResult||null,result,{previousView:resolved.previousView,nextView:view}):{changed:Boolean(changes.length),countChanges:{},scopeChanges:{},addedPreviewIds:[],removedPreviewIds:[],unchangedPreviewCount:0},blocks=buildBlocks({command,view,result,delta,workspace,regionExecution,changeText,stage:result.decisionStage}),assistantSummary=[changeText,result.comparison?comparisonText(result.comparison):''].filter(Boolean).join(' '),taskAction=command.operation==='save'&&command.persistence==='family'?'none':workspace?.mainTaskId?'update_main':'create_main';
  return{ok:true,pendingConfirmation:false,command,taskAction,resolvedView:view,commitView:resolved.commitView,result,delta,blocks,event:{type:'command_committed',payload:{command,taskAction,resolvedView:view,commitView:resolved.commitView,decisionStage:result.decisionStage}},provider:{provider:interpreted.provider?.provider||'',model:interpreted.provider?.model||'',source:command.source,latencyMs:interpreted.provider?.latencyMs||0,failures:interpreted.provider?.failures||[]},turnRecord:{userText:input,assistantSummary,changeSummary:changeText,blocks,command,stage:result.decisionStage},orchestratorVersion:AI_TURN_ORCHESTRATOR_VERSION};}
