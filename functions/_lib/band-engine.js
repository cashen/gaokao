import{RANGE_PRESETS}from'./band-config.js';import{getStatus}from'./status-engine.js';import{matchRegion,matchKeyword}from'./major-filter.js';export function getPreset(k){return RANGE_PRESETS[k]||RANGE_PRESETS.standard}export function makeBands(candidateScore,presetKey){const preset=getPreset(presetKey),out={};for(const key of['upper','near','steady']){const b=preset.bands[key],a=candidateScore+b.minDelta,c=candidateScore+b.maxDelta;out[key]={key,title:b.title,desc:b.desc,minScore:Math.min(a,c),maxScore:Math.max(a,c),rangeText:`${Math.min(a,c)}-${Math.max(a,c)}`}}return out}export function classifyBand(score,bands){for(const key of['upper','near','steady']){const b=bands[key];if(score>=b.minScore&&score<=b.maxScore)return key}return null}export function buildBandResult(records,{candidateScore,presetKey,filters}){const bands=makeBands(candidateScore,presetKey),grouped={upper:{...bands.upper,records:[]},near:{...bands.near,records:[]},steady:{...bands.steady,records:[]}};for(const record of records){if(!Number.isFinite(record.score))continue;if(!matchRegion(record,filters.region))continue;if(!matchKeyword(record,filters.schoolKeyword,filters.majorKeyword))continue;const band=classifyBand(record.score,bands);if(!band)continue;const delta=record.score-candidateScore,status=getStatus(delta);grouped[band].records.push({...record,band,scoreDelta:delta,statusKey:status.key,statusLabel:status.label,position:status.position})}for(const key of['upper','near','steady']){grouped[key].records.sort((a,b)=>key==='upper'?(a.score-candidateScore)-(b.score-candidateScore)||(a.rank||0)-(b.rank||0):Math.abs(a.score-candidateScore)-Math.abs(b.score-candidateScore)||(a.rank||0)-(b.rank||0));grouped[key].count=grouped[key].records.length}return grouped}
