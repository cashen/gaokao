function text(v){return String(v==null?'':v).trim()}function num(v){const n=Number(v);return Number.isFinite(n)?n:null}export function normalizeRecord(raw){return{id:text(raw.id)||`${text(raw.school)}-${text(raw.major)}-${raw.score2025}-${raw.rank2025}`,school:text(raw.school||raw.schoolName||raw['院校名称']),major:text(raw.major||raw.majorName||raw['专业名称']),score:num(raw.score2025??raw.minScore??raw.score??raw['最低分']),rank:num(raw.rank2025??raw.minRank??raw.rank??raw['最低位次']),region:text(raw.lnArea||raw.schoolProvince||raw.province||raw['省份']),nature:text(raw.schoolNatureLabel||raw.nature||''),tuition:text(raw.tuition2025||raw.tuition||''),flags:Array.isArray(raw.riskFlags)?raw.riskFlags.slice(0,4):[]}}
