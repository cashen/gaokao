import {AI_WORKSPACE_CONTRACT_VERSION,AI_EVENT_CONTRACT_VERSION,AI_RESULT_DELTA_VERSION,AI_ACTIVE_VIEW_VERSION,AI_DECISION_PROFILE_VERSION,AI_CONVERSATION_MEMORY_VERSION,AI_TURN_HISTORY_VERSION,WORKSPACE_LIMITS,DECISION_STAGES,PRIORITY_KEYS,PRIMARY_GOALS,nowIso,makeId,cloneValue,cleanText,uniqueStrings,normalizedScore,normalizedRank,viewSeed,turnSeed,createAiWorkspace,activeViewLabel,regionKeyLabel,applyAiViewPatch} from './ai-workspace-model.v3991_0.js';
export {AI_WORKSPACE_CONTRACT_VERSION,AI_EVENT_CONTRACT_VERSION,AI_RESULT_DELTA_VERSION,AI_ACTIVE_VIEW_VERSION,AI_DECISION_PROFILE_VERSION,AI_CONVERSATION_MEMORY_VERSION,AI_TURN_HISTORY_VERSION,createAiWorkspace,activeViewLabel,regionKeyLabel,applyAiViewPatch};

function viewIdentity(view={}){return JSON.stringify([normalizedScore(view.score),uniqueStrings(view.majorKeywords||[],8),uniqueStrings(view.regionKeys||['all'],8),uniqueStrings(view.schoolNames||[],4),cleanText(view.bottomLineMode,40),cleanText(view.target,60),cleanText(view.combination,20)]);}
function pushViewHistory(workspace,view){if(!view)return;const snapshot=viewSeed({...view,id:makeId('view-history')},workspace.examContext);if(workspace.viewHistory[0]&&viewIdentity(workspace.viewHistory[0])===viewIdentity(snapshot))return;workspace.viewHistory.unshift(snapshot);workspace.viewHistory=workspace.viewHistory.slice(0,WORKSPACE_LIMITS.views);}
function upsertConstraint(list=[],item={},sourceText=''){const index=list.findIndex(existing=>existing?.key===item.key),next={id:index>=0?list[index].id:makeId('constraint'),key:cleanText(item.key,80),values:uniqueStrings(item.values),label:cleanText(item.label,80),sourceText:cleanText(sourceText,280),updatedAt:nowIso()};if(index>=0)list[index]=next;else list.push(next);}
function applyFamilyChanges(workspace,command={}){if(command.persistence!=='family')return;const changes=command.familyChanges||{};if(Array.isArray(changes.regionIncludeKeys)&&changes.regionIncludeKeys.length)upsertConstraint(workspace.hardConstraints,{key:'regionInclude',values:changes.regionIncludeKeys,label:'家庭长期地区范围'},command.rawText);if(Array.isArray(changes.regionExcludeKeys)&&changes.regionExcludeKeys.length)upsertConstraint(workspace.hardConstraints,{key:'regionExclude',values:changes.regionExcludeKeys,label:'家庭明确排除地区'},command.rawText);if(Array.isArray(changes.majorExcludeKeywords)&&changes.majorExcludeKeywords.length)upsertConstraint(workspace.hardConstraints,{key:'majorExclude',values:changes.majorExcludeKeywords,label:'家庭明确不接受专业'},command.rawText);if(changes.bottomLineMode&&changes.bottomLineMode!=='all')upsertConstraint(workspace.hardConstraints,{key:'bottomLineMode',values:[changes.bottomLineMode],label:'学校性质/费用底线'},command.rawText);}
function mergeDecisionProfile(workspace,command={}){const persistable=command?.mentorProfile?.persistable;if(!persistable||typeof persistable!=='object')return;const explicit=workspace.decisionProfile.explicit;let changed=false;if(PRIMARY_GOALS.has(cleanText(persistable.primaryGoal,60))&&persistable.primaryGoal!=='undecided'){explicit.primaryGoal=cleanText(persistable.primaryGoal,60);changed=true;}const priorities=uniqueStrings(persistable.priorities||[],12).filter(value=>PRIORITY_KEYS.has(value)&&value!=='feasible_set');if(priorities.length){explicit.priorities=uniqueStrings([...explicit.priorities,...priorities],12);changed=true;}if(persistable.familyResourceSensitivity==='resource_sensitive'){explicit.familyResourceSensitivity='resource_sensitive';changed=true;}if(['prefer_short','long_ok'].includes(persistable.studyDurationTolerance)){explicit.studyDurationTolerance=persistable.studyDurationTolerance;changed=true;}if(changed){explicit.sourceTexts=uniqueStrings([...explicit.sourceTexts,cleanText(command.rawText,280)],12);explicit.updatedAt=nowIso();workspace.decisionProfile.updatedAt=explicit.updatedAt;}}
function createTaskFromCommand(command={},view={},kind='main'){return{id:makeId('task'),parentTaskId:'',kind,type:cleanText(command.operation,40)||'search',status:'ready',title:cleanText(command.taskTitle,100)||activeViewLabel(view),command:cloneValue(command),result:null,createdAt:nowIso(),updatedAt:nowIso()};}
function updateMemoryFromTurn(workspace,turn,result){const memory=workspace.conversationMemory;memory.lastUserText=cleanText(turn.userText,600);memory.lastChangeSummary=cleanText(turn.changeSummary,600);if(Array.isArray(result?.pendingChecks))memory.unresolved=uniqueStrings(result.pendingChecks.map(item=>item?.text||item).filter(Boolean),WORKSPACE_LIMITS.memory);const explicit=workspace.decisionProfile.explicit,pieces=[];if(explicit.primaryGoal!=='undecided')pieces.push(`主要目标:${explicit.primaryGoal}`);if(explicit.priorities.length)pieces.push(`优先:${explicit.priorities.join(',')}`);if(explicit.familyResourceSensitivity==='resource_sensitive')pieces.push('家庭资源/预算敏感');if(explicit.studyDurationTolerance!=='unspecified')pieces.push(`培养周期:${explicit.studyDurationTolerance}`);memory.confirmed=uniqueStrings(pieces,WORKSPACE_LIMITS.memory);memory.summary=cleanText([activeViewLabel(workspace.activeView),...memory.confirmed,turn.changeSummary].filter(Boolean).join('；'),1200);memory.updatedAt=nowIso();}

export function applyAiWorkspaceEvent(workspaceLike,eventLike={}){const workspace=createAiWorkspace(workspaceLike||{}),event={version:AI_EVENT_CONTRACT_VERSION,id:cleanText(eventLike.id,120)||makeId('event'),type:cleanText(eventLike.type,80),at:eventLike.at||nowIso(),payload:eventLike.payload&&typeof eventLike.payload==='object'?cloneValue(eventLike.payload):{}};
  if(event.type==='command_committed'){const command=event.payload.command||{},resolvedView=event.payload.resolvedView?viewSeed(event.payload.resolvedView,workspace.examContext):workspace.activeView;if(event.payload.commitView!==false&&normalizedScore(resolvedView.score))workspace.examContext.score=normalizedScore(resolvedView.score);applyFamilyChanges(workspace,command);mergeDecisionProfile(workspace,command);if(event.payload.commitView!==false&&viewIdentity(workspace.activeView)!==viewIdentity(resolvedView)){pushViewHistory(workspace,workspace.activeView);workspace.activeView={...resolvedView,id:makeId('view'),updatedAt:nowIso()};}const stage=cleanText(event.payload.decisionStage,50);if(DECISION_STAGES.has(stage))workspace.decisionStage=stage;const taskAction=cleanText(event.payload.taskAction,30)||'update_main';if(taskAction==='none'){}else if(!workspace.mainTaskId||taskAction==='create_main'){const task=createTaskFromCommand(command,resolvedView,'main');workspace.tasks.unshift(task);workspace.mainTaskId=task.id;}else if(taskAction==='branch'){const task=createTaskFromCommand(command,resolvedView,'branch');task.parentTaskId=workspace.mainTaskId;workspace.tasks.unshift(task);}else{const main=workspace.tasks.find(task=>task.id===workspace.mainTaskId);if(main){main.command=cloneValue(command);main.title=activeViewLabel(resolvedView);main.updatedAt=nowIso();}}}
  if(event.type==='view_restored'){const restored=viewSeed(event.payload.view||{},workspace.examContext);pushViewHistory(workspace,workspace.activeView);workspace.activeView={...restored,id:makeId('view'),updatedAt:nowIso()};if(restored.score)workspace.examContext.score=restored.score;}
  if(event.type==='result_committed'){const result=event.payload.result&&typeof event.payload.result==='object'?cloneValue(event.payload.result):null,rawTurn=event.payload.turn||{},factOnly=rawTurn?.command?.operation==='answer'&&rawTurn?.command?.target==='fact';workspace.lastResult=result;workspace.lastTurn=rawTurn&&typeof rawTurn==='object'?cloneValue(rawTurn):workspace.lastTurn;if(result?.rank?.rankEnd&&!factOnly)workspace.examContext.rank=Number(result.rank.rankEnd);if(DECISION_STAGES.has(cleanText(result?.decisionStage,50)))workspace.decisionStage=cleanText(result.decisionStage,50);const taskId=cleanText(event.payload.taskId,120)||workspace.mainTaskId,task=workspace.tasks.find(item=>item.id===taskId);if(task&&result){task.result=result;task.status=result.partial?'partial':'complete';task.updatedAt=nowIso();}if(Array.isArray(result?.evidence))workspace.evidence=result.evidence.slice(0,80);if(Array.isArray(result?.pendingChecks))workspace.pendingChecks=result.pendingChecks.slice(0,80);if(rawTurn.userText||rawTurn.blocks?.length){const turn=turnSeed({...rawTurn,stage:result?.decisionStage||workspace.decisionStage,viewLabel:activeViewLabel(workspace.activeView)});workspace.turnHistory.push(turn);workspace.turnHistory=workspace.turnHistory.slice(-WORKSPACE_LIMITS.turns);updateMemoryFromTurn(workspace,turn,result);}}
  if(event.type==='selection_snapshot_imported'){const snapshot=event.payload.snapshot||{};workspace.selectionSnapshot={version:cleanText(snapshot.version,80)||'ln-rank-selection-snapshot-v3991_0',importedAt:nowIso(),items:Array.isArray(snapshot.items)?snapshot.items.slice(0,WORKSPACE_LIMITS.selections):[]};}
  if(event.type==='decision_saved'){workspace.decisions.unshift({id:makeId('decision'),text:cleanText(event.payload.text,360),taskId:cleanText(event.payload.taskId,120)||workspace.mainTaskId,createdAt:nowIso()});workspace.decisions=workspace.decisions.slice(0,WORKSPACE_LIMITS.decisions);}
  workspace.tasks=workspace.tasks.slice(0,WORKSPACE_LIMITS.tasks);workspace.events.push(event);workspace.events=workspace.events.slice(-WORKSPACE_LIMITS.events);workspace.version+=1;workspace.updatedAt=nowIso();return workspace;}

function recordIds(result={}){const records=Array.isArray(result?.candidates?.records)?result.candidates.records:[];return new Set(records.map(record=>cleanText(record?.id,220)).filter(Boolean));}
function comparableView(view={}){return{score:normalizedScore(view.score),regionKeys:uniqueStrings(view.regionKeys||['all'],8),majorKeywords:uniqueStrings(view.majorKeywords||[],8),schoolNames:uniqueStrings(view.schoolNames||[],4),bottomLineMode:cleanText(view.bottomLineMode,40)||'all'};}
function dimensionDiff(previousView={},nextView={}){const before=comparableView(previousView),after=comparableView(nextView),changes={};for(const key of ['score','regionKeys','majorKeywords','schoolNames','bottomLineMode'])if(JSON.stringify(before[key])!==JSON.stringify(after[key]))changes[key]={before:before[key],after:after[key]};return changes;}
export function buildAiResultDelta(previous=null,next=null,context={}){const prevIds=recordIds(previous||{}),nextIds=recordIds(next||{}),added=[...nextIds].filter(value=>!prevIds.has(value)),removed=[...prevIds].filter(value=>!nextIds.has(value)),previousCounts=previous?.candidates?.counts||{},nextCounts=next?.candidates?.counts||{},countChanges={};for(const key of ['upper','near','steady','total']){const before=Number(previousCounts?.[key]||0),after=Number(nextCounts?.[key]||0);if(before!==after)countChanges[key]={before,after,delta:after-before};}const scopeChanges=dimensionDiff(context.previousView||{},context.nextView||{});return{version:AI_RESULT_DELTA_VERSION,addedPreviewIds:added.slice(0,50),removedPreviewIds:removed.slice(0,50),unchangedPreviewCount:[...nextIds].filter(value=>prevIds.has(value)).length,countChanges,scopeChanges,changed:Boolean(added.length||removed.length||Object.keys(countChanges).length||Object.keys(scopeChanges).length)};}
function compactLastResultForServer(result=null){if(!result||typeof result!=='object')return null;const candidates=result.candidates&&typeof result.candidates==='object'?{counts:result.candidates.counts||{},records:(Array.isArray(result.candidates.records)?result.candidates.records:[]).map(record=>({id:cleanText(record?.id,220),school:cleanText(record?.school||record?.schoolName,80),major:cleanText(record?.major||record?.majorName,120),score2026:Number.isFinite(Number(record?.score2026??record?.score))?Number(record.score2026??record.score):null,rank2026:normalizedRank(record?.rank2026??record?.rank),bandKey:cleanText(record?.bandKey||record?.band,30),displayLocation:cleanText(record?.displayLocation||record?.city,80)})).filter(record=>record.id||record.school||record.major).slice(0,24)}:null;return{identity:cleanText(result.identity,800),rank:result.rank?.rankEnd?{rankEnd:Number(result.rank.rankEnd)}:null,candidates,decisionStage:cleanText(result.decisionStage,50),execution:result.execution&&typeof result.execution==='object'?{score:normalizedScore(result.execution.score),majorKeywords:uniqueStrings(result.execution.majorKeywords||[],8),bottomLineMode:cleanText(result.execution.bottomLineMode,60),region:result.execution.region&&typeof result.execution.region==='object'?{includeKeys:uniqueStrings(result.execution.region.includeKeys||[],8),excludeKeys:uniqueStrings(result.execution.region.excludeKeys||[],8)}:null}:null};}
function compactSelectionSnapshotForServer(snapshot=null){if(!snapshot||typeof snapshot!=='object')return null;return{version:cleanText(snapshot.version,80),items:(Array.isArray(snapshot.items)?snapshot.items:[]).slice(0,WORKSPACE_LIMITS.selections).map(item=>({id:cleanText(item?.id,180),school:cleanText(item?.school,80),major:cleanText(item?.major,120),rank2026:normalizedRank(item?.rank2026),bandKey:cleanText(item?.bandKey,24),displayLocation:cleanText(item?.displayLocation,60),tuition:cleanText(item?.tuition,60)}))};}
export function compactAiWorkspaceForServer(workspaceLike){const workspace=createAiWorkspace(workspaceLike||{});return{contractVersion:workspace.contractVersion,id:workspace.id,version:workspace.version,examContext:workspace.examContext,mainTaskId:workspace.mainTaskId,tasks:workspace.tasks.slice(0,8).map(task=>({id:task.id,parentTaskId:task.parentTaskId,kind:task.kind,type:task.type,status:task.status,title:task.title,command:task.command})),hardConstraints:workspace.hardConstraints,softPreferences:workspace.softPreferences,decisionProfile:workspace.decisionProfile,decisionStage:workspace.decisionStage,conversationMemory:workspace.conversationMemory,activeView:viewSeed(workspace.activeView,workspace.examContext),viewHistory:workspace.viewHistory.slice(0,8).map(view=>({id:view.id,score:view.score,majorKeywords:view.majorKeywords,regionKeys:view.regionKeys,schoolNames:view.schoolNames,bottomLineMode:view.bottomLineMode,target:view.target,combination:view.combination,updatedAt:view.updatedAt})),recentTurns:workspace.turnHistory.slice(-8).map(turn=>({userText:cleanText(turn.userText,600),assistantSummary:cleanText(turn.assistantSummary,600),changeSummary:cleanText(turn.changeSummary,500),stage:turn.stage,viewLabel:turn.viewLabel,at:turn.at})),selectionSnapshot:compactSelectionSnapshotForServer(workspace.selectionSnapshot),lastResult:compactLastResultForServer(workspace.lastResult),pendingChecks:workspace.pendingChecks.slice(0,20).map(item=>({key:cleanText(item?.key,120),level:cleanText(item?.level,40),text:cleanText(item?.text,280)}))};}
