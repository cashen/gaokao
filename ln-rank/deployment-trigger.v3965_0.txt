v3.9.65.0 production deployment retrigger

Purpose: create a harmless production-branch change after PR #73 so the external Git-integrated deployment service receives a fresh main commit.
Business runtime, APIs, protected paths, immutable assets, and release ownership are unchanged.
