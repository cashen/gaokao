import { MAJOR_CATALOG_2026 } from '../kb/major-understanding/major-catalog-2026.generated.js';
import { createMajorCatalogResolver, normalizeMajorCode } from '../../shared/resources/majors/major-catalog-contract.js';

const RELEASE = 'v016.66-r156';
const STORE_KEY = 'gaokao:simulation-report:v002';
const LEGACY_KEY = 'gaokao:simulation-report:v001';
const HISTORY_API = '/api/ai/major-history';
const RANK_API = '/api/simulation-rank';
const FAMILY = ['继续考虑', '候选', '还没决定', '排除'];
const NOTE_MAX = 1000;
const MAJOR_LIMIT = 12;
const MANUAL = ['institutionCode', 'groupCode', 'campus', 'studyLocation', 'tuition', 'accommodationFee', 'planCount', 'studyLength', 'trainingMode', 'subjectRequirement', 'remark'];
const resolver = createMajorCatalogResolver(MAJOR_CATALOG_2026);
const norm = value => String(value ?? '').normalize('NFKC').replace(/\u00a0/g, ' ').trim();
const lower = value => norm(value).toLowerCase();
const normalizedKey = value => lower(value).replace(/[\s·•,，。；;：:'\"“”‘’!！?？_—\-（）()【】\[\]]+/g, '');
const esc = value => String(value ?? '').replace(/[&<>\"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', "'": '&#39;' }[char]));
const rankText = value => { const n = Number(value); return Number.isFinite(n) && n > 0 ? n.toLocaleString('zh-CN') : '—'; };
const emptyManual = () => Object.fromEntries(MANUAL.map(key => [key, '']));
const makeRow = order => ({ id: `${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`, order, school: '', confirmedSchool: '', majorQuery: '', majorName: '', majorCode: '', majorRecordId: '', standardMajorName: '', standardMajorCode: '', history: { years: {} }, error: '', manualCheck: emptyManual(), familyDecision: '', familyStatus: '', familyNote: '' });
const defaults = () => ({ version: 2, studentName: '', subjectTrack: '辽宁物理类（物化生）', totalScore: '', scores: { chinese: '', math: '', english: '', physics: '', chemistry: '', biology: '' }, rank: null, volunteers: [makeRow(1)] });
function read(key) { try { return JSON.parse(localStorage.getItem(key) || 'null'); } catch { return null; } }
function normalizeRecord(source, order) { const row = source || {}; const legacyMap = { '待讨论': '还没决定', '保留': '继续考虑', '备选': '候选', '已排除': '排除', '删除': '排除' }; return { ...makeRow(order), ...row, order, school: norm(row.school), confirmedSchool: norm(row.confirmedSchool), majorQuery: norm(row.majorQuery ?? row.majorName ?? row.majorCode ?? ''), majorName: norm(row.majorName), majorCode: norm(row.majorCode), majorRecordId: norm(row.majorRecordId || row.recordId || ''), standardMajorName: norm(row.standardMajorName), standardMajorCode: norm(row.standardMajorCode), history: { years: { ...((row.history || {}).years || {}) } }, manualCheck: { ...emptyManual(), ...(row.manualCheck || {}) }, familyStatus: FAMILY.includes(norm(row.familyStatus)) ? norm(row.familyStatus) : (legacyMap[norm(row.familyStatus)] || ''), familyNote: typeof row.familyNote === 'string' ? row.familyNote.slice(0, NOTE_MAX) : '' }; }
function load() { const source = read(STORE_KEY) || read(LEGACY_KEY); if (!source || !Array.isArray(source.volunteers)) return defaults(); const d = defaults(); return { ...d, ...source, version: 2, scores: { ...d.scores, ...(source.scores || {}) }, volunteers: source.volunteers.length ? source.volunteers.map(normalizeRecord) : [makeRow(1)] }; }
const runtime = { version: RELEASE, state: load(), worker: null, seq: 0, pending: new Map(), timers: new Map(), controllers: new Map(), rankController: null, expanded: new Set(), composing: new Set(), initialized: false };
function save(notify = true) { localStorage.setItem(STORE_KEY, JSON.stringify({ ...runtime.state, version: 2, volunteers: runtime.state.volunteers.map((row, index) => ({ ...row, order: index + 1, manualCheck: { ...emptyManual(), ...(row.manualCheck || {}) }, familyNote: typeof row.familyNote === 'string' ? row.familyNote.slice(0, NOTE_MAX) : '' })) })); if (notify) render(); }
function row(id) { return runtime.state.volunteers.find(item => String(item.id) === String(id)); }
function update(id, mutate, notify = true) { if (!row(id)) return; runtime.state = { ...runtime.state, volunteers: runtime.state.volunteers.map(item => String(item.id) === String(id) ? mutate({ ...item }) : item) }; save(notify); }
function toast(text) { document.querySelector('.sim-toast')?.remove(); const e = document.createElement('div'); e.className = 'sim-toast'; e.textContent = text; document.body.appendChild(e); setTimeout(() => e.remove(), 2400); }
function projectInfo(major) { const value = String(major || ''); if (/中外|合作办学|国际项目|联合培养|高收费/.test(value)) return { label: '中外合作/高收费，需核验', kind: 'sino' }; if (/预科|民族班|定向|专项|实验班|试验班|卓越班|拔尖|师范类/.test(value)) return { label: '特殊培养/项目，需核验', kind: 'special' }; return { label: '普通项目', kind: 'ordinary' }; }
function yearValue(record, year) { const score = Number(record?.[`score${year}`]); const rank = Number(record?.[`rank${year}`]); return { score: Number.isFinite(score) && score > 0 ? score : null, rank: Number.isFinite(rank) && rank > 0 ? rank : null }; }
function historyFromRecord(record) { return { years: Object.fromEntries([2026, 2025, 2024].map(year => { const value = yearValue(record, year); return [year, { ...value, comparable: Boolean(value.score || value.rank), recordStatus: value.score || value.rank ? 'history' : 'missing-year' }]; })) }; }
function historyText(rowData, year) { const value = rowData?.history?.years?.[year]; if (!value) return '—'; if (value.recordStatus === 'missing-year' || (!value.score && !value.rank)) return '暂无对应投档记录'; return `${value.score == null ? '—' : `${value.score}分`} / ${value.rank == null ? '—' : `${rankText(value.rank)}位`}`; }
function delta(rowData) { const admissionRank = Number(rowData?.history?.years?.[2026]?.rank); const currentRank = Number(runtime.state.rank); if (!Number.isFinite(admissionRank) || !Number.isFinite(currentRank) || currentRank <= 0) return '—'; const difference = admissionRank - currentRank; return difference === 0 ? '同位次' : `${difference > 0 ? '+' : ''}${difference.toLocaleString('zh-CN')}名`; }
function rowStatus(rowData) { if (!rowData.school) return ['incomplete', '先选择学校']; if (!rowData.confirmedSchool || normalizedKey(rowData.confirmedSchool) !== normalizedKey(rowData.school)) return ['needs-check', '已填学校，请从候选中确认']; if (!rowData.majorQuery) return ['incomplete', '再输入专业']; if (!rowData.majorRecordId || !rowData.majorName || !rowData.majorCode) return ['needs-check', '从该校实际专业中选一项']; return ['complete', '学校、专业和参考记录已对应']; }
function step(condition, current) { return condition ? 'step-done' : current ? 'step-current' : 'step-wait'; }
function stepsHtml(item) { const schoolDone = Boolean(item.confirmedSchool && normalizedKey(item.confirmedSchool) === normalizedKey(item.school)); const majorEntered = Boolean(item.majorQuery); const majorDone = Boolean(item.majorRecordId && item.majorName && item.majorCode); return `<ol class="step-rail" aria-label="这条志愿整理进度"><li class="${step(schoolDone, !schoolDone)}"><span class="step-num">1</span><span class="step-copy"><b>学校</b><small>${schoolDone ? '已确认' : item.school ? '等你确认' : '先选学校'}</small></span></li><li class="${step(majorDone, schoolDone && !majorDone)}"><span class="step-num">2</span><span class="step-copy"><b>专业</b><small>${majorDone ? '已确认' : majorEntered ? '请选择实际专业' : '选择学校后填写'}</small></span></li><li class="${step(majorDone, majorDone)}"><span class="step-num">3</span><span class="step-copy"><b>三年历史</b><small>${majorDone ? '随同一招生记录' : '选定专业后显示'}</small></span></li></ol>`; }
function manualHtml(item) { if (!runtime.expanded.has(String(item.id))) return ''; return `<section class="detail-panel"><div class="detail-grid">${MANUAL.map(key => { const label = { institutionCode: '院校代码', groupCode: '专业组/招生代码', campus: '校区', studyLocation: '实际培养地点', tuition: '学费', accommodationFee: '住宿费', planCount: '2026招生计划', studyLength: '学制', trainingMode: '培养方式', subjectRequirement: '选科要求', remark: '专业备注/特殊限制' }[key]; return `<label class="manual-field"><span>${label}</span>${key === 'remark' ? `<textarea data-detail-field="${key}" data-id="${esc(item.id)}" rows="2">${esc(item.manualCheck?.[key] || '')}</textarea>` : `<input data-detail-field="${key}" data-id="${esc(item.id)}" value="${esc(item.manualCheck?.[key] || '')}" />`}</label>`; }).join('')}</div><p>这些是你们自己的核对记录；空白不代表官方资料不存在。</p></section>`; }
function familyHtml(item) { const selected = FAMILY.includes(item.familyStatus) ? item.familyStatus : '还没决定'; return `<section class="family-record" aria-label="家庭处理"><div class="family-record-head"><strong>家庭处理</strong><span>记录这条志愿准备怎么处理，以及讨论后的原因。</span></div><div class="family-decision">${FAMILY.map(value => `<button type="button" data-family="${esc(value)}" data-id="${esc(item.id)}" aria-pressed="${selected === value ? 'true' : 'false'}">${esc(value)}</button>`).join('')}</div><label class="family-note-field"><span>备注</span><textarea data-family-note data-id="${esc(item.id)}" maxlength="${NOTE_MAX}" rows="2" placeholder="例如：学费可以接受，但校区需要再核实；家里暂时倾向保留。">${esc(item.familyNote || '')}</textarea><small>可写多句，输入内容会自动保存。</small></label></section>`; }
function historyHtml(item) { return `<section class="history-grid" aria-label="三年参考记录">${[2026, 2025, 2024].map(year => `<div class="history-cell"><span>${year}</span><strong>${historyText(item, year)}</strong></div>`).join('')}<div class="history-cell history-delta"><span>相对当前</span><strong>${esc(delta(item))}</strong></div></section>`; }
function cardHtml(item, index, total) { const [kind, label] = rowStatus(item); const expanded = runtime.expanded.has(String(item.id)); const confirmed = item.confirmedSchool && normalizedKey(item.confirmedSchool) === normalizedKey(item.school); const majorLabel = item.majorName ? `${item.majorName}${item.majorCode ? ` · 招生代码 ${item.majorCode}` : ''}` : '选学校后，这里只显示该校实际招生专业'; return `<article class="volunteer-card" data-card-id="${esc(item.id)}" data-row-state="${kind}"><div class="card-head"><div class="order-badge">${index + 1}</div><div class="card-heading"><strong>志愿 ${index + 1}</strong><span>${confirmed ? esc(item.confirmedSchool) : '学校确认后才能选择专业'}</span></div><div class="card-tools"><button type="button" data-action="up" data-id="${esc(item.id)}" ${index === 0 ? 'disabled' : ''}>↑ 上移</button><button type="button" data-action="down" data-id="${esc(item.id)}" ${index === total - 1 ? 'disabled' : ''}>↓ 下移</button><button type="button" data-action="delete" data-id="${esc(item.id)}">删除</button></div></div>${stepsHtml(item)}<div class="selection-grid"><div class="card-field"><label>学校</label><input data-field="school" data-id="${esc(item.id)}" value="${esc(item.school)}" placeholder="输入学校名称" autocomplete="off" enterkeyhint="search" /></div><div class="card-field"><label>专业</label><input data-field="majorCode" data-id="${esc(item.id)}" value="${esc(item.majorQuery)}" placeholder="例如 冶金工程" autocomplete="off" enterkeyhint="search" /><div class="major-caption">${esc(majorLabel)}</div><div class="input-helper" data-helper-id="${esc(item.id)}"></div></div></div><div class="candidate-slot" data-candidate-slot="${esc(item.id)}"></div><div class="record-identity" ${item.majorRecordId ? '' : 'hidden'}><span>已对应：${esc(item.majorName)} · 招生代码 ${esc(item.majorCode)}</span><small>${esc(projectInfo(item.majorName).label)}</small></div>${historyHtml(item)}<div class="state-line ${kind}"><span>${label}</span><button type="button" data-action="detail" data-id="${esc(item.id)}">${expanded ? '收起核对' : '展开核对'}</button></div>${manualHtml(item)}${familyHtml(item)}</article>`; }
function fitFamilyNote(element) { if (!element) return; element.style.height = 'auto'; const max = 180; element.style.height = `${Math.min(Math.max(element.scrollHeight, 68), max)}px`; element.style.overflowY = element.scrollHeight > max ? 'auto' : 'hidden'; }
function render() { const s = runtime.state; const name = document.querySelector('#wbStudentName'); const subject = document.querySelector('#wbSubject'); const score = document.querySelector('#wbTotalScore'); const rank = document.querySelector('#wbRank'); const source = document.querySelector('#wbRankSource'); if (name && document.activeElement !== name) name.value = s.studentName || ''; if (subject && document.activeElement !== subject) subject.value = s.subjectTrack || '辽宁物理类（物化生）'; if (score && document.activeElement !== score) score.value = s.totalScore || ''; if (rank) rank.textContent = rankText(s.rank); if (source) source.textContent = s.rank ? '2026辽宁物理类官方成绩统计口径' : '输入总分后自动换算'; const list = document.querySelector('#wbRows'); if (list) { list.innerHTML = s.volunteers.map((item, index) => cardHtml(item, index, s.volunteers.length)).join(''); list.querySelectorAll('[data-family-note]').forEach(fitFamilyNote); } const empty = document.querySelector('#wbEmpty'); if (empty) empty.hidden = s.volunteers.length > 0; const status = document.querySelector('#wbStatus'); if (status) status.textContent = s.volunteers.length ? '一条志愿只要确认学校、专业和参考记录，其他核对可以后补。' : '先加入一所已经考虑过的学校即可。'; const summary = document.querySelector('#wbSummary'); if (summary) { const c = s.volunteers.reduce((acc, item) => { const k = rowStatus(item)[0]; if (k === 'complete') acc.complete++; else if (k === 'needs-check') acc.check++; else acc.incomplete++; return acc; }, { complete: 0, check: 0, incomplete: 0 }); summary.innerHTML = `<span>共 ${s.volunteers.length} 条</span><span>✓ ${c.complete} 条已对应</span><span>⚠ ${c.check} 条待确认</span><span>○ ${c.incomplete} 条待填写</span>`; } }
function slot(id, html) { const element = document.querySelector(`[data-candidate-slot="${CSS.escape(String(id))}"]`); if (element) element.innerHTML = html; }
function helper(id, text, tone = 'neutral') { const element = document.querySelector(`[data-helper-id="${CSS.escape(String(id))}"]`); if (element) { element.textContent = text; element.dataset.tone = tone; } }
function candidateHtml(kind, items, id) { if (!items.length) return '<div class="candidate-empty">暂未找到合适候选，请继续输入。</div>'; return `<div class="candidate-title">${kind === 'school' ? '找到以下招生学校，请确认你想要的那一所' : '该校 2026 实际招生专业：请按名称和招生代码确认'}</div><div class="candidate-list">${items.map(item => { const name = norm(item.name || item.officialName); const code = norm(item.code || ''); const project = norm(item.projectLabel || item.meta || ''); const count = Number(item.historyCount); const history = kind === 'major' ? (count ? `${count} 年有记录` : '历史记录待核实') : ''; return `<button type="button" class="candidate" data-candidate-kind="${kind}" data-row-id="${esc(id)}" data-name="${esc(name)}" data-code="${esc(code)}" data-record-id="${esc(item.id || '')}" ${kind === 'major' ? `data-standard-name="${esc(item.standardName || '')}" data-standard-code="${esc(item.standardCode || '')}"` : ''}><span class="candidate-main"><strong>${esc(name)}</strong><small>${esc(kind === 'major' ? `招生代码 ${code || '待核对'} · ${project}` : (item.meta || ''))}</small></span>${kind === 'major' ? `<span class="candidate-history">${esc(history)}</span>` : ''}<span class="candidate-action">选择</span></button>`; }).join('')}</div>`; }
function schoolCandidateItems(items) { return items.map(item => ({ name: norm(item.officialName || item.name), meta: [item.province, item.city, item.level].map(norm).filter(Boolean).join(' · ') })); }
function scheduleSchoolSearch(id, query) { clearTimeout(runtime.timers.get(String(id))); if (!query) { slot(id, ''); return; } runtime.timers.set(String(id), setTimeout(() => { const seq = ++runtime.seq; const key = String(id); const previous = runtime.pending.get(key); if (previous) previous.cancelled = true; runtime.pending.set(key, { seq, query, cancelled: false }); runtime.worker?.postMessage({ type: 'search', id: key, seq, query }); }, 120)); }
function onSchool(id, value) { const query = norm(value); if (!row(id)) return; runtime.controllers.get(String(id))?.abort(); runtime.controllers.get(`major:${id}`)?.abort(); const pending = runtime.pending.get(String(id)); if (pending) pending.cancelled = true; update(id, item => ({ ...item, school: query, confirmedSchool: '', majorQuery: '', majorName: '', majorCode: '', majorRecordId: '', standardMajorName: '', standardMajorCode: '', history: { years: {} }, error: '' }), false); if (!query) { clearTimeout(runtime.timers.get(String(id))); slot(id, ''); helper(id, '输入学校名称，不需要等待页面刷新。'); return; } helper(id, '正在查找招生学校候选…'); scheduleSchoolSearch(id, query); }
function onMajor(id, value) { const query = norm(value); const current = row(id); if (!current) return; runtime.controllers.get(`major:${id}`)?.abort(); update(id, item => ({ ...item, majorQuery: query, majorName: '', majorCode: '', majorRecordId: '', standardMajorName: '', standardMajorCode: '', history: { years: {} }, error: '' }), false); if (!current.confirmedSchool) { slot(id, ''); helper(id, '请先确认学校；确认后这里只显示该校实际招生专业。'); return; } if (!query) { slot(id, ''); helper(id, '输入专业名称，可以只输入一部分。'); return; } helper(id, `${current.confirmedSchool} · 正在找实际招生专业…`); clearTimeout(runtime.timers.get(`major:${id}`)); runtime.timers.set(`major:${id}`, setTimeout(() => queryMajor(id, query), 180)); }
async function queryMajor(id, query) { const current = row(id); if (!current?.confirmedSchool || !query) return; const key = `major:${id}`; runtime.controllers.get(key)?.abort(); const controller = new AbortController(); runtime.controllers.set(key, controller); const params = new URLSearchParams({ schoolKeyword: current.confirmedSchool, major: query, limit: '120', offset: '0' }); try { const response = await fetch(`${HISTORY_API}?${params}`, { headers: { accept: 'application/json' }, signal: controller.signal }); const data = await response.json(); if (!response.ok || data?.ok === false) throw new Error(data?.message || `HTTP ${response.status}`); const records = Array.isArray(data.records) ? data.records : []; const latest = row(id); if (!latest || latest.majorQuery !== query || normalizedKey(latest.confirmedSchool) !== normalizedKey(current.confirmedSchool)) return; const candidates = records.filter(record => normalizedKey(record.school) === normalizedKey(latest.confirmedSchool)).map(record => { const name = norm(record.major); const code = norm(record.majorCode2026 || record.majorCode || record.standardMajorCode); const project = projectInfo(name); const historyCount = [2026, 2025, 2024].filter(year => { const v = yearValue(record, year); return Boolean(v.score || v.rank); }).length; return { ...record, name, code, id: norm(record.id), standardName: norm(record.standardMajorName), standardCode: norm(record.standardMajorCode), projectLabel: project.label, projectKind: project.kind, historyCount }; }).filter(item => item.name && (lower(item.name).includes(lower(query)) || (normalizeMajorCode(query) && normalizeMajorCode(item.code)?.startsWith(normalizeMajorCode(query))))) .reduce((list, item) => list.some(existing => existing.id && existing.id === item.id) ? list : [...list, item], []); candidates.sort((a, b) => (lower(a.name) === lower(query) ? -1 : 0) - (lower(b.name) === lower(query) ? -1 : 0) || a.name.length - b.name.length || a.name.localeCompare(b.name, 'zh-CN')); const shown = candidates.slice(0, MAJOR_LIMIT); runtime.pending.set(`major-record:${id}`, { query, records: shown }); slot(id, candidateHtml('major', shown, id)); helper(id, shown.length ? `找到 ${shown.length} 个该校实际招生专业，请按名称和招生代码选择。` : '暂未找到与这个输入对应的该校实际招生专业。', shown.length ? 'neutral' : 'warn'); } catch (error) { if (error?.name !== 'AbortError') helper(id, '在线查询暂时失败；当前输入会保留，可以稍后再试。', 'warn'); } finally { if (runtime.controllers.get(key) === controller) runtime.controllers.delete(key); } }
function applyMajorRecord(id, record) { const current = row(id); if (!current || !record) return; const name = norm(record.major || record.name); const code = norm(record.majorCode2026 || record.majorCode || record.code || record.standardMajorCode); const recordId = norm(record.id || record.recordId || ''); if (!name || !code || !recordId) { helper(id, '这条专业缺少招生名称、招生代码或记录编号，不能安全对应历史。', 'warn'); return; } update(id, item => ({ ...item, majorName: name, majorQuery: item.majorQuery || name, majorCode: code, majorRecordId: recordId, standardMajorName: norm(record.standardMajorName || record.standardName), standardMajorCode: norm(record.standardMajorCode || record.standardCode), history: historyFromRecord(record), error: '' })); slot(id, ''); const count = [2026, 2025, 2024].filter(year => { const v = yearValue(record, year); return Boolean(v.score || v.rank); }).length; helper(id, `✓ 已确认：${current.confirmedSchool} · ${name} · 招生代码 ${code}。${count} 年有对应记录。`, 'ok'); }
function selectSchoolCandidate(id, name) { const value = norm(name); if (!value || !row(id)) return; const pending = runtime.pending.get(String(id)); if (pending) pending.cancelled = true; runtime.controllers.get(String(id))?.abort(); clearTimeout(runtime.timers.get(String(id))); update(id, item => ({ ...item, school: value, confirmedSchool: value, majorQuery: '', majorName: '', majorCode: '', majorRecordId: '', standardMajorName: '', standardMajorCode: '', history: { years: {} }, error: '' })); slot(id, ''); helper(id, `✓ 已确认学校：${value}。现在可以直接输入专业。`, 'ok'); }
function manual(id, key, value) { update(id, item => ({ ...item, manualCheck: { ...emptyManual(), ...(item.manualCheck || {}), [key]: value } }), false); }
function updateRank() { const score = Math.round(Number(String(runtime.state.totalScore || '').trim())); const rankElement = document.querySelector('#wbRank'); const sourceElement = document.querySelector('#wbRankSource'); if (!Number.isFinite(score) || score < 150 || score > 750) { if (runtime.state.rank !== null) { runtime.state = { ...runtime.state, rank: null }; save(false); } if (rankElement) rankElement.textContent = '—'; if (sourceElement) sourceElement.textContent = '输入总分后自动换算'; return; } runtime.rankController?.abort(); const controller = new AbortController(); runtime.rankController = controller; fetch(`${RANK_API}?score=${score}`, { headers: { accept: 'application/json' }, signal: controller.signal }).then(async response => { const data = await response.json(); if (!response.ok || data?.ok === false) throw new Error(); return data; }).then(data => { if (String(runtime.state.totalScore) !== String(score)) return; runtime.state = { ...runtime.state, rank: data.rank ?? null }; save(false); if (rankElement) rankElement.textContent = rankText(runtime.state.rank); if (sourceElement) sourceElement.textContent = '2026辽宁物理类官方成绩统计口径'; }).catch(error => { if (error?.name !== 'AbortError') toast('分数位次暂时无法读取'); }).finally(() => { if (runtime.rankController === controller) runtime.rankController = null; }); }
function move(id, direction) { const list = [...runtime.state.volunteers]; const i = list.findIndex(item => String(item.id) === String(id)); const j = i + direction; if (i < 0 || j < 0 || j >= list.length) return; [list[i], list[j]] = [list[j], list[i]]; runtime.state = { ...runtime.state, volunteers: list.map((item, order) => ({ ...item, order: order + 1 })) }; save(); }
function add() { const s = runtime.state; if (s.volunteers.some(item => !item.school && !item.majorQuery)) { toast('已有一条空白志愿，直接填写即可'); return; } if (s.volunteers.length >= 30) { toast('最多整理 30 条志愿'); return; } runtime.state = { ...s, volunteers: [...s.volunteers, makeRow(s.volunteers.length + 1)] }; save(); }
function reset() { if (confirm(`确定清空 ${runtime.state.volunteers.length} 条模拟志愿？`)) { runtime.state = defaults(); runtime.expanded.clear(); save(); } }
function exportPdf() { const button = document.querySelector('#wbPdf'); if (button?.dataset.busy === '1') return; if (button) { button.dataset.busy = '1'; button.textContent = '正在生成…'; } import('./simulation-report-pdf-service.js').then(module => module.exportSimulationPdf(runtime.state, { android: /Android/i.test(navigator.userAgent) })).then(() => toast('方案已生成')).catch(error => alert(`PDF生成未完成：${error?.message || '未知错误'}`)).finally(() => { if (button) { button.dataset.busy = '0'; button.textContent = '打印 / 保存这份方案'; } }); }
function inbound() { const params = new URLSearchParams(location.search); const school = norm(params.get('school')); const code = norm(params.get('majorCode') || ''); const name = norm(params.get('majorName') || params.get('major') || ''); if (!school) return; const existing = runtime.state.volunteers.find(item => normalizedKey(item.school) === normalizedKey(school) && code && item.majorCode === code); if (existing) { toast(`这条内容已在志愿 ${runtime.state.volunteers.indexOf(existing) + 1}`); return; } const target = runtime.state.volunteers.find(item => !item.school && !item.majorQuery) || makeRow(runtime.state.volunteers.length + 1); if (!runtime.state.volunteers.some(item => String(item.id) === String(target.id))) runtime.state = { ...runtime.state, volunteers: [...runtime.state.volunteers, target] }; update(target.id, item => ({ ...item, school, confirmedSchool: '', majorQuery: name || code, majorName: '', majorCode: '', majorRecordId: '', history: { years: {} } }), false); const id = String(target.id); const seq = ++runtime.seq; runtime.pending.set(id, { seq, query: school, cancelled: false, preset: { code, name } }); runtime.worker?.postMessage({ type: 'resolve', id, seq, query: school }); render(); }
function initWorker() { runtime.worker = new Worker(`/ln-rank/js/simulation-school-search-worker-v001.js?v=${RELEASE}`, { type: 'module' }); runtime.worker.addEventListener('message', event => { const data = event.data || {}; const id = String(data.id); const pending = runtime.pending.get(id); if (!pending || pending.seq !== data.seq || pending.cancelled) return; const current = row(id); if (!current || normalizedKey(current.school) !== normalizedKey(pending.query)) return; if (data.type === 'school-candidates') { const items = schoolCandidateItems((data.candidates || []).slice(0, 6)); slot(id, candidateHtml('school', items, id)); helper(id, items.length ? '请按名称、地区和层次选择正确的学校。' : '暂未找到合适的招生学校候选。', items.length ? 'neutral' : 'warn'); return; } if (data.type === 'school-resolved') { const official = norm(data.result?.officialName); if (!official) return; update(id, item => ({ ...item, school: official, confirmedSchool: official, error: '' }), true); helper(id, `✓ 已确认学校：${official}。现在可以直接输入专业。`, 'ok'); const preset = pending.preset; runtime.pending.delete(id); if (preset?.name) { const after = row(id); if (after) { runtime.state = { ...runtime.state, volunteers: runtime.state.volunteers.map(item => String(item.id) === id ? { ...item, majorQuery: preset.name } : item) }; save(true); confirmMajor(id, preset.code, preset.name); } } return; } if (data.type === 'school-error') helper(id, '学校候选暂时不可用，可以继续输入；不会锁住后面的编辑。', 'warn'); }); }
async function confirmMajor(id, code, name) { const current = row(id); if (!current?.confirmedSchool || !name) return; const controller = new AbortController(); const key = `major:${id}`; runtime.controllers.get(key)?.abort(); runtime.controllers.set(key, controller); try { const params = new URLSearchParams({ schoolKeyword: current.confirmedSchool, major: name, limit: '120', offset: '0' }); const response = await fetch(`${HISTORY_API}?${params}`, { headers: { accept: 'application/json' }, signal: controller.signal }); const data = await response.json(); if (!response.ok || data?.ok === false) throw new Error(); const records = Array.isArray(data.records) ? data.records : []; const exact = records.find(record => normalizedKey(record.school) === normalizedKey(current.confirmedSchool) && norm(record.major) === norm(name) && norm(record.majorCode2026 || '') === norm(code)); if (!exact) throw new Error('not_found'); applyMajorRecord(id, exact); } catch (error) { if (error?.name !== 'AbortError') helper(id, '没有找到完全对应的招生记录，请从候选中重新选择。', 'warn'); } finally { if (runtime.controllers.get(key) === controller) runtime.controllers.delete(key); } }
function bind() { document.body.addEventListener('compositionstart', e => { const target = e.target?.closest?.('[data-field]'); if (target) runtime.composing.add(String(target.dataset.id)); }); document.body.addEventListener('compositionend', e => { const target = e.target?.closest?.('[data-field]'); if (!target) return; const id = String(target.dataset.id); runtime.composing.delete(id); if (target.matches('[data-field="school"]')) onSchool(id, target.value); else if (target.matches('[data-field="majorCode"]')) onMajor(id, target.value); }); document.body.addEventListener('input', e => { const target = e.target; if (target.matches('[data-field="school"]')) { if (!runtime.composing.has(String(target.dataset.id))) onSchool(target.dataset.id, target.value); } else if (target.matches('[data-field="majorCode"]')) { if (!runtime.composing.has(String(target.dataset.id))) onMajor(target.dataset.id, target.value); } else if (target.id === 'wbStudentName') { runtime.state = { ...runtime.state, studentName: target.value }; save(false); } else if (target.id === 'wbTotalScore') { runtime.state = { ...runtime.state, totalScore: target.value }; save(false); updateRank(); } else if (target.matches('[data-detail-field]')) manual(target.dataset.id, target.dataset.detailField, target.value); else if (target.matches('[data-family-note]')) { fitFamilyNote(target); update(target.dataset.id, item => ({ ...item, familyNote: norm(target.value).slice(0, NOTE_MAX) }), false); } }); document.body.addEventListener('focusin', e => { if (e.target.matches?.('[data-family-note]')) fitFamilyNote(e.target); }); document.body.addEventListener('change', e => { const target = e.target; if (target.id === 'wbSubject') { runtime.state = { ...runtime.state, subjectTrack: target.value }; save(); } else if (target.matches('[data-detail-field]')) manual(target.dataset.id, target.dataset.detailField, target.value); }); document.body.addEventListener('click', e => { const candidate = e.target.closest('[data-candidate-kind]'); if (candidate) { const id = candidate.dataset.rowId; if (candidate.dataset.candidateKind === 'school') selectSchoolCandidate(id, candidate.dataset.name); else { const cache = runtime.pending.get(`major-record:${id}`); const record = cache?.records?.find(item => norm(item.id) === norm(candidate.dataset.recordId)); if (record) applyMajorRecord(id, record); else helper(id, '这条专业候选已过期，请重新输入专业。', 'warn'); } return; } const action = e.target.closest('[data-action]'); if (action) { const id = action.dataset.id; const type = action.dataset.action; if (type === 'detail') { runtime.expanded.has(String(id)) ? runtime.expanded.delete(String(id)) : runtime.expanded.add(String(id)); render(); } else if (type === 'delete') { if (confirm('删除这条模拟志愿？只影响这里的整理清单。')) { runtime.state = { ...runtime.state, volunteers: runtime.state.volunteers.length === 1 ? [makeRow(1)] : runtime.state.volunteers.filter(item => String(item.id) !== String(id)) }; runtime.expanded.delete(String(id)); save(); } } else if (type === 'up') move(id, -1); else if (type === 'down') move(id, 1); return; } const family = e.target.closest('[data-family]'); if (family) { update(family.dataset.id, item => ({ ...item, familyStatus: family.dataset.family }), true); return; } if (e.target.closest('#wbAdd') || e.target.closest('[data-empty-add]')) add(); else if (e.target.closest('#wbReset')) reset(); else if (e.target.closest('#wbPdf')) exportPdf(); }); }
function init() { if (runtime.initialized) return; runtime.initialized = true; bind(); initWorker(); render(); updateRank(); inbound(); }
if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init, { once: true }); else init();
window.GAOKAO_SIMULATION_RUNTIME = runtime;
export { runtime };