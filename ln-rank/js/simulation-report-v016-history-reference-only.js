// History is reference-only in v016.44/r134.
// The page-level compatibility block performs the active normalization; this
// module exists as the explicit release-owned asset so the page has no missing
// script dependency and future revisions have a stable ownership boundary.
export const SIMULATION_HISTORY_REFERENCE_ONLY = 'v016.44-r134';
