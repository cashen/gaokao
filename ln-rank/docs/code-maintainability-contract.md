# 代码易读性 / 可接手性 / 维护性合同

1. 文件命名表达职责，不使用 fix.js、patch.js、final-utils.js。
2. 每个模块只做一件事：score-state 只输出状态，不查询数据，不渲染卡片。
3. 规则集中，不在多个文件散落判断 score < 400 / score > 750。
4. CSS 组件化，禁止用大扫把选择器改变组件行为。
5. 注释写“为什么”，不是重复“做了什么”。
6. 审计分层：快速门禁用于小改，完整门禁用于发版，深度门禁用于大版本。
