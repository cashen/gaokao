V3.941
辽宁物理类志愿梯度位次参考工具

版本主题：右侧轻自选池入口 + 默认不展开 + 自选池/路径分析一键飞书报告

关键变更：
1. 首页自选池从左侧悬浮改为右侧小胶囊入口。
2. 页面加载默认不打开自选池，localStorage 有历史数据也不自动展开。
3. 加入专业后只更新右侧数量和轻提示，不打断用户继续浏览专业。
4. 删除首页顶部“自选池排序”链接，完整排序页只从自选池入口进入。
5. 删除主流程中的“复制报告文本 / 下载 JSON / 本地保存·可复制到飞书”工具感文案。
6. 新增 /api/feishu-create-selection-pool-report，自选池排序与路径分析均可一键生成飞书文档。
7. 飞书报告默认复用现有逻辑：创建 docx 文档、写入内容、尝试设置公开可读、返回 URL。

数据口径：
/ln-rank 页面读取 /fenxi 已接入的辽宁 2025 物理类院校专业数据。
本工具用于形成可讨论专业池和自选池路径分析，不等同于录取预测。
