V3.942
辽宁物理类志愿梯度位次参考工具

版本主题：自选池紧凑排序工作台 + 飞书动作归位 + 志愿排序诊断人话化

关键变更：
1. /ln-rank/selection-pool.html 左侧大卡片改为紧凑排序清单，一屏可整理更多志愿。
2. “发送当前排序到飞书”归位到左侧排序清单区，表示只发送当前自选池顺序。
3. 右侧只保留诊断动作：“检查当前排序”和“发送诊断报告到飞书”。
4. “生成路径分析 / 分析后发送飞书”等机器化文案改为家长更容易理解的排序检查文案。
5. PC 端采用左 58% 排序、右 42% 诊断；小屏和平板改为上下结构；手机端单列按钮分组。
6. 首页右侧自选池仍保持小胶囊入口，默认不展开，加入专业后不打断查询。
7. 飞书报告继续复用现有创建文档、写入内容、尝试公开共享、返回链接逻辑。

数据口径：
/ln-rank 页面读取 /fenxi 已接入的辽宁 2025 物理类院校专业数据。
本工具用于形成可讨论专业池、自选池排序清单和排序诊断，不等同于录取预测。
