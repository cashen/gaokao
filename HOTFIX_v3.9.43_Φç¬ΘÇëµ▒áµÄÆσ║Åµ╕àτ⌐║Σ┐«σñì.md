# HOTFIX v3.9.43 自选池排序与清空修复

## 问题

V3.942 中 `/ln-rank/selection-pool` 出现：

- 点击“清空”后列表仍然存在；
- 点击“上移 / 下移”后顺序不变化。

## 根因

1. `getPoolItems()` 在当前版本 key 读到空数组时，会继续读取旧版本 key，导致清空后旧数据又被回填。
2. `movePoolItem()` 已经交换数组位置，但 `savePoolItems()` 内部又按旧 `userOrder` 排序，导致交换被还原。

## 修复

- 当前版本 key 一旦存在，即使是空数组，也不再回退读取旧版本 key。
- 只在当前版本 key 不存在时，才从旧版本 key 迁移一次。
- `savePoolItems()` 改为尊重传入数组顺序，并重新写入连续 `userOrder`。

## 验证

- 清空后刷新仍为空。
- 上移 / 下移后刷新仍保持新顺序。
- 删除后刷新仍保持删除结果。
