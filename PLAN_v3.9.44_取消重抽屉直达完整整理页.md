# V3.944 取消重抽屉，右侧自选池直达完整整理页

## 目标

首页右侧自选池不再打开半屏抽屉。右侧小胶囊只承担轻入口职责，点击后直接进入 `/ln-rank/selection-pool.html` 完整整理页。

## 修正点

1. 首页右侧自选池入口默认关闭，不弹抽屉。
2. 点击右侧“自选池 N”直接进入完整整理页。
3. 加入专业后只做数量跳动和轻提示，不打断查询。
4. 首页不再承载检查排序、发送飞书、清空等重动作。
5. 完整整理页继续承载：排序、清空、检查当前排序、发送当前排序到飞书、发送诊断报告到飞书。
6. 继续保留 V3.943 的清空/上移/下移修复。

## 产品原则

首页只负责查询与加入；自选池完整页负责整理、诊断和报告。避免“半吊子抽屉”同时承担预览、排序、诊断、飞书和清空，减少多层滚动和按钮堆叠。
