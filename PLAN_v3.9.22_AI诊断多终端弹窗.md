# v3.9.22 AI诊断多终端弹窗方案

## 目标

AI现实诊断在不同终端都能舒服阅读：

```text
PC：居中弹窗
平板：居中大卡片
手机：底部抽屉式弹层
极窄屏：缩小标题和间距
```

## 交互

```text
点击“现实诊断” → 打开弹层
右上角 × → 关闭
底部“阅读完成，关闭” → 关闭
点击遮罩 → 关闭
按 Esc → 关闭
```

## 关键处理

- 使用 `dvh` 适配移动端浏览器地址栏；
- 使用 `safe-area-inset-*` 适配 iPhone 刘海屏；
- 手机端底部弹层，按钮全宽，更适合拇指操作；
- 弹窗打开时锁定 body 滚动，避免背后页面乱动；
- 弹窗内部可滚动，长诊断不会撑爆页面。
