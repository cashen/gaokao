export const SCHOOL_ENTITIES_V150=Object.freeze([
E('neu-main','东北大学','official_school','','东北大学','辽宁省','沈阳市',['东北大学本部'],null,'neu'),
E('neu-qhd','东北大学秦皇岛分校','branch_school','neu-main','东北大学秦皇岛分校','河北省','秦皇岛市',['东秦','东北大学秦皇岛','东北大学秦皇岛校区','东北大学（秦皇岛分校）'],820,'neu'),
E('hit-main','哈尔滨工业大学','official_school','','哈尔滨工业大学','黑龙江省','哈尔滨市',['哈工大本部','哈工大校本部'],null,'hit'),
E('hit-weihai','哈尔滨工业大学（威海）','admission_campus','hit-main','哈尔滨工业大学（威海）','山东省','威海市',['哈工威','哈威','哈工大威海','哈尔滨工业大学威海','哈尔滨工业大学威海校区'],839,'hit'),
E('hit-shenzhen','哈尔滨工业大学（深圳）','admission_campus','hit-main','哈尔滨工业大学（深圳）','广东省','深圳市',['哈工深','哈深','哈工大深圳','哈尔滨工业大学深圳','哈尔滨工业大学深圳校区'],844,'hit'),
E('dlut-main','大连理工大学','official_school','','大连理工大学','辽宁省','大连市',['大工本部','大连理工主校区'],null,'dlut'),
E('dlut-panjin','大连理工大学（盘锦校区）','admission_campus','dlut-main','大连理工大学（盘锦校区）','辽宁省','盘锦市',['大工盘锦','大工盘','大连理工盘锦','大连理工大学盘锦校区'],1301,'dlut'),
E('ncepu-beijing','华北电力大学（北京）','official_school','','华北电力大学','北京市','昌平区',['华电北京','华北电力大学北京','华北电力大学北京校部'],null,'ncepu','华北电力大学'),
E('ncepu-baoding','华北电力大学（保定）','admission_campus','ncepu-beijing','华北电力大学（保定）','河北省','保定市',['华电保定','华电保','华北电力大学保定','华北电力大学保定校区'],846,'ncepu'),
E('cup-beijing','中国石油大学（北京）','official_school','','中国石油大学（北京）','北京市','昌平区',['中石大北京','石大北京'],57,'cup'),
E('cup-east','中国石油大学（华东）','official_school','','中国石油大学（华东）','山东省','青岛市',['中石大华东','石大华东','华东石大'],83,'cup'),
E('cup-kelamayi','中国石油大学（北京）克拉玛依校区','admission_campus','cup-beijing','中国石油大学（北京）克拉玛依校区','新疆维吾尔自治区','克拉玛依市',['中石大克拉玛依','石大克拉玛依','中石大克校区','中国石油大学克拉玛依校区'],979,'cup'),
E('cugb','中国地质大学（北京）','official_school','','中国地质大学（北京）','北京市','海淀区',['地大北京','北地'],93,'cug'),
E('cug-wuhan','中国地质大学（武汉）','official_school','','中国地质大学（武汉）','湖北省','武汉市',['地大武汉','武地'],63,'cug'),
E('cumt','中国矿业大学','official_school','','中国矿业大学','江苏省','徐州市',['矿大徐州','中国矿大徐州'],70,'cumt'),
E('cumtb','中国矿业大学（北京）','official_school','','中国矿业大学（北京）','北京市','海淀区',['矿大北京','北矿大'],94,'cumt'),
E('sdu-main','山东大学','official_school','','山东大学','山东省','济南市',['山大本部'],null,'sdu'),
E('sdu-weihai','山东大学（威海）','branch_school','sdu-main','山东大学（威海）','山东省','威海市',['山威','山东大学威海','山东大学威海分校','山东大学威海校区'],817,'sdu'),
E('bjtu-main','北京交通大学','official_school','','北京交通大学','北京市','海淀区',['北交本部'],null,'bjtu'),
E('bjtu-weihai','北京交通大学（威海校区）','admission_campus','bjtu-main','北京交通大学（威海校区）','山东省','威海市',['北交威海','北京交大威海','北京交通大学威海校区'],1426,'bjtu'),
E('bnu-main','北京师范大学','official_school','','北京师范大学','北京市','海淀区',['北师大北京'],null,'bnu'),
E('bnu-zhuhai','北京师范大学（珠海校区）','admission_campus','bnu-main','北京师范大学（珠海校区）','广东省','珠海市',['北师珠海','北师大珠海','北京师范大学珠海校区'],1421,'bnu'),
E('ruc-main','中国人民大学','official_school','','中国人民大学','北京市','海淀区',['人大北京'],null,'ruc'),
E('ruc-suzhou','中国人民大学（苏州校区）','admission_campus','ruc-main','中国人民大学（苏州校区）','江苏省','苏州市',['人大苏州','中国人民大学苏州校区'],1240,'ruc'),
E('uestc-main','电子科技大学','official_school','','电子科技大学','四川省','成都市',['成电本部'],null,'uestc'),
E('uestc-shahe','电子科技大学（沙河校区）','ordinary_campus','uestc-main','电子科技大学（沙河校区）','四川省','成都市',['成电沙河','电子科大沙河','电子科技大学沙河校区'],null,'uestc','','not_found'),
E('hfut-main','合肥工业大学','official_school','','合肥工业大学','安徽省','合肥市',['合工大本部'],null,'hfut'),
E('hfut-xuancheng','合肥工业大学（宣城校区）','admission_campus','hfut-main','合肥工业大学（宣城校区）','安徽省','宣城市',['合工宣城','合工大宣城','合肥工业大学宣城校区'],1310,'hfut')
]);
export const SCHOOL_ENTITY_GROUPS_V150=Object.freeze([
G('hit',['哈工大','哈工'],['hit-main','hit-weihai','hit-shenzhen']),
G('ncepu',['华电','华北电力大学'],['ncepu-beijing','ncepu-baoding']),
G('cup',['中国石油大学','中石大','石大'],['cup-beijing','cup-east']),
G('cug',['中国地质大学','地大'],['cugb','cug-wuhan']),
G('cumt',['矿大','中国矿大'],['cumt','cumtb'])
]);
export const SCHOOL_ENTITIES_V130=SCHOOL_ENTITIES_V150;
export const SCHOOL_ENTITY_GROUPS_V130=SCHOOL_ENTITY_GROUPS_V150;
const ENTITY_INITIAL_ALIASES=Object.freeze({
'neu-main':['dbdx'],'neu-qhd':['dq','dbdxqhd'],'hit-main':['hgdbb'],'hit-weihai':['hgw'],'hit-shenzhen':['hgs'],
'dlut-main':['dllgdx'],'dlut-panjin':['dgpj'],'ncepu-beijing':['hdbj'],'ncepu-baoding':['hdbd'],
'cup-beijing':['zsdbj'],'cup-east':['zsdhd'],'cup-kelamayi':['zsdklmy'],'cugb':['ddbj'],'cug-wuhan':['ddwh'],
'cumt':['kdxz'],'cumtb':['kdbj'],'sdu-main':['sddx'],'sdu-weihai':['sw'],'bjtu-main':['bjjtdx'],'bjtu-weihai':['bjwh'],
'bnu-main':['bjsfdx'],'bnu-zhuhai':['bszh'],'ruc-main':['zgrmdx'],'ruc-suzhou':['rdsz'],
'uestc-main':['dzkjdx'],'uestc-shahe':['cdsh'],'hfut-main':['hfgydx'],'hfut-xuancheng':['hgxc']
});
const GROUP_INITIAL_ALIASES=Object.freeze({hit:['hgd','hg'],ncepu:['hd'],cup:['zgsydx','zgsy','zsd','sd'],cug:['zgdzdx','zgdz','dd'],cumt:['kd','zgkd']});
const MUNICIPALITIES=new Set(['北京市','天津市','上海市','重庆市']);
const byId=new Map(SCHOOL_ENTITIES_V150.map(item=>[item.entityId,item]));
const byDisplay=new Map(SCHOOL_ENTITIES_V150.map(item=>[norm(item.displayName),item]));
const aliasIndex=new Map();
for(const entity of SCHOOL_ENTITIES_V150)for(const alias of [entity.displayName,...entity.aliases])add(aliasIndex,norm(alias),entity);
const groupIndex=new Map();
for(const group of SCHOOL_ENTITY_GROUPS_V150)for(const alias of group.aliases)groupIndex.set(norm(alias),group);
const entityInitialIndex=new Map();
for(const entity of SCHOOL_ENTITIES_V150)for(const code of ENTITY_INITIAL_ALIASES[entity.entityId]||[])add(entityInitialIndex,normalizeInitialQuery(code),entity);
const groupInitialIndex=new Map();
for(const group of SCHOOL_ENTITY_GROUPS_V150)for(const code of GROUP_INITIAL_ALIASES[group.groupId]||[])add(groupInitialIndex,normalizeInitialQuery(code),group);
export function getSchoolEntity(entityId){return byId.get(String(entityId||''))||null;}
export function findSchoolEntityByName(name){const rows=aliasIndex.get(norm(name))||[];return rows.length===1?rows[0]:byDisplay.get(norm(name))||null;}
export function publicSchoolEntity(entity){if(!entity)return null;const parent=getSchoolEntity(entity.parentEntityId);return{entityId:entity.entityId,displayName:entity.displayName,entityType:entity.entityType,typeLabel:typeLabel(entity.entityType),parentEntityId:entity.parentEntityId,parentSchoolName:parent?.displayName||'',province:entity.province,city:entity.city,groupId:entity.groupId,separateExperience:entity.entityType!=='official_school',sourceStatus:entity.sourceStatus};}
export function resolveEntityRequest(entityId,school){if(entityId){const entity=getSchoolEntity(entityId);if(!entity)return{error:'invalid_entity'};const names=[entity.displayName,...entity.aliases].map(norm);if(school&&!names.includes(norm(school)))return{error:'entity_school_mismatch',entity};return{entity};}return{entity:findSchoolEntityByName(school)};}
export function isEntitySourceAvailable(entity){return !entity||entity.sourceStatus!=='not_found';}
export function entitySourceQuery(entity,fallback){return entity?.sourceQuery||fallback;}
export function entitySourceId(entity){return entity?.sourceSchoolId??null;}
export function createEntityAwareResolver(baseResolver,baseMetadata){
 const metadata=new Map(baseMetadata||[]);
 for(const entity of SCHOOL_ENTITIES_V150){const inherited=metadata.get(entity.officialCatalogName)||metadata.get(entity.displayName)||{};metadata.set(entity.displayName,Object.freeze({...inherited,name:entity.displayName,province:entity.province,city:entity.city,location:formatLocation(entity.province,entity.city),level:inherited.level||'本科',entityId:entity.entityId,entityType:entity.entityType,entityTypeLabel:typeLabel(entity.entityType),parentEntityId:entity.parentEntityId,parentSchoolName:getSchoolEntity(entity.parentEntityId)?.displayName||'',sourceStatus:entity.sourceStatus}));}
 function candidatesFor(group,matchType='entity_group'){return group.entityIds.map(id=>byId.get(id)).filter(Boolean).map(entity=>candidate(entity,0.995,matchType));}
 function regionQuery(query,options={}){
  const requestedLimit=Math.max(1,Number(options.limit||8));
  const base=baseResolver.regionQuery?.(query,{...options,limit:5000});
  if(!base)return null;
  const entityRows=[];
  for(const entity of SCHOOL_ENTITIES_V150){
   if(base.region?.province&&entity.province!==base.region.province)continue;
   if(base.region?.city&&!entityMatchesCity(entity,base.region.city))continue;
   const score=base.region?.keyword?scoreEntity(norm(base.region.keyword),entity):0.9;
   if(base.region?.keyword&&score<0.5)continue;
   entityRows.push(candidate(entity,Math.max(0.9,score),'region_entity'));
  }
  const baseNames=new Set((base.candidates||[]).map(item=>item.officialName));
  const merged=dedupeCandidates([...(base.candidates||[]),...entityRows]).sort(compareRegionCandidates);
  const entityTotal=merged.filter(item=>item.entityId&&!baseNames.has(item.officialName)).length;
  return{...base,officialTotal:Number(base.total||baseNames.size),entityTotal,total:merged.length,candidates:merged.slice(0,requestedLimit)};
 }
 function resolve(query,options={}){
  const key=norm(query),group=groupIndex.get(key);
  if(group)return{status:'ambiguous',input:String(query||'').trim(),resolvedName:null,candidates:candidatesFor(group),matchType:'entity_group',confidence:0};
  const exact=aliasIndex.get(key)||[];
  if(exact.length===1)return{status:'resolved',input:String(query||'').trim(),resolvedName:exact[0].displayName,candidates:[],matchType:'entity_alias_exact',confidence:1,entityId:exact[0].entityId};
  if(exact.length>1)return{status:'ambiguous',input:String(query||'').trim(),resolvedName:null,candidates:exact.map(entity=>candidate(entity,0.99,'entity_alias_exact')),matchType:'entity_alias_exact',confidence:0};
  if(isInitialQuery(query)){
   const code=normalizeInitialQuery(query),initialGroups=groupInitialIndex.get(code)||[];
   if(initialGroups.length===1)return{status:'ambiguous',input:String(query||'').trim(),resolvedName:null,candidates:candidatesFor(initialGroups[0],'entity_group_initial'),matchType:'entity_group_initial',confidence:0};
   if(initialGroups.length>1)return{status:'ambiguous',input:String(query||'').trim(),resolvedName:null,candidates:dedupeCandidates(initialGroups.flatMap(row=>candidatesFor(row,'entity_group_initial'))),matchType:'entity_group_initial',confidence:0};
   const initialEntities=entityInitialIndex.get(code)||[];
   if(initialEntities.length===1)return{status:'resolved',input:String(query||'').trim(),resolvedName:initialEntities[0].displayName,candidates:[],matchType:'entity_initial_exact',confidence:1,entityId:initialEntities[0].entityId};
   if(initialEntities.length>1)return{status:'ambiguous',input:String(query||'').trim(),resolvedName:null,candidates:initialEntities.map(entity=>candidate(entity,0.99,'entity_initial_exact')),matchType:'entity_initial_exact',confidence:0};
  }
  const baseResult=baseResolver.resolve(query,options);
  if(baseResult.status==='region')return regionQuery(query,options)||baseResult;
  const entity=baseResult.resolvedName?byDisplay.get(norm(baseResult.resolvedName)):null;
  return entity?{...baseResult,entityId:entity.entityId}:baseResult;
 }
 function search(query,options={}){
  const region=regionQuery(query,options);if(region)return region.candidates;
  const key=norm(query),limit=Math.max(1,Number(options.limit||8)),group=groupIndex.get(key);
  if(group)return candidatesFor(group).slice(0,limit);
  const initialCode=isInitialQuery(query)?normalizeInitialQuery(query):'',initialGroups=initialCode?(groupInitialIndex.get(initialCode)||[]):[];
  if(initialGroups.length)return dedupeCandidates(initialGroups.flatMap(row=>candidatesFor(row,'entity_group_initial'))).slice(0,limit);
  const entityRows=[];
  for(const entity of SCHOOL_ENTITIES_V150){let score=scoreEntity(key,entity);if(initialCode)score=Math.max(score,scoreInitial(initialCode,entity));if(score>=0.5)entityRows.push(candidate(entity,score,score===1?'entity_alias_exact':(initialCode?'entity_initial_match':'entity_match')));}
  const merged=[...entityRows,...baseResolver.search(query,options)],seen=new Set(),out=[];
  merged.sort((a,b)=>b.score-a.score||a.officialName.length-b.officialName.length||a.officialName.localeCompare(b.officialName,'zh-CN'));
  for(const item of merged){if(seen.has(item.officialName))continue;seen.add(item.officialName);out.push(item);if(out.length>=limit)break;}
  return out;
 }
 return Object.freeze({...baseResolver,metadata,entityCount:SCHOOL_ENTITIES_V150.filter(item=>item.entityType!=='official_school').length,getMetadata(name){return metadata.get(String(name||'').trim())||baseResolver.getMetadata?.(name)||null;},resolve,search,regionQuery});
}
function E(entityId,displayName,entityType,parentEntityId,sourceQuery,province,city,aliases,sourceSchoolId,groupId,officialCatalogName='',sourceStatus='direct'){return Object.freeze({entityId,displayName,entityType,parentEntityId,sourceQuery,province,city,aliases:Object.freeze(aliases),sourceSchoolId,groupId,officialCatalogName:officialCatalogName||displayName,sourceStatus});}
function G(groupId,aliases,entityIds){return Object.freeze({groupId,aliases:Object.freeze(aliases),entityIds:Object.freeze(entityIds)});}
function norm(value){return String(value||'').normalize('NFKC').toLowerCase().replace(/[（【\[]/g,'(').replace(/[）】\]]/g,')').replace(/[\s·•,，。；;：:'"“”‘’!！?？_—-]+/g,'').trim();}
function normalizeInitialQuery(value){return String(value||'').normalize('NFKC').toLowerCase().replace(/[^a-z0-9]+/g,'');}
function isInitialQuery(value){const source=String(value||'').normalize('NFKC').trim(),normalized=normalizeInitialQuery(source);return normalized.length>=2&&/^[a-z0-9\s._-]+$/i.test(source);}
function add(map,key,value){if(!key)return;if(!map.has(key))map.set(key,[]);if(!map.get(key).includes(value))map.get(key).push(value);}
function candidate(entity,score,matchType){return{officialName:entity.displayName,score,matchType,entityId:entity.entityId};}
function dedupeCandidates(rows){const seen=new Set();return rows.filter(row=>{if(seen.has(row.officialName))return false;seen.add(row.officialName);return true;});}
function scoreEntity(query,entity){if(!query)return 0;let best=0;for(const value of [entity.displayName,...entity.aliases].map(norm)){if(query===value)return 1;if(value.startsWith(query))best=Math.max(best,0.84+0.14*query.length/value.length);else if(value.includes(query))best=Math.max(best,0.7+0.18*query.length/value.length);else if(query.includes(value)&&value.length>=3)best=Math.max(best,0.78);}return Math.min(0.99,best);}
function scoreInitial(query,entity){if(!query)return 0;let best=0;for(const code of ENTITY_INITIAL_ALIASES[entity.entityId]||[]){if(query===code)return 1;if(code.startsWith(query))best=Math.max(best,0.82+0.16*query.length/code.length);}return Math.min(0.99,best);}
function typeLabel(type){return({official_school:'独立高校',branch_school:'分校',admission_campus:'招生校区',ordinary_campus:'校区'})[type]||'学校实体';}
function entityMatchesCity(entity,city){return entity.city===city||(MUNICIPALITIES.has(city)&&entity.province===city);}
function compareRegionCandidates(a,b){const ae=a.entityId?1:0,be=b.entityId?1:0;return ae-be||b.score-a.score||a.officialName.length-b.officialName.length||a.officialName.localeCompare(b.officialName,'zh-CN');}
function formatLocation(province,city){return province&&city&&province!==city?province+' · '+city:(city||province||'');}
