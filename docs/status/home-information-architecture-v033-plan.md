# 首页信息架构 r033：按问题进入

## 基线

- 基线 main：`a8ea229f062157b4e81957047d56f5976f435319`
- 工作分支：`feat/home-problem-entry-v033`
- 当前公开发布：`v3.9.90.3 / v3990_3`
- 本轮局部修订：`r033-home-problem-entry`
- 不改变全站 generation；首页保留既有 `r031-home-redesign` 作为稳定视觉基线。

## 信息架构

1. 删除首页旧的“01 / 现在先做什么”说明区，包括三步说明和右侧家庭决策说明。
2. 删除“02”编号，不再把首页组织成教学式 01→02。
3. “按问题进入”直接成为工具区域，副文案为“想到哪一步，就从哪一步开始。”。
4. 主线只保留两个连续入口：`专业初选` → `模拟志愿`。
5. 模拟志愿只允许一个首页 owner，canonical route 为 `/ln-rank/simulation-report.html`；运行时 fallback 也必须插入在专业初选之后，不能 prepend 到最前。
6. 删除首页旧的“家庭方案与逐项复核”卡片，避免与模拟志愿表达重复。
7. 保留“了解 / 证据 / 产业”，默认收起。

## 文案

- Hero：`输入孩子的模考或预估参考分数，再结合专业方向和地区，先看看哪些专业值得继续了解。`
- 主线说明：`先看专业，再整理已经考虑的志愿`
- 专业初选：`输入参考分数，看看哪些专业值得继续了解。`
- 模拟志愿：`把已经考虑过的学校和专业放在一起，再慢慢核对。`
- 历史边界：`历史记录只用来缩小范围，正式填报还要看当年的招生资料。`

## 多终端

- 1280px：主线横向两卡，其余分类放在下方两列。
- 768px：单列分组，主线内部保持清楚的连续关系。
- 390px / 360px：全部单列，专业初选和模拟志愿连续可见。
- 所有尺寸必须无横向溢出；触摸控件保留 `touch-action: manipulation`。
- 浏览器回归使用实际 pointer/mouse 命中测试，覆盖模拟志愿和“了解”展开控件。

## 发布门禁

- HTML/JS syntax。
- 首页 owner / resource graph 不漂移。
- countdown 既有行为不改变。
- PC / Pad / Android browser regression 全通过。
- Preview 必须对应最终 PR head 的精确 SHA。
- PR 检查全部通过后才合并。
- 合并后重新验证 main SHA 与正式站首页。

## 明确不做

- 不重做 Hero、倒计时或全站视觉系统。
- 不复制模拟志愿页面。
- 不改变 `/ln-rank/simulation-report.html` canonical 路由。
- 不修改 fenxi 受保护路径。
