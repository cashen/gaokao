# V2.9.8.3.fix7｜启动关键规则内联与资源容错修正版

## 修复背景
线上 F12 显示 `rules.v297fix2.js` 被站点 fallback 返回为 `text/html`，浏览器严格 MIME 检查拒绝执行，导致启动链路中断。

## 修复内容
- 将 `LN_GAOKAO_RULES_V2953` 规则内联到 `fenxi/index.html` 启动脚本中。
- 从启动关键脚本队列移除 `assets/rules.v297fix2.js`。
- 保留 `fenxi/assets/rules.v297fix2.js` 作为历史兼容文件，但不再作为启动必需资源。
- 继续保留 fix6 的主题令牌内联修复。
- 版本戳更新为 `2983fix7-20260511`。

## 验证重点
- F12 不应再出现 `rules.v297fix2.js MIME type text/html` 阻断启动。
- 页面应能继续加载后续脚本。
- `window.LN_GAOKAO_RULES_V2953` 应存在。
