# V2.9.7.fix2｜状态就绪与路径色回调修正版

## 调整目标

本版在 V2.9.7 基础上回调视觉系统：颜色只作为路径线索，不做页面主角；PC 下家庭底线第一行保持控件高度、边框、圆角和文字层级统一。

## 主要调整

1. 顶部标题区去掉大面积蓝绿渐变，改为白底、浅蓝灰边框、低饱和路径细线。
2. 六步流程条改为白底控件，只保留顶部 2px 功能色细线。
3. 家庭预算、资格入口保护、区域筛选方式在 PC 下三列同高对齐。
4. 资格入口保护从信息卡降级为 select-like 状态控件，主页面只显示状态和“管理”。
5. 删除资格入口主页面重复说明，完整解释保留在抽屉。
6. A/B/C 与证据卡保留功能色，但降低背景染色面积。

## 不变内容

- 不改变筛选规则。
- 不改变 A/B/C 逻辑。
- 不改变资格入口保护规则。
- 不改变 Pages Function 登录。
- 不覆盖根目录首页。
