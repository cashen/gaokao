diff --git a/.github/workflows/assemble-academic-background-static-v3972.yml b/.github/workflows/assemble-academic-background-static-v3972.yml
index 6a918546..33947271 100644
--- a/.github/workflows/assemble-academic-background-static-v3972.yml
+++ b/.github/workflows/assemble-academic-background-static-v3972.yml
@@ -85,7 +85,7 @@ jobs:
           if(index.scoreBands.reduce((sum,item)=>sum+item.admissionRecordCount,0)!==index.records.length) throw new Error('band coverage');
           if(fs.statSync(file).size>3500000) throw new Error('browser index too large');
           const release=fs.readFileSync('shared/resources/release/current-release.js','utf8');
-          if(!release.includes("display: 'v3.9.72.2'")) throw new Error('release display');
+          if(!release.includes("display: 'v3.9.72.5'")) throw new Error('release display');
           if(!release.includes("all211DataVersion: 'all-211-static-v3972_0'")) throw new Error('release data owner');
           const compat=fs.readFileSync('functions/_lib/211-static-compat-response.js','utf8');
           if(!compat.includes('scannedCount: 0')||!compat.includes('build-time-static-index')) throw new Error('scan-free compatibility contract');
diff --git a/.github/workflows/deploy-cloudflare-pages-main.yml b/.github/workflows/deploy-cloudflare-pages-main.yml
index c763ae6c..57dad5d3 100644
--- a/.github/workflows/deploy-cloudflare-pages-main.yml
+++ b/.github/workflows/deploy-cloudflare-pages-main.yml
@@ -35,7 +35,7 @@ jobs:
           grep -q 'distributed-major-bands-buckets-v3972-2' 404.html
           test -f ln-rank/data/major-bands-static-v3972_2/manifest.json
           test -f ln-rank/data/major-bands-static-v3972_2/audit.json
-          grep -q "display: 'v3.9.72.2'" shared/resources/release/current-release.js
+          grep -q "display: 'v3.9.72.5'" shared/resources/release/current-release.js
           grep -q "localStrengthDataVersion: 'local-strength-static-v3971_2'" shared/resources/release/current-release.js
           grep -q "all211Architecture: 'build-time-static-index'" shared/resources/release/current-release.js
           grep -q "maximumProbeChunks: 1" functions/api/major-bands-health.js
@@ -114,11 +114,11 @@ jobs:
             --branch=main \
             --commit-hash="$GITHUB_SHA" \
             --commit-message="$commit_message"
-      - name: Verify v3.9.72.2 production with 40 concurrent cycles
+      - name: Verify v3.9.72.5 production with 40 concurrent cycles
         env:
           PAGES_BASE: https://gaokao-4y9.pages.dev
           CUSTOM_BASE: https://gaokao.powers.org.cn
-          EXPECTED_RELEASE: v3.9.72.2
+          EXPECTED_RELEASE: v3.9.72.5
           EXPECTED_LOCAL_STRENGTH_PAGE_RELEASE: v3.9.71.2
           EXPECTED_LOCAL_STRENGTH_SCORE_POSITION: local-strength-score-position-v3972_3
           PRODUCTION_VERIFY_ATTEMPTS: '45'
diff --git a/.github/workflows/migrate-canonical-release-on-pr-v3972_5.yml b/.github/workflows/migrate-canonical-release-on-pr-v3972_5.yml
deleted file mode 100644
index 45ddb389..00000000
--- a/.github/workflows/migrate-canonical-release-on-pr-v3972_5.yml
+++ /dev/null
@@ -1,64 +0,0 @@
-name: Migrate canonical release on PR v3972.5
-
-on:
-  pull_request:
-    branches:
-      - main
-
-permissions:
-  contents: write
-
-jobs:
-  migrate:
-    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'fix/canonical-release-v3972-5'
-    runs-on: ubuntu-latest
-    timeout-minutes: 30
-    steps:
-      - uses: actions/checkout@v4
-        with:
-          ref: ${{ github.event.pull_request.head.ref }}
-          fetch-depth: 0
-      - uses: actions/setup-node@v4
-        with:
-          node-version: '22'
-      - name: Apply deterministic canonical release migration
-        run: |
-          set -euo pipefail
-          python3 tools/migrate-canonical-release-v3972_5.py
-          python3 - <<'PY'
-          from pathlib import Path
-          path = Path('shared/resources/release/current-release.js')
-          text = path.read_text(encoding='utf-8')
-          text = text.replace(
-              "const EXAM = LIAONING_PHYSICS_EXAM_CONFIG;\nconst RELEASE_VERSION = 'v3.9.72.5';\nconst SITE_RUNTIME_GENERATION = 'v3972_5';\nconst ASSET_QUERY_VERSION = '3972_5';\n",
-              "const EXAM = LIAONING_PHYSICS_EXAM_CONFIG;\n"
-          )
-          for before, after in {
-              '  display: RELEASE_VERSION,': "  display: 'v3.9.72.5',",
-              '  version: RELEASE_VERSION,': "  version: 'v3.9.72.5',",
-              '  asset: ASSET_QUERY_VERSION,': "  asset: '3972_5',",
-              '  assetVersion: SITE_RUNTIME_GENERATION,': "  assetVersion: 'v3972_5',",
-              '  assetReleaseVersion: RELEASE_VERSION,': "  assetReleaseVersion: 'v3.9.72.5',",
-              '  siteRuntimeGeneration: SITE_RUNTIME_GENERATION,': "  siteRuntimeGeneration: 'v3972_5',",
-          }.items():
-              text = text.replace(before, after, 1)
-          path.write_text(text, encoding='utf-8')
-          PY
-          node --experimental-default-type=module tools/audit-canonical-release-version-v3972_5.mjs
-          node --experimental-default-type=module tools/audit-site-runtime-generation-v3972_5.mjs
-          python3 tools/ln-2026/verify-final-release-v3970.py
-          if git grep -n 'v3.9.72.2' -- ':!docs/release-acceptance/**' ':!ln-rank/data/local-strength/**' ':!ln-rank/data/211-static/**'; then
-            echo 'retired public release remains outside historical or immutable package records'
-            exit 1
-          fi
-
-          mkdir -p docs/release-acceptance
-          git diff -- .github/workflows > docs/release-acceptance/v3972-5-canonical-workflow.patch
-          git checkout HEAD -- .github/workflows
-          git clean -fd .github/workflows
-
-          git config user.name 'github-actions[bot]'
-          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
-          git add -A
-          git commit -m 'release: unify canonical version v3.9.72.5'
-          git push origin HEAD:${{ github.event.pull_request.head.ref }}
diff --git a/.github/workflows/migrate-canonical-release-v3972_5.yml b/.github/workflows/migrate-canonical-release-v3972_5.yml
deleted file mode 100644
index 13481100..00000000
--- a/.github/workflows/migrate-canonical-release-v3972_5.yml
+++ /dev/null
@@ -1,226 +0,0 @@
-name: Migrate canonical release v3972.5
-
-on:
-  push:
-    branches:
-      - fix/canonical-release-v3972-5
-    paths:
-      - '.github/workflows/migrate-canonical-release-v3972_5.yml'
-
-permissions:
-  contents: write
-
-jobs:
-  migrate:
-    runs-on: ubuntu-latest
-    timeout-minutes: 30
-    steps:
-      - uses: actions/checkout@v4
-        with:
-          ref: fix/canonical-release-v3972-5
-          fetch-depth: 0
-      - uses: actions/setup-node@v4
-        with:
-          node-version: '22'
-      - name: Unify canonical release identity and governance
-        run: |
-          set -euo pipefail
-          python3 - <<'PY'
-          from pathlib import Path
-          import subprocess
-
-          old = 'v3.9.72.2'
-          new = 'v3.9.72.5'
-          migration = Path('.github/workflows/migrate-canonical-release-v3972_5.yml')
-          historical_prefix = 'docs/release-acceptance/'
-
-          tracked = subprocess.check_output(['git', 'ls-files', '-z']).split(b'\0')
-          for raw in tracked:
-              if not raw:
-                  continue
-              rel = raw.decode('utf-8')
-              if rel == str(migration) or rel.startswith(historical_prefix):
-                  continue
-              path = Path(rel)
-              try:
-                  text = path.read_text(encoding='utf-8')
-              except (UnicodeDecodeError, IsADirectoryError):
-                  continue
-              if old in text:
-                  path.write_text(text.replace(old, new), encoding='utf-8')
-
-          release_path = Path('shared/resources/release/current-release.js')
-          release = release_path.read_text(encoding='utf-8')
-          anchor = "const EXAM = LIAONING_PHYSICS_EXAM_CONFIG;\n"
-          constants = (
-              "const EXAM = LIAONING_PHYSICS_EXAM_CONFIG;\n"
-              "const RELEASE_VERSION = 'v3.9.72.5';\n"
-              "const SITE_RUNTIME_GENERATION = 'v3972_5';\n"
-              "const ASSET_QUERY_VERSION = '3972_5';\n"
-          )
-          if "const RELEASE_VERSION = 'v3.9.72.5';" not in release:
-              release = release.replace(anchor, constants, 1)
-          replacements = {
-              "  display: 'v3.9.72.5',": "  display: RELEASE_VERSION,",
-              "  version: 'v3.9.72.5',": "  version: RELEASE_VERSION,",
-              "  asset: '3972_5',": "  asset: ASSET_QUERY_VERSION,",
-              "  assetVersion: 'v3972_5',": "  assetVersion: SITE_RUNTIME_GENERATION,",
-              "  assetReleaseVersion: 'v3.9.72.5',": "  assetReleaseVersion: RELEASE_VERSION,",
-              "  siteRuntimeGeneration: 'v3972_5',": "  siteRuntimeGeneration: SITE_RUNTIME_GENERATION,",
-          }
-          for before, after in replacements.items():
-              release = release.replace(before, after, 1)
-          release_path.write_text(release, encoding='utf-8')
-
-          contract_path = Path('shared/resources/release/site-runtime-contract.v3972_5.js')
-          contract = contract_path.read_text(encoding='utf-8')
-          import_line = "import { CURRENT_RELEASE } from './current-release.js?v=3972_5';\n\n"
-          if not contract.startswith('import { CURRENT_RELEASE }'):
-              contract = import_line + contract
-          contract = contract.replace("  generation: 'v3972_5',", "  generation: CURRENT_RELEASE.siteRuntimeGeneration,", 1)
-          contract = contract.replace("  queryVersion: '3972_5',", "  queryVersion: CURRENT_RELEASE.asset,", 1)
-          contract = contract.replace("  releaseVersion: 'v3.9.72.5',", "  releaseVersion: CURRENT_RELEASE.version,", 1)
-          contract_path.write_text(contract, encoding='utf-8')
-
-          skill_path = Path('docs/skills/unified-site-release/SKILL.md')
-          skill = skill_path.read_text(encoding='utf-8')
-          skill = skill.replace(
-              "The site has one active generation at a time. For the current release it is `v3972_5`, while the public business release remains `v3.9.72.5`.",
-              "The site has one canonical current release at a time. For this release the public version is `v3.9.72.5`, the runtime generation is `v3972_5`, and the asset query is `3972_5`; these are three encodings of the same release identity, not independent versions."
-          )
-          skill = skill.replace(
-              "5. Keep the public business release unchanged unless the task explicitly changes it.",
-              "5. Derive the public version, runtime generation and asset query from one canonical release identity. Any active-generation change must advance the canonical release; never keep an older public version while publishing a newer active runtime."
-          )
-          forbidden_anchor = "- A version number is added only to one small module while the active site graph remains unchanged.\n"
-          forbidden_rule = "- The public release, runtime generation, asset query, HTML markers, release manifest or production verifier encode different current releases.\n"
-          if forbidden_rule not in skill:
-              skill = skill.replace(forbidden_anchor, forbidden_anchor + forbidden_rule)
-          procedure_anchor = "6. Add or update source-contract tests that compare HTML, release center, cache contract, execution contract and runtime globals.\n"
-          procedure_rule = "6.1. Run the canonical-release audit and fail on any non-historical reference to a retired public release. Stable dependency versions must remain explicitly classified and must never populate current-release fields.\n"
-          if procedure_rule not in skill:
-              skill = skill.replace(procedure_anchor, procedure_anchor + procedure_rule)
-          skill_path.write_text(skill, encoding='utf-8')
-
-          audit = r'''import assert from 'node:assert/strict';
-          import fs from 'node:fs';
-          import path from 'node:path';
-          import { fileURLToPath } from 'node:url';
-          import { CURRENT_RELEASE } from '../shared/resources/release/current-release.js';
-          import { SITE_RUNTIME_CONTRACT } from '../shared/resources/release/site-runtime-contract.v3972_5.js';
-          import { LN_RANK_RUNTIME_CACHE_CONTRACT } from '../shared/resources/release/runtime-cache-contract.v3972_5.js';
-
-          const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
-          const read = rel => fs.readFileSync(path.join(ROOT, rel), 'utf8');
-          const canonicalRelease = 'v3.9.72.5';
-          const canonicalGeneration = 'v3972_5';
-          const canonicalQuery = '3972_5';
-          const retiredPublicRelease = 'v3.9.72.2';
-
-          function generationFromRelease(version) {
-            const match = /^v(\d+)\.(\d+)\.(\d+)\.(\d+)$/.exec(version);
-            assert.ok(match, `invalid canonical release ${version}`);
-            return `v${match[1]}${match[2]}${match[3]}_${match[4]}`;
-          }
-
-          assert.equal(CURRENT_RELEASE.display, canonicalRelease);
-          assert.equal(CURRENT_RELEASE.version, canonicalRelease);
-          assert.equal(CURRENT_RELEASE.assetReleaseVersion, canonicalRelease);
-          assert.equal(CURRENT_RELEASE.siteRuntimeGeneration, canonicalGeneration);
-          assert.equal(CURRENT_RELEASE.assetVersion, canonicalGeneration);
-          assert.equal(CURRENT_RELEASE.asset, canonicalQuery);
-          assert.equal(generationFromRelease(CURRENT_RELEASE.version), canonicalGeneration);
-
-          assert.equal(SITE_RUNTIME_CONTRACT.releaseVersion, canonicalRelease);
-          assert.equal(SITE_RUNTIME_CONTRACT.generation, canonicalGeneration);
-          assert.equal(SITE_RUNTIME_CONTRACT.queryVersion, canonicalQuery);
-          assert.equal(LN_RANK_RUNTIME_CACHE_CONTRACT.releaseVersion, canonicalRelease);
-          assert.equal(LN_RANK_RUNTIME_CACHE_CONTRACT.assetVersion, canonicalGeneration);
-
-          const activePages = [
-            'index.html',
-            'ln-rank/index.html',
-            'ln-rank/selection-pool.html',
-            'ln-rank/211-mainline.html'
-          ];
-          for (const rel of activePages) {
-            const html = read(rel);
-            assert.ok(html.includes(`data-release="${canonicalRelease}"`), `${rel} does not declare canonical release`);
-            assert.ok(!html.includes(`data-release="${retiredPublicRelease}"`), `${rel} exposes retired public release`);
-          }
-
-          const manifest = JSON.parse(read('ln-rank/site-active-generation.v3972_5.json'));
-          assert.equal(manifest.releaseVersion, canonicalRelease);
-          assert.equal(manifest.generation, canonicalGeneration);
-
-          assert.equal(read('VERSION.txt').trim(), canonicalRelease);
-
-          const skill = read('docs/skills/unified-site-release/SKILL.md');
-          assert.ok(skill.includes('three encodings of the same release identity'));
-          assert.ok(skill.includes('never keep an older public version while publishing a newer active runtime'));
-
-          const stable = SITE_RUNTIME_CONTRACT.preservedBusinessResources;
-          assert.equal(stable.localStrength, 'local-strength-static-v3971_2');
-          assert.equal(stable.all211, 'all-211-static-v3972_0');
-          assert.equal(stable.majorBands, 'major-bands-static-v3972_2');
-
-          console.log(JSON.stringify({
-            ok: true,
-            canonicalRelease,
-            canonicalGeneration,
-            canonicalQuery,
-            activePages,
-            preservedBusinessResources: stable
-          }, null, 2));
-          '''
-          audit = '\n'.join(line[10:] if line.startswith('          ') else line for line in audit.splitlines()) + '\n'
-          Path('tools/audit-canonical-release-version-v3972_5.mjs').write_text(audit, encoding='utf-8')
-
-          workflow = r'''name: Verify canonical release v3972.5
-
-          on:
-            pull_request:
-            workflow_dispatch:
-
-          permissions:
-            contents: read
-
-          jobs:
-            canonical-release:
-              runs-on: ubuntu-latest
-              timeout-minutes: 20
-              steps:
-                - uses: actions/checkout@v4
-                - uses: actions/setup-node@v4
-                  with:
-                    node-version: '22'
-                - name: Verify one canonical current release
-                  run: |
-                    set -euo pipefail
-                    node --experimental-default-type=module tools/audit-canonical-release-version-v3972_5.mjs
-                    node --experimental-default-type=module tools/audit-site-runtime-generation-v3972_5.mjs
-                    if git grep -n 'v3.9.72.2' -- ':!docs/release-acceptance/**'; then
-                      echo 'retired public release remains in active source, tests or deployment governance'
-                      exit 1
-                    fi
-          '''
-          workflow = '\n'.join(line[10:] if line.startswith('          ') else line for line in workflow.splitlines()) + '\n'
-          Path('.github/workflows/verify-canonical-release-v3972_5.yml').write_text(workflow, encoding='utf-8')
-
-          migration.unlink()
-          PY
-
-          node --experimental-default-type=module tools/audit-canonical-release-version-v3972_5.mjs
-          node --experimental-default-type=module tools/audit-site-runtime-generation-v3972_5.mjs
-          python3 tools/ln-2026/verify-final-release-v3970.py
-
-          if git grep -n 'v3.9.72.2' -- ':!docs/release-acceptance/**'; then
-            echo 'retired public release remains outside historical acceptance records'
-            exit 1
-          fi
-
-          git config user.name 'github-actions[bot]'
-          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
-          git add -A
-          git commit -m 'release: unify canonical version v3.9.72.5'
-          git push origin HEAD:fix/canonical-release-v3972-5
diff --git a/.github/workflows/tongxue-live-verification.yml b/.github/workflows/tongxue-live-verification.yml
index 83d8c5d8..c781c811 100644
--- a/.github/workflows/tongxue-live-verification.yml
+++ b/.github/workflows/tongxue-live-verification.yml
@@ -75,7 +75,7 @@ jobs:
           import fs from 'node:fs';
           import { CURRENT_RELEASE } from './shared/resources/release/current-release.js';
           import { SITE_RUNTIME_CONTRACT } from './shared/resources/release/site-runtime-contract.v3972_5.js';
-          if(CURRENT_RELEASE.display!=='v3.9.72.2'||CURRENT_RELEASE.assetReleaseVersion!=='v3.9.72.2') throw new Error('release mismatch');
+          if(CURRENT_RELEASE.display!=='v3.9.72.5'||CURRENT_RELEASE.assetReleaseVersion!=='v3.9.72.5') throw new Error('release mismatch');
           if(CURRENT_RELEASE.siteRuntimeGeneration!==SITE_RUNTIME_CONTRACT.generation||SITE_RUNTIME_CONTRACT.generation!=='v3972_5') throw new Error('site generation mismatch');
           if(CURRENT_RELEASE.schoolQueryVersion!=='school-query-contract-v3969_0') throw new Error('school query mismatch');
           if(CURRENT_RELEASE.tongxueRuntimeVersion!=='tongxue-runtime-v159-r3968') throw new Error('Tongxue stable package mismatch');
@@ -91,7 +91,7 @@ jobs:
             if(source.includes('MutationObserver')||source.includes('setInterval(')||source.includes('setTimeout(')) throw new Error(`delayed ownership ${rel}`);
           }
           if(!controller.includes('observerCount: 0')||controller.split('function bindEvents').length-1!==1) throw new Error('controller owner');
-          console.log('Tongxue stable v159 package under site generation v3972_5 and release v3.9.72.2 passed');
+          console.log('Tongxue stable v159 package under site generation v3972_5 and release v3.9.72.5 passed');
           NODE
 
       - name: Verify school resolver and unified query contracts
diff --git a/.github/workflows/verify-dual-search-v3963.yml b/.github/workflows/verify-dual-search-v3963.yml
index bc5a5897..3bb18727 100644
--- a/.github/workflows/verify-dual-search-v3963.yml
+++ b/.github/workflows/verify-dual-search-v3963.yml
@@ -60,7 +60,7 @@ jobs:
           const html=fs.readFileSync('ln-rank/index.html','utf8');
           const home=fs.readFileSync('index.html','utf8');
           const plan=fs.readFileSync('ln-rank/selection-pool.html','utf8');
-          if(CURRENT_RELEASE.display!=='v3.9.72.2') throw new Error('release');
+          if(CURRENT_RELEASE.display!=='v3.9.72.5') throw new Error('release');
           if(CURRENT_RELEASE.siteRuntimeGeneration!=='v3972_5'||CURRENT_RELEASE.assetVersion!=='v3972_5') throw new Error('site generation');
           if(CURRENT_RELEASE.resourceExecutionVersion!==RESOURCE_EXECUTION_VERSION) throw new Error('execution contract');
           if(LN_RANK_RUNTIME_CACHE_CONTRACT.assetVersion!==SITE_RUNTIME_CONTRACT.generation) throw new Error('cache generation');
@@ -74,7 +74,7 @@ jobs:
           if(!html.includes('/ln-rank/js/app.v3972_5.js?v=3972_5')) throw new Error('selection bootstrap');
           if((html.match(/data-ui-navigation="auxiliary-background"/g)||[]).length!==2) throw new Error('auxiliary actions');
           if(/<a[^>]+href="\/ln-rank\/(local|211)-mainline\.html"/.test(html)) throw new Error('native auxiliary navigation');
-          console.log('one active site generation v3972_5 passed under v3.9.72.2');
+          console.log('one active site generation v3972_5 passed under v3.9.72.5');
           NODE
 
       - name: Install Chromium journey runner
diff --git a/.github/workflows/verify-ln-2026-final.yml b/.github/workflows/verify-ln-2026-final.yml
index a4f3e6fa..38563e8d 100644
--- a/.github/workflows/verify-ln-2026-final.yml
+++ b/.github/workflows/verify-ln-2026-final.yml
@@ -71,7 +71,7 @@ jobs:
           import { LN_RANK_RUNTIME_CACHE_CONTRACT } from './shared/resources/release/runtime-cache-contract.v3972_5.js';
           import { lookupScoreRank, getRankTableRows, getRankPopulation, validateScoreRank } from './functions/_lib/rank-table-provider.js';
           import { buildHistoricalScoreRankEvidence } from './functions/_lib/historical-score-rank-evidence.js';
-          if(CURRENT_RELEASE.display!=='v3.9.72.2'||CURRENT_RELEASE.assetVersion!=='v3972_5'||CURRENT_RELEASE.assetReleaseVersion!=='v3.9.72.2') throw new Error(JSON.stringify(CURRENT_RELEASE));
+          if(CURRENT_RELEASE.display!=='v3.9.72.5'||CURRENT_RELEASE.assetVersion!=='v3972_5'||CURRENT_RELEASE.assetReleaseVersion!=='v3.9.72.5') throw new Error(JSON.stringify(CURRENT_RELEASE));
           if(CURRENT_RELEASE.siteRuntimeGeneration!==SITE_RUNTIME_CONTRACT.generation) throw new Error('site generation owner');
           if(CURRENT_RELEASE.resourceExecutionVersion!==RESOURCE_EXECUTION_VERSION) throw new Error('resource execution owner');
           if(CURRENT_RELEASE.schoolQueryVersion!=='school-query-contract-v3969_0') throw new Error('school query owner');
diff --git a/.github/workflows/verify-production-api-health-v3971.yml b/.github/workflows/verify-production-api-health-v3971.yml
index 48138fe4..739ff85c 100644
--- a/.github/workflows/verify-production-api-health-v3971.yml
+++ b/.github/workflows/verify-production-api-health-v3971.yml
@@ -143,7 +143,7 @@ jobs:
       - name: Verify Cloudflare Preview Worker and LocalStrength journey
         if: github.event_name == 'pull_request'
         env:
-          EXPECTED_RELEASE: v3.9.72.2
+          EXPECTED_RELEASE: v3.9.72.5
           EXPECTED_LOCAL_STRENGTH_PAGE_RELEASE: v3.9.71.2
           EXPECTED_LOCAL_STRENGTH_SCORE_POSITION: local-strength-score-position-v3972_3
           PRODUCTION_VERIFY_ATTEMPTS: '1'
@@ -181,12 +181,12 @@ jobs:
         run: |
           set -euo pipefail
           node tools/verify-production-baseline-v3971.mjs 2>&1 | tee /tmp/v3972-production-health.out
-      - name: Verify v3.9.72.2 production after main deployment
+      - name: Verify v3.9.72.5 production after main deployment
         if: github.event_name != 'pull_request'
         env:
           PAGES_BASE: https://gaokao-4y9.pages.dev
           CUSTOM_BASE: https://gaokao.powers.org.cn
-          EXPECTED_RELEASE: v3.9.72.2
+          EXPECTED_RELEASE: v3.9.72.5
           EXPECTED_LOCAL_STRENGTH_PAGE_RELEASE: v3.9.71.2
           EXPECTED_LOCAL_STRENGTH_SCORE_POSITION: local-strength-score-position-v3972_3
           PRODUCTION_VERIFY_ATTEMPTS: '45'
diff --git a/.github/workflows/verify-production-release-v3970.yml b/.github/workflows/verify-production-release-v3970.yml
index aa034f0a..fa6514b8 100644
--- a/.github/workflows/verify-production-release-v3970.yml
+++ b/.github/workflows/verify-production-release-v3970.yml
@@ -77,7 +77,7 @@ jobs:
         run: |
           set -euo pipefail
           mkdir -p /tmp/v3972-5-production
-          expected_release='v3.9.72.2'
+          expected_release='v3.9.72.5'
           expected_generation='v3972_5'
 
           fetch_response() {
@@ -118,7 +118,7 @@ jobs:
             preserved_resources=1
 
             if [ "$(cat /tmp/v3972-5-production/pages-all211.status)" != '200' ] \
-              || ! grep -q 'data-release="v3.9.72.2"' /tmp/v3972-5-production/pages-all211.body \
+              || ! grep -q 'data-release="v3.9.72.5"' /tmp/v3972-5-production/pages-all211.body \
               || [ "$(cat /tmp/v3972-5-production/pages-all211-data.status)" != '200' ] \
               || ! grep -q '"version":"all-211-static-v3972_0"' /tmp/v3972-5-production/pages-all211-data.body \
               || [ "$(cat /tmp/v3972-5-production/pages-local.status)" != '200' ] \
