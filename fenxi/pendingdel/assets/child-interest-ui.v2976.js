// V2.9.7.6 child-interest UI: shows auto-mapped interests and manual professional cards.
(function(){
  function esc(v){const fn=window.htmlSafeV2945||window.v2950Text; if(typeof fn==='function')return fn(String(v??'')); return String(v??'').replace(/[&<>"']/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[s]));}
  function rt(){return window.LN_CHILD_INTEREST_RUNTIME_V2976||window.LN_CHILD_INTEREST_RUNTIME_V296;}
  let search='';
  function autoChips(auto){return (auto||[]).map(x=>`<span class="auto-interest-chip-v2976">${esc(x.label)}<button title="取消自动带入" data-action="child-interest-auto-toggle" data-intent-id="${esc(x.intentId)}" data-interest-id="${esc(x.interestId)}">×</button></span>`).join('');}
  function renderSummary(){const box=document.getElementById('childInterestBoxV2955'); if(!box||!rt())return; const s=rt().readState(); const sum=rt().summary(); const manual=(s.selectedGroups||[]).map(id=>rt().groupById(id)).filter(Boolean); const auto=rt().autoMappings?.(s)||[]; box.innerHTML=`<div class="child-interest-compact-v296 ${sum.names?.length?'selected':'undecided'}"><div class="compact-head-v296"><div><b>${esc(sum.title)}</b><span>${esc(sum.text)}</span></div><button class="execute-secondary" data-action="child-interest-start">编辑兴趣</button></div><div class="child-interest-tags-v296">${manual.map(g=>`<span>${esc(g.name)}<button aria-label="移除${esc(g.name)}" data-action="child-interest-remove" data-interest-id="${esc(g.id)}">×</button></span>`).join('')}${autoChips(auto)||(!manual.length?'<em>暂不确定，综合推荐</em>':'')}</div><div class="compact-actions-v296"><button class="secondary slim" data-action="child-interest-undecided">暂不确定，听系统推荐</button><label><input type="checkbox" id="onlyChildInterestV296" ${s.manualOnlyInterest?'checked':''}/> 只看孩子已选/带入方向</label></div></div>${window.LN_CHILD_INTENT_UI_V2976?.renderInline?.()||''}`; window.LN_CHILD_INTENT_UI_V2976?.bind?.(box); const chk=document.getElementById('onlyChildInterestV296'); if(chk&&!chk.dataset.bound){chk.dataset.bound='1'; chk.addEventListener('change',()=>{const st=rt().readState(); st.manualOnlyInterest=chk.checked; rt().saveState(st); renderSummary(); window.LN_REFRESH_SCHEDULER_V296?.request?.({reason:'child-interest-change',level:'soft',delay:180});});}}
  function groupCard(g,s,q){const on=(s.selectedGroups||[]).includes(g.id); const auto=(rt().autoMappings?.(s)||[]).some(x=>x.interestId===g.id); const text=[g.name,g.desc,(g.match?.core||[]).join(' '),(g.match?.related||[]).join(' '),(g.match?.review||[]).join(' ')].join(' '); if(q&&!text.includes(q))return ''; const tags=(arr,n)=>(arr||[]).slice(0,n).map(x=>`<span>${esc(x)}</span>`).join('')||'<span>暂无</span>'; return `<div class="child-interest-card-v296 ${on?'active':''} ${auto?'auto-mapped-v2976':''} color-${esc(g.color||'gray')}" role="button" tabindex="0" data-child-interest-group="${esc(g.id)}"><div class="card-main-v296"><strong>${esc(g.name)}${auto?'｜自动带入':''}</strong><span>${esc(g.desc||'')}</span><em>${esc(g.cycle?.label||'需结合孩子适配复核')}</em></div><div class="interest-subtags-v296"><b>正主</b>${tags(g.match?.core,4)}</div><div class="interest-subtags-v296"><b>相近</b>${tags(g.match?.related,4)}</div><div class="interest-subtags-v296 review"><b>需复核</b>${tags(g.match?.review,3)}</div></div>`;}
  function renderDrawerBody(){if(!rt())return; const s=rt().readState(); const q=search.trim(); const manual=(s.selectedGroups||[]).map(id=>rt().groupById(id)).filter(Boolean); const auto=rt().autoMappings?.(s)||[]; const body=`<div class="child-interest-drawer-v296"><p class="drawer-help-v296">孩子兴趣只做软加权和解释优先级，不替代家庭底线。自动带入来自孩子想法，可取消，也可手动补选方向。</p>${window.LN_CHILD_INTENT_UI_V2976?.renderInline?.()||''}<div class="child-interest-selected-line-v296">${manual.map(g=>`<span>${esc(g.name)}<button data-action="child-interest-remove" data-interest-id="${esc(g.id)}">×</button></span>`).join('')}${autoChips(auto)||(!manual.length?'<em>当前为综合推荐</em>':'')}</div><div class="child-interest-search-v296"><input id="childInterestSearchV296" placeholder="搜索专业方向，例如：动物医学、法学、电气、仪器" value="${esc(search)}"/><button class="secondary slim" data-action="child-interest-undecided">暂不确定</button></div><div class="child-interest-grid-v296">${rt().groups().map(g=>groupCard(g,s,q)).join('')||'<div class="notice">没有匹配方向，可以换一个关键词。</div>'}</div><div class="child-interest-cycle-v296"><b>周期提醒：</b>兴趣会变化，建议在分数、家庭底线或招生计划更新后重新确认一次。</div></div>`; window.LN_DRAWER_V296?.setBody?.(body); window.LN_CHILD_INTENT_UI_V2976?.bind?.(document); const input=document.getElementById('childInterestSearchV296'); if(input){input.focus(); input.setSelectionRange(input.value.length,input.value.length); let timer=null; input.addEventListener('input',()=>{search=input.value; clearTimeout(timer); timer=setTimeout(renderDrawerBody,120);});}}
  function openDrawer(){window.LN_DRAWER_V296?.open?.('孩子兴趣与专业方向','<div class="notice">正在加载...</div>'); renderDrawerBody();}
  const api={renderSummary,openDrawer,renderDrawerBody,ready:true}; window.LN_CHILD_INTEREST_UI_V2976=api; window.LN_CHILD_INTEREST_UI_V296=api;
})();
