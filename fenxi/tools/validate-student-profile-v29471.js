const fs=require('fs');const p='data/student_profile_model/student_profile_rules_v29471.json';if(!fs.existsSync(p)){console.error('missing',p);process.exit(1)}const data=JSON.parse(fs.readFileSync(p,'utf8'));if(!Array.isArray(data.rules)||data.rules.length<5){console.error('too few rules');process.exit(1)}const bad=JSON.stringify(data).match(/女孩不适合|男孩适合|女生不适合|男生适合/);if(bad){console.error('gender stereotype phrase found');process.exit(1)}console.log('[OK] student profile rules',data.rules.length);