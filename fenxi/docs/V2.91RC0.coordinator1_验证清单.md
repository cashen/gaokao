# V2.91RC0.coordinator1 验证清单

## 版本确认

- 页面标题显示 `V2.91RC0.coordinator1`
- Debug 页面显示 `V2.91RC0.coordinator1`
- 自测报告版本戳为 `291rc0-coordinator1-20260513`

## Debug deep 自测

重点确认以下步骤 PASS：

- rules-core1 分段规则包加载与导出检查
- ui-core1 分段 UI 包加载与导出检查
- coordinator1 协调层门面加载与导出检查
- 学生画像抽屉打开/变更/关闭联动
- 孩子兴趣抽屉打开/点选/上限/删除/关闭
- 只看真实命中兴趣方向 + 快速预筛
- 场景卡联动
- A/B/C、翻页、候选区基础联动
- 96 个矩阵场景

## 关键标记

最终标记应包含：

```text
rulesBundle = v291rc0rules1
uiBundle = v291rc0ui1
appCoordinator = v291rc0coord1
safePerfOpt = true
computePipeline = v2983fix12
interactFix = v29rcfix1
interactDedupe = v29rcfix2
```
