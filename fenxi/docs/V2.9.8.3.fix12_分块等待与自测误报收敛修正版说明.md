# V2.9.8.3.fix12｜分块等待与自测误报收敛修正版

本版只修 Debug 自测误报与主页面分块加载提示。

- 深度矩阵自测会等待位次分块数据加载完成后再断言 `loadedRows>0`。
- 自测报告新增 `waitData=xxms rows=xx`，便于区分“业务失败”和“分块未就绪”。
- 位次已输入但 DATA 尚未就绪时，主页面显示“正在加载位次附近数据”，不再直接表现成空结果。
- 保留 fix11 的 config 内联启动修复，保留 fix9 的兴趣快速预筛。
