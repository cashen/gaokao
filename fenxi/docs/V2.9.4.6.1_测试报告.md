# V2.9.4.6.1 测试报告

## 测试范围

本次测试重点验证：

- V2.9.4.6 原数据包是否保留
- V2.9.4.6.1 新增 JS / CSS 是否被入口页引用
- 卡片显示模式脚本是否存在
- 易混专业模型是否仍可加载
- 家长关心点模型是否仍兼容
- 原专业名三层口径数据是否仍通过校验

## 已执行命令

```bash
node --check assets/app.v29461.js
node --check assets/confusable-major-model.v2946.js
node --check assets/major-name-model.v2946.js
node --check tools/validate-confusable-major-model-v2946.js
node --check tools/validate-major-name-model-v2944.js
node --check tools/validate-parent-interest-v2946.js
node --check tools/validate-card-density-v29461.js

node tools/validate-confusable-major-model-v2946.js
node tools/validate-major-name-model-v2944.js
node tools/validate-parent-interest-v2946.js
node tools/validate-card-density-v29461.js
```

## 校验结果

通过：

- 易混专业模型校验
- 专业名三层模型校验
- 家长关心点兼容校验
- 卡片折叠与显示模式校验
- JS 语法校验
- 静态资源 HTTP 访问检查

## 数据保留情况

V2.9.4.6 数据仍保留：

```text
易混规则组：13 组
易混成员：412 个
自动检测易混专业对：2,752 对
record 索引：2,425 条
涉及学校：440 所
```

V2.9.4.4 专业名数据仍保留：

```text
投档记录：13,145 条
招生专业名：3,003 个
本科目录专业：850 个
招生名→本科目录映射：6,543 条
研究生参考：1,322 条
```

## 浏览器测试说明

容器内 Chromium / Playwright 访问本地页面仍被环境策略阻断，报 `net::ERR_BLOCKED_BY_ADMINISTRATOR`。因此本次没有把 Chromium 渲染截图算作通过项。

已完成 HTTP 静态资源检查，以下文件返回 200：

- `index.html`
- `assets/app.v29461.js`
- `assets/app.v29461.css`
- `assets/confusable-major-model.v2946.js`
- `data/confusable_major_model/v2946_manifest.json`
- `data/manifest.json`

## 人工本地测试建议

打开 `index.html` 后：

```text
访问码：ln2025
分数：480
学校：营口理工学院
专业：大数据
勾选：只看易混专业提醒
```

重点检查：

1. 默认卡片是否比 V2.9.4.6 更短。
2. 是否出现“卡片显示：紧凑 / 标准 / 详细”。
3. 易混提醒是否默认只显示摘要。
4. 点击后是否显示完整对比表。
5. 家长必读是否默认折叠摘要。
6. 手机端默认是否更紧凑。
