# V2.9.5.2｜测试报告

## 已执行

- `node --check assets/rules.v2952.js`
- `node --check assets/app.v2952.js`
- `node tools/validate-v2952-release.js`
- zip 完整性测试

## 重点验证项

1. `index.html` 已切换至 `rules.v2952.js / app.v2952.js / app.v2952.css`。
2. 第一部分不再包含“填报目标”。
3. 第三部分包含“当前目标路径”。
4. 目标路径下拉框由 `preferenceRules` 自动生成。
5. 场景卡仍由 `scenarioPresets` 自动生成。
6. 生产包内不存在历史 `assets/app.v294xx.js/css`、`assets/app.v2950.js/css`、`assets/app.v2951.js/css`。
7. 高校专项默认隐藏、中外合作提档、主专业归一搜索继续保留。

## 建议人工点测

- 610 分，选择高分平台稳妥，再手动改目标路径为保研/深造，检查是否显示“已手动微调”。
- 460 分，选择本科机会边缘，检查 A/B/C 是否偏 A 但仍保留 B/C 对照。
- 预算较宽 + 会计学 + 中外合作，检查 C 方案是否纳入中外合作提档候选。
- 未通过高校专项时，检查高校专项不进入 A/B/C。
