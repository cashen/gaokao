# V2.91RC0.ui-core1 验证清单

## 一、版本确认

- 页面标题显示 `V2.91RC0.ui-core1`
- Debug 页面显示 `V2.91RC0.ui-core1`
- 自测报告版本戳为 `291rc0-ui-core1-20260513`

## 二、Debug 自测

打开 `/fenxi/debug.html`，运行 deep 自测。

必须看到：

- `rules-core1 分段规则包加载与导出检查` PASS
- `ui-core1 分段 UI 包加载与导出检查` PASS
- 96 个矩阵场景 PASS
- 失败 0，警告 0

## 三、人工重点检查

- 学生画像抽屉：打开、选择、关闭
- 孩子兴趣抽屉：打开、点选、上限拦截、删除、关闭
- 场景卡：employment/grid/exam/broad
- A/B/C：显示正常
- 候选卡：分页正常
- 详情卡：打开正常
- 高级筛选入口：可打开
- 导出入口：存在
- interact1/interact2：仍然生效

## 四、回退测试

将：

```js
window.LN_UI_BUNDLE_OPT = true;
```

改为：

```js
window.LN_UI_BUNDLE_OPT = false;
```

应回到旧 UI 单文件加载队列。
