# V2.9.5.3｜模块化解耦与加载瘦身版

本版不新增业务功能，重点是工程结构重排：

1. 拆出 config、rules、data-engine、filter-engine、plan-engine、render、selection、export、app。
2. 清理 index.html 的内联 onclick，改用事件委托。
3. 继续保留独立规则集，场景卡和目标路径仍由 rules 文件控制。
4. 生产包只保留当前运行链路，旧 app 入口不再进入部署包。
5. 为后续筛选引擎、A/B/C 方案引擎和导出模块单独维护打基础。
