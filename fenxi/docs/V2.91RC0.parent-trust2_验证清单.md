# V2.91RC0.parent-trust2 验证清单

## 必跑

```bash
cd fenxi
node tools/validate-v291rc0-parent-trust2-release.js
```

## 线上 deep 自测

访问 `/fenxi/debug.html`，输入访问凭证后运行 deep。

应满足：

- 失败 0。
- 警告 0。
- parentTrust = v291rc0parenttrust2。
- scenarioRaw 字段存在。
- scenarioEffective 字段存在且可读。
- effectivePriority 可读。
- prioritySource = scenario-default 或 user-tuned。

## 不能变

- compute-pipeline / filter-engine / plan-engine 不得改。
- A/B/C 方案计算不得改。
- 兴趣真正对口模式不得改。
- 地域 hard 不得改。
- fenxi-only update，不覆盖根目录首页。
