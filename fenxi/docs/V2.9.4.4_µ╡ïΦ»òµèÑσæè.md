# V2.9.4.4 测试报告

## 已执行测试

1. `node --check assets/app.v2944.js`
2. `node --check assets/major-name-model.v2944.js`
3. `node tools/validate-major-name-model-v2944.js`
4. 静态检查：index.html 引用的 JS/CSS 文件均存在。
5. JSON 检查：`data/major_name_model/*.json` 全部可解析。
6. HTTP 烟测：通过本地 `python3 -m http.server` 访问 index、JS、CSS、manifest、major_name_model 主要 JSON，全部返回 200。

## 数据质量结果

- overall_passed：True
- 原分块投档记录：13145
- 招生专业名复核项：3003
- 本科目录专业：850
- 映射行：6543
- 研究生参考行：1322

## 注意

容器里的 Chromium headless 在最小系统环境下无法正常退出，连 `data:text/html` 也会超时，属于浏览器运行环境限制；因此本次采用 Node 语法检查 + JSON 校验 + HTTP 烟测确认部署文件可被浏览器访问。实际普通浏览器打开不依赖该容器的 Chromium。
