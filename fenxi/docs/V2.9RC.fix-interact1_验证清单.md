# V2.9RC.fix-interact1 验证清单

## 1. 版本检查

页面显示：

```text
V2.9RC.fix-interact1
```

`/fenxi/debug.html` 报告中应能看到：

```text
interactFix = v29rcfix1
interactVersion = 29rc-interact1-20260513
```

## 2. 业务一致性检查

固定三个样本：

```text
625 分 / 全国 / 平台优先
600 分 / 辽宁 / 稳就业
500 分 / 辽宁 / 普通家庭 / 兴趣真实命中
```

对比旧版：

```text
有效候选数量
A/B/C 推荐
候选前 10
兴趣命中数量
地域 hard 结果
```

要求结果一致。

## 3. 点击卡顿检查

打开兴趣抽屉，不做任何修改，直接关闭。

预期：

```text
不应触发 applyFilters
debug 中 interactRefreshDecision = suppress-no-change
```

修改一个兴趣后关闭。

预期：

```text
仍会触发 applyFilters
但 debug 中 interactRefreshDecision = delay-after-close
```

## 4. 回退检查

把：

```js
window.LN_INTERACT_FIX_OPT = true;
```

改为：

```js
window.LN_INTERACT_FIX_OPT = false;
```

预期：

```text
交互去重关闭
页面仍可正常使用
候选结果不变
```
