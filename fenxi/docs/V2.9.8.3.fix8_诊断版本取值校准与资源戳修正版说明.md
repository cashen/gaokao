# V2.9.8.3.fix8｜诊断版本取值校准与资源戳修正版

本版不改变业务筛选逻辑，专门修复 fix7 包中 debug-runtime、debug.html、app.v2983.js 仍显示 fix4/fix5 旧版本号的问题。

## 修复项

- `fenxi/index.html` 版本戳更新为 `2983fix8-20260511`。
- 增加 `window.__LN_TOOL_VERSION` 与 `window.__LN_TOOL_STAMP`，作为 debug 统一取值来源。
- `debug-runtime.v2983.js` 不再硬编码 `V2.9.8.3.fix4`，改为读取统一版本变量。
- `debug.html` 标题更新为 fix8。
- `app.v2983.js` 页面标题、body class 与 `LN_APP_V2983.version` 更新为 fix8。
- `compute-pipeline.v2983.js` 的 debug 标记更新为 `v2983fix8`，避免报告误判。

## 说明

fix7 中启动资源内联修复仍保留；本版主要修正诊断显示与资源版本戳，不改动筛选业务口径。
