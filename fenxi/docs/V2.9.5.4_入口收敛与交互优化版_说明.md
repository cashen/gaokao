# V2.9.5.4｜规则中心落地与场景保护修正版

本版基于用户上传的 V2.9.5.3_textfix 工程包继续修订，保持原有数据、筛选、A/B/C 方案、导出和 diagnostics 结构不变。

## 调整范围

1. 移除页面标题、页脚中的“旧版调试副标题”遗留命名。
2. 顶部可见区域不再展示旧版访问标签字样。
3. 访问输入框、访问状态提示不再暗示具体凭证。
4. 顶部标签旧版规则标签改为“家长初筛规则”。
5. 数据状态文案由“2026本科目录+招生名已复核”调整为“本科目录与招生名已复核”。
6. “重置条件”按钮改为柔和绿色样式。
7. 主资产文件名升级为 v2954fix2，减少浏览器或 Cloudflare 旧缓存影响。

## 未改动范围

- 不改投档数据。
- 不改一分一段位次匹配。
- 不改 A/B/C 方案盘逻辑。
- 不改高校专项默认隐藏逻辑。
- 不改招生名称 / 本科目录 / 研究生参考三层模型。
- 不改 CSV / PNG 导出口径。
