# V2.9RC.fix-safeperf1 验证清单

## 1. 版本

- 页面标题显示 `V2.9RC.fix-safeperf1`
- `/fenxi/debug.html` 显示 `V2.9RC.fix-safeperf1`
- `VERSION.txt` 版本戳为 `29rc-safeperf1-20260513`

## 2. 必须确认

- 不存在 `fenxi/v3/`
- `compute-pipeline.v2983.js` 未改动公式
- `plan-engine.v297fix2.js` 未改动 A/B/C 规则
- `taxonomy_runtime/*` 未延后
- `school_geo_model/*` 未延后
- `chunks / manifest / rank_2025_physics` 完整

## 3. 三组样本

固定验证：

1. 625 分 / 全国 / 平台优先
2. 600 分 / 辽宁 / 稳就业
3. 500 分 / 辽宁 / 普通家庭 / 兴趣真实命中

对比旧版：

- 有效候选数量
- A/B/C 推荐
- 候选前 10
- 兴趣命中数量
- 地域 hard 结果

## 4. 性能

打开 `/fenxi/debug.html` 复制深度报告，重点看：

- `safePerfOpt=true`
- `safePerfVersion=29rc-safeperf1-20260513`
- `resourceSummary`
- `safePerf.majorNameModel.status`
- `safePerf.confusableModel.status`

## 5. 回退验证

把 `LN_SAFE_PERF_OPT` 改为 `false` 后，页面应恢复旧加载方式，候选结果不变。
