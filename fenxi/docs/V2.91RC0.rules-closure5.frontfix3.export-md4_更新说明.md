# V2.91RC0.rules-closure5.frontfix3.export-md4 更新说明

本版基于 V2.91RC0.rules-closure4.family-read1.frontfix2.export-md3。

## 业务修正

1. C 组新增机会项门槛：只收小冲、压线、匹配边缘或明确提档机会，不再用低分民办/强保底凑数。
2. B 组新增主讨论门槛：强保底/分数利用偏低不得显示为“最匹配主项”。
3. A/B/C 角色名按最终位次风险动态生成，不再按固定槽位硬命名。
4. 普通家庭/普通学费优先时，民办、独立学院、中外合作、高收费默认不进入 A/B/C 主方案。
5. 候选不足时不强行补足 6 个。
6. 重复候选按“学校+专业+招生专业代号”合并，只保留一个主身份，其他只作为来源。
7. Markdown 输出按最终裁决重新生成，不再保留矛盾标题。

## 前端修正

1. 登录成功后隐藏大号“进入工具”按钮。
2. “重新输入”降级为小链接。
3. 步骤条改为轻流程，去掉大按钮、彩色粗线和闯关感。
4. A/B/C 摘要卡去色块化，改为暖白灰蓝报告风。

## 边界

不改候选池、不改基础筛选、不改地域 hard、不改兴趣真实命中、不改 compute-pipeline/filter-engine/plan-engine。
