# V2.91RC0.rules-core1 验证清单

## 1. 版本检查

- 页面标题显示 V2.91RC0.rules-core1
- 页脚显示 V2.91RC0.rules-core1
- debug 页面标题显示 V2.91RC0.rules-core1
- 自测报告顶部显示 V2.91RC0.rules-core1
- VERSION.txt 显示 V2.91RC0.rules-core1

## 2. 规则包检查

- `rules-path-early-core.v291rc0rules1.js` 存在
- `rules-interest-core.v291rc0rules1.js` 存在
- `rules-path-core.v291rc0rules1.js` 存在
- `rules-decision-core.v291rc0rules1.js` 存在
- `rules-detail-model-early-core.v291rc0rules1.js` 存在
- `rules-detail-export-late-core.v291rc0rules1.js` 存在
- `LN_RULES_BUNDLE_OPT = true`
- `LN_RULES_BUNDLE_VERSION = 291rc0-rules-core1-20260513`

## 3. deep 自测

运行 `/fenxi/debug.html` deep 自测，重点确认：

- rules-core1 分段规则包加载与导出检查 PASS
- 自动访问凭证登录 PASS
- 基础 DOM 覆盖 PASS
- 位次输入 + 辽宁 hard PASS
- 学生画像抽屉 PASS
- 孩子兴趣抽屉 PASS
- 只看真实命中兴趣方向 PASS
- 场景卡联动 PASS
- A/B/C、翻页、候选区基础联动 PASS
- 96 个矩阵场景 PASS

## 4. 业务一致性

与 V2.91RC0 对比：

- 有效候选数量不应异常变化
- A/B/C 推荐不应异常变化
- 兴趣真实命中逻辑不应失效
- 地域 hard 规则不应失效
- 导出入口不应失效

## 5. 回退验证

将：

```js
window.LN_RULES_BUNDLE_OPT = true;
```

改为：

```js
window.LN_RULES_BUNDLE_OPT = false;
```

刷新后应回到旧单文件规则加载方式。

