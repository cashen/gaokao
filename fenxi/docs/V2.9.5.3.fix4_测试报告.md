# V2.9.5.3.fix4 测试报告

## 已执行

```text
node --check assets/*.js
node tools/validate-v2953fix4-release.js
zip -T ln_physics_volunteer_tool_v2953_fix3_deploy_root.zip
```

## 静态校验项

- `index.html` 只加载 fix3 模块。
- 生产包不包含旧 `app.v2953.js / app.v2953fix1.js / app.v2953fix2.js`。
- `data-engine.v2953fix4.js` 不包含 `autoRefresh` / `debouncedAutoRefresh`。
- `render.v2953fix4.js` 不导出 `renderStructure`。
- `app.v2953fix4.js` 不包含 `const debouncedAutoRefresh`。
- `diagnostics.html` 存在。

## 人工浏览器建议

1. 打开 `diagnostics.html`，确认模块检查为通过。
2. 打开 `index.html`。
3. 输入 `ln2025`，点击开启工具或按 Enter。
4. 输入分数 530 或 610，查看 A/B/C 方案盘。
5. 展开高级筛选，加入自选，测试 CSV / PNG 导出。
