# V3.0.0.alpha6.fix3｜Debug一键总检与复制流程简化版

本版基于 `V3.0.0.alpha5.fix2` 的决策上下文继续开发 Step5 A/B/C 方案包，修复线上可能因浏览器缓存继续加载旧 alpha6 简化场景逻辑的问题。

## 重点

- 版本戳更新为 `v300alpha6fix3-20260512`。
- 所有 v3 CSS / JS 资源在 `index.html`、`debug.html`、`debug.htm`、`debug/index.html` 中追加版本查询参数，强制刷新资源。
- Debug 增加旧逻辑回归检查：如果 Step4 回退到旧的 5 路径场景矩阵或 `grid` 简化推荐，会在自测中暴露。
- Step5 继续按分数段、家庭路径、地域选择强度、孩子兴趣、学生画像和专业画像生成 A/B/C 方案包。

## 当前主流程预期

`56548 / 500 → 辽宁 hard → 电气真实命中 → 省内公办稳妥 → A/B/C 方案包`

不应回退为：

`电气 → 电网 / 体制内倾向`
