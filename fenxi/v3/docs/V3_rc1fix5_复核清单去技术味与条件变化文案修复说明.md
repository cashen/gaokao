# V3.0.0.rc1.fix5｜复核清单去技术味与条件变化文案修正版

版本戳：`v300rc1fix5-20260512`

## 修复点

- 精简版“必须复核”不再输出 `复核证据等级`、`缺失数据`、`模型判断项` 等 debug 味文案。
- 复核任务合并为家庭可理解的三类：专业真实内容复核、招生章程复核、资格 / 费用 / 校区复核。
- 条件变化提示根据当前实际存在的对照项动态生成，不再在全国可比场景里固定提示“地域从只看改优先”。
- 保留完整版/debug 中的证据等级和模型判断信息，精简版只保留人话提醒。

## 不改范围

- 不改 Step4 家庭路径权重。
- 不改 A/B/C 方案包生成规则。
- 不改候选生成规则。
- 不启用旧 compute 替换。
- 不覆盖旧 `/fenxi/index.html`。
