name: Integrate LN 2026 runtime release

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]

permissions:
  contents: read

jobs:
  integrate:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "LN 2026 runtime integration is frozen after the verified generated commit; final corrections are validated by the release test workflow."