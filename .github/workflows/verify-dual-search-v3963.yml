name: Verify unified family action v3970.0

on:
  pull_request:
    branches: [main]
    paths:
      - '.production-acceptance-family-only'

permissions:
  contents: read

jobs:
  family-action-runtime:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'not used by one-time production acceptance PR'
