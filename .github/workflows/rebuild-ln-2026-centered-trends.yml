name: Rebuild LN 2026 centered trends

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]

permissions:
  contents: read

jobs:
  rebuild:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "The centered 2024-2026 trend analysis has been generated, verified and frozen."
