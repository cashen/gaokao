name: Archived simulation report regression

# Historical regression workflow. Current page verification is centralized in
# Verify simulation workspace v016. It is retained for manual forensic reruns.
on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical simulation-report workflow; use the canonical simulation workspace gate for current changes."
