name: Verify LN 2026 final release v3970.0

on:
  pull_request:
    branches: [main]
    paths:
      - '.production-acceptance-full-release-only'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'not used by one-time production acceptance PR'
