name: Apply LN 2026 runtime corrections

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]

permissions:
  contents: read

jobs:
  correct:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "The deterministic LN 2026 correction pass has completed and is frozen."
