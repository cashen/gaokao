# V3.9.45 自选池拖拽排序工作台完整包

## 目标

解决 V3.9.44 只靠上移/下移整理过慢的问题，把自选池整理页升级为“拖拽 + 快速移动 + 智能整理”的真实工作台。

## 核心改动

1. PC 端每行增加拖拽手柄，按住即可排序。
2. 触屏端长按手柄进入拖拽，避免和页面滚动冲突。
3. 每行保留上移/下移兜底，并新增“移动”菜单。
4. 移动菜单支持置顶、上移 5 位、移到第 N 位、下移 5 位、置底。
5. 顶部新增“按冲稳保整理”，用于先自动归类，再人工微调。
6. 首页入口逻辑保持 V3.9.44：右侧胶囊直接进入完整整理页，不弹半屏抽屉。
7. 飞书报告继续读取最终排序结果。

## 部署检查

- `/ln-rank/`：右侧自选池入口仍直接跳转完整工作台。
- `/ln-rank/selection-pool.html`：拖拽、移动菜单、按冲稳保整理、飞书发送均可用。
- `/api/path-analysis`：排序诊断接口。
- `/api/feishu-create-selection-pool-report`：自选池/诊断飞书报告接口。
