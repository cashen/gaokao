# V3.942 紧凑排序工作台与飞书归位

本版本基于 V3.941 完整包继续打包，仍为 `/ln-rank/` 可部署完整包，不是增量包。专业数据仍读取 `/fenxi` 已接入的辽宁 2025 物理类院校专业数据。

## 本版目标

把自选池工作台从“大卡片展示页”改成真正适合排序的紧凑工作台，并把飞书动作按用户理解重新归位：

- 左侧整理当前顺序；
- 左侧发送当前排序清单到飞书；
- 右侧检查冲稳保风险；
- 右侧发送完整诊断报告到飞书。

## 交互修正

1. 左侧自选池项从大卡片改为紧凑排序行。
2. 每条只保留：序号、学校、专业、冲稳保标签、分数、位次、相对分差、地域、上移、下移、删除。
3. “发送当前排序到飞书”放在左侧排序清单标题区。
4. 右侧不再出现“发送自选池到飞书”。
5. “生成路径分析”改为“检查当前排序”。
6. “分析后发送飞书”改为“发送诊断报告到飞书”。
7. 空状态说明改为：系统会帮你看是否冲得太多、稳得是否接得住、保底是否够用。

## 多终端

- PC ≥ 1180px：双栏，左侧排序清单更宽，右侧诊断区更窄。
- 768px - 1179px：上下结构，先排序清单，后诊断区。
- < 768px：单列，按钮纵向排列，排序项压缩但保持可点。

## 飞书报告

继续使用 `/api/feishu-create-selection-pool-report`：

- `selectionPoolOnly`：发送当前排序清单。
- `selectionPoolWithAnalysis`：如果未诊断，则先检查当前排序，再发送完整诊断报告。

报告口径仍说明：基于辽宁 2025 物理类历史录取数据和 `/fenxi` 已接入专业池，仅用于讨论与人工复核，不等同于录取预测。
